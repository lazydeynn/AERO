-- phpMyAdmin SQL Dump
-- version 5.2.1
-- https://www.phpmyadmin.net/
--
-- Host: 127.0.0.1
-- Generation Time: Jan 03, 2026 at 09:49 PM
-- Server version: 10.4.32-MariaDB
-- PHP Version: 8.2.12

SET SQL_MODE = "NO_AUTO_VALUE_ON_ZERO";
START TRANSACTION;
SET time_zone = "+00:00";


/*!40101 SET @OLD_CHARACTER_SET_CLIENT=@@CHARACTER_SET_CLIENT */;
/*!40101 SET @OLD_CHARACTER_SET_RESULTS=@@CHARACTER_SET_RESULTS */;
/*!40101 SET @OLD_COLLATION_CONNECTION=@@COLLATION_CONNECTION */;
/*!40101 SET NAMES utf8mb4 */;

--
-- Database: `aero_db`
--

-- --------------------------------------------------------

--
-- Table structure for table `feedback`
--

CREATE TABLE `feedback` (
  `feedback_id` int(11) NOT NULL,
  `user_id` int(11) NOT NULL,
  `message` text NOT NULL,
  `date_sent` datetime DEFAULT current_timestamp()
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_general_ci;

-- --------------------------------------------------------

--
-- Table structure for table `forum_replies`
--

CREATE TABLE `forum_replies` (
  `reply_id` int(11) NOT NULL,
  `thread_id` int(11) DEFAULT NULL,
  `user_id` int(11) DEFAULT NULL,
  `body` text NOT NULL,
  `created_at` datetime DEFAULT current_timestamp()
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_general_ci;

-- --------------------------------------------------------

--
-- Table structure for table `forum_threads`
--

CREATE TABLE `forum_threads` (
  `thread_id` int(11) NOT NULL,
  `user_id` int(11) DEFAULT NULL,
  `title` varchar(255) NOT NULL,
  `body` text NOT NULL,
  `created_at` datetime DEFAULT current_timestamp()
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_general_ci;

-- --------------------------------------------------------

--
-- Table structure for table `generated_quizzes`
--

CREATE TABLE `generated_quizzes` (
  `quiz_id` int(11) NOT NULL,
  `user_id` int(11) NOT NULL,
  `topic_id` int(11) NOT NULL,
  `score` int(11) DEFAULT 0,
  `total_items` int(11) DEFAULT 5,
  `date_taken` datetime DEFAULT current_timestamp(),
  `duration_seconds` int(11) DEFAULT 0
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_general_ci;

-- --------------------------------------------------------

--
-- Table structure for table `languages`
--

CREATE TABLE `languages` (
  `language_id` int(11) NOT NULL,
  `name` varchar(50) NOT NULL,
  `description` text DEFAULT NULL,
  `icon_path` varchar(255) DEFAULT NULL
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_general_ci;

--
-- Dumping data for table `languages`
--

INSERT INTO `languages` (`language_id`, `name`, `description`, `icon_path`) VALUES
(1, 'Python', 'A high-level, versatile language known for simplicity and data science.', 'python.png'),
(2, 'C#', 'A modern, object-oriented language by Microsoft for Windows apps and games.', 'cs.png'),
(3, 'C++', 'A powerful, high-performance language used for system software and games.', 'c++.png'),
(4, 'JavaScript', 'The language of the web, used to create interactive and dynamic content.', 'javascript.png'),
(5, 'Java', 'A versatile, object-oriented language used for enterprise and Android apps.', 'java.png'),
(6, 'Dart', 'A client-optimized language for fast apps on any platform (Flutter).', 'dart.png');

-- --------------------------------------------------------

--
-- Table structure for table `password_resets`
--

CREATE TABLE `password_resets` (
  `id` int(11) NOT NULL,
  `email` varchar(255) NOT NULL,
  `token` varchar(64) NOT NULL,
  `expiry` datetime NOT NULL
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_general_ci;

-- --------------------------------------------------------

--
-- Table structure for table `quiz_questions`
--

CREATE TABLE `quiz_questions` (
  `question_id` int(11) NOT NULL,
  `quiz_id` int(11) NOT NULL,
  `question_text` text NOT NULL,
  `option_a` varchar(255) NOT NULL,
  `option_b` varchar(255) NOT NULL,
  `option_c` varchar(255) NOT NULL,
  `option_d` varchar(255) NOT NULL,
  `correct_option` char(1) NOT NULL,
  `user_answer` char(1) DEFAULT NULL
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_general_ci;

-- --------------------------------------------------------

--
-- Table structure for table `reports`
--

CREATE TABLE `reports` (
  `report_id` int(11) NOT NULL,
  `user_id` int(11) DEFAULT NULL,
  `target_type` enum('thread','reply') NOT NULL,
  `target_id` int(11) NOT NULL,
  `reason` text NOT NULL,
  `status` enum('pending','resolved') DEFAULT 'pending',
  `date_reported` datetime DEFAULT current_timestamp()
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_general_ci;

-- --------------------------------------------------------

--
-- Table structure for table `saved_lessons`
--

CREATE TABLE `saved_lessons` (
  `save_id` int(11) NOT NULL,
  `user_id` int(11) DEFAULT NULL,
  `topic_id` int(11) DEFAULT NULL,
  `date_saved` datetime DEFAULT current_timestamp()
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_general_ci;

-- --------------------------------------------------------

--
-- Table structure for table `topics`
--

CREATE TABLE `topics` (
  `topic_id` int(11) NOT NULL,
  `language_id` int(11) NOT NULL,
  `title` varchar(100) NOT NULL,
  `content_description` text DEFAULT NULL,
  `pdf_file_path` varchar(255) DEFAULT NULL,
  `external_link` varchar(255) DEFAULT NULL,
  `ai_context_summary` text NOT NULL,
  `order_sequence` int(11) DEFAULT 0
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_general_ci;

--
-- Dumping data for table `topics`
--

INSERT INTO `topics` (`topic_id`, `language_id`, `title`, `content_description`, `pdf_file_path`, `external_link`, `ai_context_summary`, `order_sequence`) VALUES
(10, 1, 'Python Introduction', '<div class=\"topic-content-area\">\r\n    <h2>What is Python?</h2>\r\n    <p>Python is a popular, high-level programming language created by <b>Guido van Rossum</b> and released in <b>1991</b>. It is designed for readability and simplicity.</p>\r\n    \r\n    <div class=\"w3-example\">\r\n        <h3>Common Uses:</h3>\r\n        <ul>\r\n            <li><b>Web Development:</b> Server-side applications.</li>\r\n            <li><b>Software Development:</b> Creating workflows and production software.</li>\r\n            <li><b>Data Science:</b> Mathematics and big data handling.</li>\r\n            <li><b>System Scripting:</b> Automating tasks and connecting to databases.</li>\r\n        </ul>\r\n    </div>\r\n\r\n    <hr class=\"my-8 border-gray-100\">\r\n\r\n    <h2>Why Choose Python?</h2>\r\n    <p>Python is favored by developers because it is versatile and efficient:</p>\r\n    <ul>\r\n        <li><b>Cross-Platform:</b> Works on Windows, Mac, Linux, and Raspberry Pi.</li>\r\n        <li><b>Simple Syntax:</b> Very similar to the English language, allowing for fewer lines of code.</li>\r\n        <li><b>Interpreted System:</b> Code is executed as soon as it is written, making prototyping very fast.</li>\r\n        <li><b>Flexible Paradigms:</b> Supports procedural, object-oriented, and functional programming.</li>\r\n    </ul>\r\n\r\n    <hr class=\"my-8 border-gray-100\">\r\n\r\n    <h2>Syntax Comparison</h2>\r\n    <p>Python differs from other languages in a few key ways:</p>\r\n    \r\n    <div class=\"w3-example\">\r\n        <p><b>New Lines:</b> Python uses new lines to complete a command (no semicolons required).</p>\r\n        <p><b>Indentation:</b> Python relies on whitespace/indentation to define scope (loops, functions, classes) instead of curly brackets <code>{}</code>.</p>\r\n    </div>\r\n\r\n    <div class=\"w3-example bg-gray-50\">\r\n        <h3>First Program Example</h3>\r\n        <pre><code>print(\"Hello, World!\")</code></pre>\r\n    </div>\r\n\r\n    <div class=\"bg-blue-50 p-4 border-l-4 border-blue-500 rounded-r-lg mt-6\">\r\n        <p class=\"text-sm\"><b>Good to know:</b> This tutorial uses <b>Python 3</b>. While code can be written in simple text editors, larger projects often use IDEs like Pycharm or Thonny.</p>\r\n    </div>\r\n</div>', NULL, NULL, 'Topic: Introduction to Python Key Concepts:\r\n\r\nOrigin: Created by Guido van Rossum, released in 1991.\r\n\r\nCore Capabilities: Web/Software development, database connection, file modification, big data, and complex math.\r\n\r\nFeatures: Cross-platform compatibility, English-like syntax, interpreted nature (fast prototyping), supports multiple programming styles (OOP, functional, procedural).\r\n\r\nSyntax Rules: Uses new lines for command completion (unlike semicolons in C++/Java); uses indentation for scope (unlike curly brackets).\r\n\r\nVersioning: Focus on Python 3. Quiz Focus: History and creators of Python, its use cases, unique syntax features (indentation vs brackets), and the benefits of being an interpreted language.', 1),
(11, 1, 'Python Syntax', '<div class=\"topic-content-area\">\r\n    <h2>Execute Python Syntax</h2>\r\n    <p>Python syntax can be executed by writing directly in the Command Line:</p>\r\n    \r\n    <div class=\"w3-example\">\r\n        <h3>Command Line Execution</h3>\r\n        <pre><code>>>> print(\"Hello, World!\")\r\nHello, World!</code></pre>\r\n    </div>\r\n\r\n    <p>Alternatively, you can create a Python file on the server using the <b>.py</b> file extension and run it via the Command Line:</p>\r\n    \r\n    <pre><code>C:\\Users\\Your Name>python myfile.py</code></pre>\r\n\r\n    <hr class=\"my-8 border-gray-100\">\r\n\r\n    <h2>Python Indentation</h2>\r\n    <p>Indentation refers to the spaces at the beginning of a code line. In Python, indentation is <b>mandatory</b> and is used to indicate a block of code.</p>\r\n\r\n    <div class=\"w3-example\">\r\n        <h3>Correct Indentation</h3>\r\n        <pre><code>if 5 > 2:\r\n  print(\"Five is greater than two!\")</code></pre>\r\n    </div>\r\n\r\n    <div class=\"w3-example border-l-red-500 bg-red-50\">\r\n        <h3 class=\"text-red-600\">Syntax Error Example</h3>\r\n        <p class=\"text-xs mb-2\">Python will give you an error if you skip the indentation:</p>\r\n        <pre><code>if 5 > 2:\r\nprint(\"Five is greater than two!\")</code></pre>\r\n    </div>\r\n\r\n    <p>While the number of spaces is up to you (common use is four), it must be at least one. Most importantly, you must use the <b>same number of spaces</b> within the same block of code.</p>\r\n\r\n    <hr class=\"my-8 border-gray-100\">\r\n\r\n    <h2>Python Variables</h2>\r\n    <p>In Python, variables are created the moment you assign a value to them. Python has no command for declaring a variable.</p>\r\n\r\n    <div class=\"w3-example\">\r\n        <h3>Variable Assignment</h3>\r\n        <pre><code>x = 5\r\ny = \"Hello, World!\"</code></pre>\r\n    </div>\r\n\r\n    <hr class=\"my-8 border-gray-100\">\r\n\r\n    <h2>Comments</h2>\r\n    <p>Python uses comments for in-code documentation. Comments start with a <b>#</b>, and Python will ignore the rest of that line.</p>\r\n\r\n    <div class=\"w3-example\">\r\n        <h3>Example</h3>\r\n        <pre><code># This is a comment.\r\nprint(\"Hello, World!\")</code></pre>\r\n    </div>\r\n</div>', NULL, NULL, 'Topic: Python Syntax, Indentation, Variables, and Comments Key Concepts:\r\n\r\nExecution: Python code can be executed directly in the Command Line (interactive mode) or by running scripts saved with a .py extension.\r\n\r\nIndentation: Unlike many other languages, Python uses indentation to define code blocks. A missing or inconsistent indentation level within a block results in a SyntaxError.\r\n\r\nVariables: Variables are initialized upon assignment; there is no specific keyword for declaration.\r\n\r\nComments: The # symbol denotes the start of a comment; the interpreter ignores all text following it on that line. Quiz Focus: Validating correct code block indentation, identifying variable assignment syntax, and understanding the role of comments in Python scripts.', 2),
(12, 1, 'Python Statements', '<div class=\"topic-content-area\">\r\n    <h2>Python Statements</h2>\r\n    <p>A computer program is a list of \"instructions\" to be \"executed\" by a computer. In a programming language, these instructions are called <b>statements</b>.</p>\r\n    \r\n    <div class=\"w3-example\">\r\n        <h3>Simple Statement</h3>\r\n        <p>The following statement prints text to the screen:</p>\r\n        <pre><code>print(\"Python is fun!\")</code></pre>\r\n    </div>\r\n\r\n    <p>In Python, a statement usually ends when the line ends. Unlike languages like Java or C, you do not need to use a semicolon (<b>;</b>).</p>\r\n\r\n    <hr class=\"my-8 border-gray-100\">\r\n\r\n    <h2>Executing Many Statements</h2>\r\n    <p>Most programs contain many statements. Python executes them one by one, in the exact order they are written.</p>\r\n\r\n    <div class=\"w3-example\">\r\n        <h3>Execution Order Example</h3>\r\n        <pre><code>print(\"Hello World!\")\r\nprint(\"Have a good day.\")\r\nprint(\"Learning Python is fun!\")</code></pre>\r\n    </div>\r\n\r\n    <div class=\"bg-blue-50 p-4 rounded-lg text-sm mb-6\">\r\n        <p><b>How it works:</b> The first line is executed first, followed by the second, and finally the third.</p>\r\n    </div>\r\n\r\n    <hr class=\"my-8 border-gray-100\">\r\n\r\n    <h2>Semicolons in Python</h2>\r\n    <p>Semicolons are <b>optional</b>. While you can use them to put multiple statements on one line, it is considered bad practice because it reduces readability.</p>\r\n\r\n    <div class=\"w3-example\">\r\n        <h3>Rarely Used Syntax</h3>\r\n        <pre><code>print(\"Hello\"); print(\"How are you?\"); print(\"Bye bye!\")</code></pre>\r\n    </div>\r\n\r\n    <div class=\"w3-example border-l-red-500 bg-red-50\">\r\n        <h3 class=\"text-red-600\">Syntax Error Example</h3>\r\n        <p class=\"text-xs mb-2\">Placing two statements on one line without a separator (newline or semicolon) causes an error:</p>\r\n        <pre><code>print(\"Python is fun!\") print(\"Really!\")</code></pre>\r\n        <p class=\"text-[10px] text-red-500 font-bold mt-1\">Result: SyntaxError: invalid syntax</p>\r\n    </div>\r\n\r\n    <div class=\"bg-green-50 p-4 border-l-4 border-green-500 rounded-r-lg mt-6\">\r\n        <p class=\"text-sm\"><b>Best Practice:</b> Always put each statement on its own line to keep your code clean and easy to understand.</p>\r\n    </div>\r\n</div>', NULL, NULL, 'Topic: Python Statements Key Concepts:\r\n\r\nDefinition: Statements are programming instructions that the computer executes.\r\n\r\nLine Termination: In Python, a newline character typically signifies the end of a statement; semicolons are not required.\r\n\r\nExecution Flow: Statements are executed sequentially from top to bottom.\r\n\r\nMultiple Statements: Multiple statements can technically exist on one line if separated by a semicolon (;), but this is not recommended.\r\n\r\nSyntax Errors: Placing multiple statements on a single line without any separator (neither a newline nor a semicolon) triggers a SyntaxError. Quiz Focus: Identifying what constitutes a statement, predicting the output order of multiple print() calls, and recognizing valid vs. invalid ways to separate statements on a single line.', 3),
(14, 1, 'Python Variables 1.1', '<div class=\"topic-content-area\">\r\n    <h2>Assign Multiple Values</h2>\r\n    <p>Python allows you to assign values to multiple variables in one line, making your code more concise.</p>\r\n    \r\n    <div class=\"w3-example\">\r\n        <h3>Many Values to Multiple Variables</h3>\r\n        <pre><code>x, y, z = \"Orange\", \"Banana\", \"Cherry\"\r\nprint(x)\r\nprint(y)\r\nprint(z)</code></pre>\r\n    </div>\r\n\r\n    <p><b>Note:</b> Ensure the number of variables matches the number of values, or Python will give an error.</p>\r\n\r\n    <hr class=\"my-8 border-gray-100\">\r\n\r\n    <h2>One Value to Multiple Variables</h2>\r\n    <p>You can also assign the <b>same</b> value to multiple variables in one line:</p>\r\n\r\n    <div class=\"w3-example\">\r\n        <pre><code>x = y = z = \"Orange\"\r\nprint(x)\r\nprint(y)\r\nprint(z)</code></pre>\r\n    </div>\r\n\r\n    <hr class=\"my-8 border-gray-100\">\r\n\r\n    <h2>Unpacking a Collection</h2>\r\n    <p>If you have a collection of values in a list or tuple, Python allows you to extract the values into variables. This is called <b>unpacking</b>.</p>\r\n\r\n    <div class=\"w3-example\">\r\n        <h3>Example: Unpacking a List</h3>\r\n        <pre><code>fruits = [\"apple\", \"banana\", \"cherry\"]\r\nx, y, z = fruits\r\nprint(x)\r\nprint(y)\r\nprint(z)</code></pre>\r\n    </div>\r\n</div>', NULL, NULL, 'Topic: Python Variables - Multiple Assignments & Unpacking Key Concepts:\r\n\r\nAssigning different values to multiple variables in one line using commas.\r\n\r\nAssigning a single value to multiple variables simultaneously using consecutive equals signs (x = y = z).\r\n\r\nUnpacking: Extracting values from a list or tuple directly into separate variables. Quiz Focus: Correct syntax for multi-variable assignment, identifying errors when variable/value counts don\'t match, and the result of unpacking collections.', 4),
(15, 1, 'Python Variables 1.2', '<div class=\"topic-content-area\">\r\n    <h2>Output Variables</h2>\r\n    <p>The Python <code>print()</code> function is often used to output variables.</p>\r\n    \r\n    <div class=\"w3-example\">\r\n        <pre><code>x = \"Python is awesome\"\r\nprint(x)</code></pre>\r\n    </div>\r\n\r\n    <hr class=\"my-8 border-gray-100\">\r\n\r\n    <h2>Multiple Outputs</h2>\r\n    <p>In the <code>print()</code> function, you output multiple variables, separated by a comma:</p>\r\n\r\n    <div class=\"w3-example\">\r\n        <pre><code>x = \"Python\"\r\ny = \"is\"\r\nz = \"awesome\"\r\nprint(x, y, z)</code></pre>\r\n    </div>\r\n\r\n    <p>You can also use the <code>+</code> operator to output multiple variables:</p>\r\n    <div class=\"w3-example\">\r\n        <pre><code>print(x + y + z)</code></pre>\r\n    </div>\r\n    <p><b>Note:</b> Notice the space character after \"Python \" and \"is \", without them the result would be \"Pythonisawesome\".</p>\r\n\r\n    <div class=\"w3-example border-l-red-500 bg-red-50\">\r\n        <h3 class=\"text-red-600\">Common Error</h3>\r\n        <p class=\"text-xs mb-2\">For numbers, the <code>+</code> character works as a mathematical operator. You <b>cannot</b> combine a string and a number with the <code>+</code> operator:</p>\r\n        <pre><code>x = 5\r\ny = \"John\"\r\nprint(x + y) # This will throw a TypeError</code></pre>\r\n    </div>\r\n</div>', NULL, NULL, 'Topic: Python Output Variables Key Concepts:\r\n\r\nUsing print() to display variables.\r\n\r\nUsing commas in print() to output multiple items (automatically adds a space).\r\n\r\nUsing the + operator to concatenate strings (does not add a space).\r\n\r\nMathematical behavior of + with integers vs. string concatenation.\r\n\r\nTypeError: Occurs when trying to use + to combine a string and an integer. Quiz Focus: Predicting output of concatenated variables, identifying when a space will be automatically added (comma vs +), and recognizing the TypeError when mixing data types.', 5),
(16, 1, 'Python Variables 1.3', '<div class=\"topic-content-area\">\r\n    <h2>Global Variables</h2>\r\n    <p>Variables created outside of a function are known as <b>global variables</b>. They can be accessed by everyone, both inside and outside of functions.</p>\r\n    \r\n    <div class=\"w3-example\">\r\n        <h3>Example: Using a Global Variable</h3>\r\n        <pre><code>x = \"awesome\"\r\n\r\ndef myfunc():\r\n  print(\"Python is \" + x)\r\n\r\nmyfunc()</code></pre>\r\n    </div>\r\n\r\n    <hr class=\"my-8 border-gray-100\">\r\n\r\n    <h2>The <code>global</code> Keyword</h2>\r\n    <p>Normally, a variable created inside a function is local. To create a global variable inside a function, use the <b>global</b> keyword.</p>\r\n\r\n    <div class=\"w3-example\">\r\n        <h3>Example: Declaring a Global Variable Inside a Function</h3>\r\n        <pre><code>def myfunc():\r\n  global x\r\n  x = \"fantastic\"\r\n\r\nmyfunc()\r\nprint(\"Python is \" + x)</code></pre>\r\n    </div>\r\n\r\n    <p>Also, use the <code>global</code> keyword if you want to change a global variable inside a function.</p>\r\n</div>', NULL, NULL, 'Topic: Python Global Variables & Global Keyword Key Concepts:\r\n\r\nGlobal variables are defined outside functions and accessible globally.\r\n\r\nLocal variables defined inside a function with the same name as a global variable do not overwrite the global value outside that function\'s scope.\r\n\r\nThe global keyword allows a programmer to create or modify a global variable from within a function\'s local scope. Quiz Focus: Determining variable scope, predicting output when local and global names conflict, and identifying correct usage of the global keyword.', 6),
(17, 1, 'Python Data Types', '<div class=\"topic-content-area\">\r\n    <h2>Built-in Data Types</h2>\r\n    <p>In programming, data type is an important concept. Python has the following categories of built-in data types:</p>\r\n    \r\n    <div class=\"w3-example\">\r\n        <ul class=\"space-y-2\">\r\n            <li><b>Text Type:</b> <code>str</code></li>\r\n            <li><b>Numeric Types:</b> <code>int</code>, <code>float</code>, <code>complex</code></li>\r\n            <li><b>Sequence Types:</b> <code>list</code>, <code>tuple</code>, <code>range</code></li>\r\n            <li><b>Mapping Type:</b> <code>dict</code></li>\r\n            <li><b>Set Types:</b> <code>set</code>, <code>frozenset</code></li>\r\n            <li><b>Boolean Type:</b> <code>bool</code></li>\r\n            <li><b>Binary Types:</b> <code>bytes</code>, <code>bytearray</code>, <code>memoryview</code></li>\r\n        </ul>\r\n    </div>\r\n\r\n    <hr class=\"my-8 border-gray-100\">\r\n\r\n    <h2>Getting the Data Type</h2>\r\n    <p>You can get the data type of any object by using the <code>type()</code> function:</p>\r\n\r\n    <div class=\"w3-example\">\r\n        <pre><code>x = 5\r\nprint(type(x)) # Output: &lt;class \'int\'&gt;</code></pre>\r\n    </div>\r\n</div>', NULL, NULL, 'Topic: Python Built-in Data Types Key Concepts:\r\n\r\nPrimary categories: Text (str), Numeric (int, float, complex), Sequence (list, tuple, range), Mapping (dict), and Boolean (bool).\r\n\r\nVariables in Python do not require explicit type declaration; the type is set when a value is assigned.\r\n\r\nThe type() function is used to inspect the data type of a variable. Quiz Focus: Identifying data types based on value (e.g., \"Hello\" is str, 20.5 is float), and understanding the purpose of the type() function.', 7),
(18, 1, 'Python Numbers', '<div class=\"topic-content-area\">\r\n    <h2>Numeric Types</h2>\r\n    <p>There are three distinct numeric types in Python:</p>\r\n    \r\n    <div class=\"w3-example\">\r\n        <ul>\r\n            <li><b>int:</b> Whole numbers, positive or negative, without decimals (e.g., <code>10</code>, <code>-5</code>).</li>\r\n            <li><b>float:</b> Numbers containing one or more decimals; also includes scientific numbers with an \"e\" to indicate the power of 10 (e.g., <code>2.5</code>, <code>-35.59</code>, <code>35e3</code>).</li>\r\n            <li><b>complex:</b> Numbers written with a \"j\" as the imaginary part (e.g., <code>3+5j</code>).</li>\r\n        </ul>\r\n    </div>\r\n\r\n    <hr class=\"my-8 border-gray-100\">\r\n\r\n    <h2>Random Number</h2>\r\n    <p>Python does not have a <code>random()</code> function to make a random number, but it has a built-in module called <code>random</code> that can be used to make random numbers:</p>\r\n\r\n    <div class=\"w3-example\">\r\n        <pre><code>import random\r\nprint(random.randrange(1, 10))</code></pre>\r\n    </div>\r\n</div>', NULL, NULL, 'Topic: Python Numbers (Int, Float, Complex) and Random Module Key Concepts:\r\n\r\nInteger (int): Whole numbers of unlimited length.\r\n\r\nFloat: Floating point numbers; scientific notation with \'e\' is supported.\r\n\r\nComplex: Formatted as (real part) + (imaginary part)j.\r\n\r\nType Conversion: Converting between types using int(), float(), and complex().\r\n\r\nRandomness: Using import random and random.randrange() to generate values within a range. Quiz Focus: Distinguishing between numeric types, identifying valid scientific notation, and understanding the syntax for generating random integers.', 8),
(19, 1, 'Python Casting', '<div class=\"topic-content-area\">\r\n    <h2>Specify a Variable Type</h2>\r\n    <p>There may be times when you want to specify a type onto a variable. This is done using constructor functions:</p>\r\n    \r\n    <div class=\"w3-example\">\r\n        <ul>\r\n            <li><code>int()</code> - constructs an integer from an integer literal, a float literal (by removing all decimals), or a string literal.</li>\r\n            <li><code>float()</code> - constructs a float from an integer literal, a float literal or a string literal.</li>\r\n            <li><code>str()</code> - constructs a string from a wide variety of data types, including strings, integer literals and float literals.</li>\r\n        </ul>\r\n    </div>\r\n\r\n    <div class=\"w3-example bg-gray-50\">\r\n        <h3>Example</h3>\r\n        <pre><code>x = int(2.8) # x will be 2\r\ny = float(\"3\") # y will be 3.0\r\nz = str(3.0) # z will be \'3.0\'</code></pre>\r\n    </div>\r\n</div>', NULL, NULL, 'Topic: Python Casting (Type Conversion) Key Concepts:\r\n\r\nUsing int() to round down floats to the nearest whole number.\r\n\r\nUsing float() to ensure a number has a decimal point.\r\n\r\nUsing str() to convert numbers to text for concatenation or display. Quiz Focus: Predicting the result of int(2.9), converting string-based numbers to actual math-ready floats, and the behavior of casting numeric strings.', 9),
(20, 1, 'Python Strings', '<div class=\"topic-content-area\">\r\n    <h2>Strings in Python</h2>\r\n    <p>Strings in python are surrounded by either single quotation marks, or double quotation marks.</p>\r\n    \r\n    <div class=\"w3-example\">\r\n        <pre><code>print(\"Hello\")\r\nprint(\'Hello\')</code></pre>\r\n    </div>\r\n\r\n    <h2>Multiline Strings</h2>\r\n    <p>You can assign a multiline string to a variable by using three quotes:</p>\r\n\r\n    <div class=\"w3-example\">\r\n        <pre><code>a = \"\"\"This is a\r\nmultiline string\r\nin Python.\"\"\"</code></pre>\r\n    </div>\r\n\r\n    <hr class=\"my-8 border-gray-100\">\r\n\r\n    <h2>Strings are Arrays</h2>\r\n    <p>Like many other popular programming languages, strings in Python are arrays of bytes representing unicode characters. You can access elements of the string using square brackets.</p>\r\n\r\n    <div class=\"w3-example\">\r\n        <pre><code>a = \"Hello, World!\"\r\nprint(a[1]) # Outputs \'e\'</code></pre>\r\n    </div>\r\n</div>', NULL, NULL, 'Topic: Python Strings Basics Key Concepts:\r\n\r\nDefinition: Text literals using \' \' or \" \".\r\n\r\nMultiline Strings: Use triple quotes (\"\"\" or \'\'\').\r\n\r\nArray Nature: Strings are indexed starting at 0.\r\n\r\nString Length: Checked using the len() function.\r\n\r\nCheck String: Using keywords in or not in to find substrings. Quiz Focus: Indexing (e.g., what is text[0]), calculating len(), and boolean results of the in operator.', 10),
(21, 1, 'Python Booleans', '<div class=\"topic-content-area\">\r\n    <h2>Boolean Values</h2>\r\n    <p>Booleans represent one of two values: <b>True</b> or <b>False</b>. You often need to know if an expression is True or False when evaluating code.</p>\r\n    \r\n    <div class=\"w3-example\">\r\n        <pre><code>print(10 > 9) # Returns True\r\nprint(10 == 9) # Returns False</code></pre>\r\n    </div>\r\n\r\n    <hr class=\"my-8 border-gray-100\">\r\n\r\n    <h2>Most Values are True</h2>\r\n    <p>Almost any value is evaluated to <code>True</code> if it has some sort of content.</p>\r\n    <ul>\r\n        <li>Any string is <code>True</code>, except empty strings.</li>\r\n        <li>Any number is <code>True</code>, except <code>0</code>.</li>\r\n        <li>Any list, tuple, set, and dictionary are <code>True</code>, except empty ones.</li>\r\n    </ul>\r\n\r\n    <div class=\"w3-example border-l-red-500 bg-red-50\">\r\n        <h3>Values that evaluate to False:</h3>\r\n        <p><code>bool(False)</code>, <code>bool(None)</code>, <code>bool(0)</code>, <code>bool(\"\")</code>, <code>bool(())</code>, <code>bool([])</code>, <code>bool({})</code></p>\r\n    </div>\r\n</div>', NULL, NULL, 'Topic: Python Booleans Key Concepts:\r\n\r\nBoolean values: True and False.\r\n\r\nComparison: Evaluation using ==, >, <, etc.\r\n\r\nTruthiness: Concepts of which values evaluate to True vs False (0, empty sequences, and None are False).\r\n\r\nbool() function: Used to evaluate any value. Quiz Focus: Predicting boolean results of comparisons and identifying \"Falsey\" values like empty lists or the number zero.', 11),
(22, 1, 'Python Operators', '<div class=\"topic-content-area\">\r\n    <h2>Arithmetic Operators</h2>\r\n    <p>Arithmetic operators are used with numeric values to perform common mathematical operations:</p>\r\n    \r\n    <div class=\"w3-example\">\r\n        <table class=\"w-full text-sm text-left\">\r\n            <tr class=\"border-b\"><th class=\"py-2\">Operator</th><th class=\"py-2\">Name</th><th class=\"py-2\">Example</th></tr>\r\n            <tr class=\"border-b\"><td>+</td><td>Addition</td><td>x + y</td></tr>\r\n            <tr class=\"border-b\"><td>-</td><td>Subtraction</td><td>x - y</td></tr>\r\n            <tr class=\"border-b\"><td>*</td><td>Multiplication</td><td>x * y</td></tr>\r\n            <tr class=\"border-b\"><td>/</td><td>Division</td><td>x / y</td></tr>\r\n            <tr class=\"border-b\"><td>%</td><td>Modulus</td><td>x % y</td></tr>\r\n            <tr class=\"border-b\"><td>**</td><td>Exponentiation</td><td>x ** y</td></tr>\r\n            <tr class=\"border-b\"><td>//</td><td>Floor division</td><td>x // y</td></tr>\r\n        </table>\r\n    </div>\r\n\r\n    <hr class=\"my-8 border-gray-100\">\r\n\r\n    <h2>Assignment & Logical Operators</h2>\r\n    <p>Assignment operators are used to assign values to variables (e.g., <code>+=</code>, <code>-=</code>). Logical operators are used to combine conditional statements (<code>and</code>, <code>or</code>, <code>not</code>).</p>\r\n</div>', NULL, NULL, 'Topic: Python Operators Key Concepts:\r\n\r\nArithmetic: Basic math including exponentiation (**) and floor division (//).\r\n\r\nComparison: ==, !=, >, <, >=, <=.\r\n\r\nLogical: and, or, not.\r\n\r\nIdentity: is, is not (checks if objects are the same).\r\n\r\nMembership: in, not in (checks if a sequence is present). Quiz Focus: Differentiating between / (float division) and // (floor division), using modulus (%) to find remainders, and applying logical operators to complex conditions.', 12),
(23, 1, 'Python Lists', '<div class=\"topic-content-area\">\r\n    <h2>Python Lists</h2>\r\n    <p>Lists are used to store multiple items in a single variable. They are <b>ordered</b>, <b>changeable</b>, and <b>allow duplicate values</b>.</p>\r\n    \r\n    <div class=\"w3-example\">\r\n        <h3>Example: Create a List</h3>\r\n        <pre><code>thislist = [\"apple\", \"banana\", \"cherry\"]\r\nprint(thislist)</code></pre>\r\n    </div>\r\n\r\n    <hr class=\"my-8 border-gray-100\">\r\n\r\n    <h2>Access and Change Items</h2>\r\n    <p>List items are indexed, and you can access them by referring to the index number (starting at 0):</p>\r\n\r\n    <div class=\"w3-example\">\r\n        <pre><code>print(thislist[1]) # Outputs \"banana\"\r\nthislist[1] = \"blackcurrant\" # Changes banana to blackcurrant</code></pre>\r\n    </div>\r\n\r\n    <div class=\"bg-blue-50 p-4 rounded-lg text-sm mt-6\">\r\n        <p><b>Useful Methods:</b> Use <code>.append()</code> to add items, <code>.remove()</code> to delete them, and <code>len()</code> to get the list size.</p>\r\n    </div>\r\n</div>', NULL, NULL, 'Topic: Python Lists Key Concepts:\r\n\r\nDefinition: Ordered, changeable collections that allow duplicates.\r\n\r\nIndexing: Items are zero-indexed; support negative indexing (-1 for last item).\r\n\r\nMutability: Elements can be modified, added, or removed.\r\n\r\nList Length: Measured with the len() function. Quiz Focus: Identifying list characteristics, predicting output of indexed access, and understanding common methods like append or insert.', 13),
(24, 1, 'Python Tuples', '<div class=\"topic-content-area\">\r\n    <h2>Python Tuples</h2>\r\n    <p>Tuples are used to store multiple items in a single variable. A tuple is a collection which is <b>ordered</b> and <b>unchangeable (immutable)</b>.</p>\r\n    \r\n    <div class=\"w3-example\">\r\n        <h3>Example: Create a Tuple</h3>\r\n        <pre><code>thistuple = (\"apple\", \"banana\", \"cherry\")\r\nprint(thistuple)</code></pre>\r\n    </div>\r\n\r\n    <hr class=\"my-8 border-gray-100\">\r\n\r\n    <h2>Unchangeable Nature</h2>\r\n    <p>Once a tuple is created, you cannot change, add, or remove items. This is the main difference between a List and a Tuple.</p>\r\n\r\n    <div class=\"w3-example border-l-red-500 bg-red-50\">\r\n        <h3 class=\"text-red-600\">Important Tip</h3>\r\n        <p class=\"text-xs\">To create a tuple with only <b>one</b> item, you must add a comma after the item, otherwise Python will not recognize it as a tuple:</p>\r\n        <pre><code>thistuple = (\"apple\",) # Correct\r\nnot_a_tuple = (\"apple\") # Wrong - this is just a string</code></pre>\r\n    </div>\r\n</div>', NULL, NULL, 'Topic: Python Tuples Key Concepts:\r\n\r\nDefinition: Ordered, unchangeable (immutable) collections that allow duplicates.\r\n\r\nImmutability: Items cannot be modified once created.\r\n\r\nSingle Item Tuple: Requires a trailing comma (e.g., (\"val\",)). Quiz Focus: Distinguishing Tuples from Lists, identifying the error when trying to modify a tuple, and the specific syntax for single-element tuples.', 14),
(25, 1, 'Python Sets', '<div class=\"topic-content-area\">\r\n    <h2>Python Sets</h2>\r\n    <p>A set is a collection which is <b>unordered</b>, <b>unindexed</b>, and <b>does not allow duplicate values</b>.</p>\r\n    \r\n    <div class=\"w3-example\">\r\n        <h3>Example: Create a Set</h3>\r\n        <pre><code>thisset = {\"apple\", \"banana\", \"cherry\"}\r\nprint(thisset)</code></pre>\r\n    </div>\r\n\r\n    <hr class=\"my-8 border-gray-100\">\r\n\r\n    <h2>No Duplicates</h2>\r\n    <p>Sets cannot have two items with the same value. If you try to add a duplicate, it will be ignored:</p>\r\n\r\n    <div class=\"w3-example\">\r\n        <pre><code>thisset = {\"apple\", \"banana\", \"cherry\", \"apple\"}\r\nprint(thisset) # Output: {\'apple\', \'banana\', \'cherry\'}</code></pre>\r\n    </div>\r\n\r\n    <p><b>Note:</b> Since sets are unordered, the items will appear in a random order every time you print them.</p>\r\n</div>', NULL, NULL, 'Topic: Python Sets Key Concepts:\r\n\r\nDefinition: Unordered, unindexed collections.\r\n\r\nDuplicates: Not allowed; redundant items are automatically removed.\r\n\r\nAccess: Cannot access items via index; must use a loop or the in keyword. Quiz Focus: Recognizing set behavior regarding duplicate values and understanding that they are unindexed (random order).', 15),
(26, 1, 'Python Dictionaries', '<div class=\"topic-content-area\">\r\n    <h2>Python Dictionaries</h2>\r\n    <p>Dictionaries are used to store data values in <b>key:value</b> pairs. A dictionary is a collection which is <b>ordered</b> (as of Python 3.7), <b>changeable</b>, and <b>does not allow duplicates</b>.</p>\r\n    \r\n    <div class=\"w3-example\">\r\n        <h3>Example: Create a Dictionary</h3>\r\n        <pre><code>thisdict = {\r\n  \"brand\": \"Ford\",\r\n  \"model\": \"Mustang\",\r\n  \"year\": 1964\r\n}\r\nprint(thisdict[\"brand\"])</code></pre>\r\n    </div>\r\n\r\n    <hr class=\"my-8 border-gray-100\">\r\n\r\n    <h2>Dictionary Items</h2>\r\n    <p>Items are presented in key:value pairs, and can be referred to by using the key name. Duplicate keys are not allowed; later values will overwrite earlier ones.</p>\r\n</div>', NULL, NULL, 'Topic: Python Dictionaries Key Concepts:\r\n\r\nMapping: Stores data in Key:Value pairs.\r\n\r\nUniqueness: Keys must be unique; duplicate keys overwrite existing values.\r\n\r\nAccess: Values are accessed via their keys (e.g., dict[\"key\"]). Quiz Focus: Retrieving values using keys, identifying valid dictionary syntax, and understanding how duplicate keys behave.', 16),
(27, 1, 'Python If...Else', '<div class=\"topic-content-area\">\r\n    <h2>Conditions and If Statements</h2>\r\n    <p>Python supports the usual logical conditions from mathematics (<code>==</code>, <code>!=</code>, <code>></code>, etc.). These can be used in \"if statements\".</p>\r\n    \r\n    <div class=\"w3-example\">\r\n        <h3>Example: If...Elif...Else</h3>\r\n        <pre><code>a = 200\r\nb = 33\r\nif b > a:\r\n  print(\"b is greater than a\")\r\nelif a == b:\r\n  print(\"a and b are equal\")\r\nelse:\r\n  print(\"a is greater than b\")</code></pre>\r\n    </div>\r\n\r\n    <hr class=\"my-8 border-gray-100\">\r\n\r\n    <h2>Short Hand If...Else</h2>\r\n    <p>If you have only one statement to execute, one for if, and one for else, you can put it all on one line:</p>\r\n\r\n    <div class=\"w3-example\">\r\n        <pre><code>print(\"A\") if a > b else print(\"B\")</code></pre>\r\n    </div>\r\n\r\n    <p class=\"text-red-600 font-bold mt-4 italic\">Important: Don\'t forget the indentation after the <code>:</code>!</p>\r\n</div>', NULL, NULL, 'Topic: Python Conditional Statements Key Concepts:\r\n\r\nif statement: Executes code if a condition is true.\r\n\r\nelif: Short for \"else if,\" used to check additional conditions if the previous ones were false.\r\n\r\nelse: Executes code if none of the previous conditions were met.\r\n\r\nIndentation: Critical for defining the code block of the condition. Quiz Focus: Predicting the outcome of nested conditions, identifying the correct syntax for elif, and recognizing indentation errors in logic blocks.', 17),
(28, 1, 'Python While Loops', '<div class=\"topic-content-area\">\r\n    <h2>The while Loop</h2>\r\n    <p>With the <code>while</code> loop we can execute a set of statements as long as a condition is true.</p>\r\n    \r\n    <div class=\"w3-example\">\r\n        <h3>Example: Simple While Loop</h3>\r\n        <pre><code>i = 1\r\nwhile i < 6:\r\n  print(i)\r\n  i += 1</code></pre>\r\n    </div>\r\n\r\n    <p><b>Note:</b> remember to increment i, or else the loop will continue forever.</p>\r\n\r\n    <hr class=\"my-8 border-gray-100\">\r\n\r\n    <h2>The break and continue Statements</h2>\r\n    <p>With the <code>break</code> statement we can stop the loop even if the while condition is true. The <code>continue</code> statement allows us to stop the current iteration and continue with the next.</p>\r\n\r\n    <div class=\"w3-example\">\r\n        <h3>Example: Using Break</h3>\r\n        <pre><code>i = 1\r\nwhile i < 6:\r\n  print(i)\r\n  if i == 3:\r\n    break\r\n  i += 1</code></pre>\r\n    </div>\r\n</div>', NULL, NULL, 'Topic: Python While Loops Key Concepts:\r\n\r\nConditional execution: Loops run as long as a specific condition remains True.\r\n\r\nInfinite loops: Occur if the loop condition never becomes False (e.g., forgetting to increment a counter).\r\n\r\nbreak: Immediately terminates the loop.\r\n\r\ncontinue: Skips the rest of the current block and moves to the next iteration. Quiz Focus: Predicting loop output, identifying the result of break vs continue, and recognizing logic that leads to infinite loops.', 18),
(29, 1, 'Python For Loops', '<div class=\"topic-content-area\">\r\n    <h2>Python For Loops</h2>\r\n    <p>A <code>for</code> loop is used for iterating over a sequence (that is either a list, a tuple, a dictionary, a set, or a string).</p>\r\n    \r\n    <div class=\"w3-example\">\r\n        <h3>Example: Iterating Through a List</h3>\r\n        <pre><code>fruits = [\"apple\", \"banana\", \"cherry\"]\r\nfor x in fruits:\r\n  print(x)</code></pre>\r\n    </div>\r\n\r\n    <hr class=\"my-8 border-gray-100\">\r\n\r\n    <h2>The range() Function</h2>\r\n    <p>To loop through a set of code a specified number of times, we can use the <code>range()</code> function. The <code>range()</code> function returns a sequence of numbers, starting from 0 by default, and increments by 1 (by default), and ends at a specified number.</p>\r\n\r\n    <div class=\"w3-example\">\r\n        <pre><code>for x in range(6):\r\n  print(x) # Prints 0 to 5</code></pre>\r\n    </div>\r\n</div>', NULL, NULL, 'Topic: Python For Loops and Range Key Concepts:\r\n\r\nIteration: Automatically traverses items in a sequence without a manual counter.\r\n\r\nrange(start, stop, step): Generates a sequence of numbers. Note that it stops before the specified stop value.\r\n\r\nNested Loops: A loop inside another loop. Quiz Focus: Determining the number of iterations in a range() call, predicting output from nested loops, and iterating through strings as sequences.', 19),
(30, 1, 'Python Arrays', '<div class=\"topic-content-area\">\r\n    <h2>What is an Array?</h2>\r\n    <p>An array is a special variable, which can hold more than one value at a time. Note: Python does not have built-in support for Arrays, but Python <b>Lists</b> can be used instead.</p>\r\n    \r\n    <div class=\"w3-example\">\r\n        <pre><code>cars = [\"Ford\", \"Volvo\", \"BMW\"]</code></pre>\r\n    </div>\r\n\r\n    <hr class=\"my-8 border-gray-100\">\r\n\r\n    <h2>Array Methods</h2>\r\n    <p>You can use methods to manage arrays/lists, such as adding or removing elements.</p>\r\n\r\n    <div class=\"w3-example\">\r\n        <ul>\r\n            <li><code>append()</code> - Adds an element at the end</li>\r\n            <li><code>pop()</code> - Removes the element at the specified position</li>\r\n            <li><code>remove()</code> - Removes the first item with the specified value</li>\r\n        </ul>\r\n    </div>\r\n</div>', NULL, NULL, 'Topic: Python Arrays (Lists as Arrays) Key Concepts:\r\n\r\nTerminology: In standard Python, \"Arrays\" typically refer to Lists unless using a library like NumPy.\r\n\r\nIndexing: Accessing elements via position starting at 0.\r\n\r\nManipulation: Using built-in methods to modify the collection. Quiz Focus: Identifying the first and last elements via index, and selecting the correct method to add or delete items.', 20),
(31, 2, 'Introduction to C#', '<div class=\"topic-content-area\">\r\n    <h2>What is C#?</h2>\r\n    <p>C# (pronounced \"C-Sharp\") is a modern, object-oriented programming language developed by <b>Microsoft</b> that runs on the <b>.NET Framework</b>.</p>\r\n    \r\n    <div class=\"w3-example\">\r\n        <h3>Common Uses:</h3>\r\n        <ul>\r\n            <li>Mobile applications (specifically Android and iOS)</li>\r\n            <li>Desktop applications</li>\r\n            <li>Web applications and services</li>\r\n            <li>Game Development</li>\r\n            <li>Virtual Reality</li>\r\n        </ul>\r\n    </div>\r\n\r\n    <hr class=\"my-8 border-gray-100\">\r\n\r\n    <h2>Why Use C#?</h2>\r\n    <p>C# is one of the most popular programming languages in the world. It has a huge community support and is easy to learn. Because C# is close to C, C++, and Java, it makes it easy for programmers to switch to C# or vice versa.</p>\r\n\r\n    <div class=\"bg-blue-50 p-4 border-l-4 border-blue-500 rounded-r-lg mt-6\">\r\n        <p class=\"text-sm\"><b>C# Syntax:</b> It follows an Object-Oriented structure, making code clear and reusable.</p>\r\n    </div>\r\n</div>', NULL, NULL, 'Topic: Introduction to C# (C-Sharp) Key Concepts:\r\n\r\nOrigin: Developed by Microsoft, runs on .NET Framework.\r\n\r\nVersatility: Used for mobile apps, desktop software, web services, and games (often with Unity).\r\n\r\nFeatures: Object-oriented (OOP), modern, easy-to-use, and highly popular.\r\n\r\nCompatibility: Similar syntax to C, C++, and Java. Quiz Focus: Creator/Origin of C#, common platforms it runs on, and its core programming paradigm (OOP).', 1),
(33, 2, 'C# Syntax', '<div class=\"topic-content-area\">\r\n    <h2>C# Basic Syntax</h2>\r\n    <p>In C#, every line of code must be inside a <b>class</b>. In our example, we named the class <code>Program</code>.</p>\r\n    \r\n    <div class=\"w3-example\">\r\n        <h3>A Simple \"Hello World\"</h3>\r\n        <pre><code>using System;\r\n\r\nnamespace HelloWorld\r\n{\r\n  class Program\r\n  {\r\n    static void Main(string[] args)\r\n    {\r\n      Console.WriteLine(\"Hello World!\");\r\n    }\r\n  }\r\n}</code></pre>\r\n    </div>\r\n\r\n    <hr class=\"my-8 border-gray-100\">\r\n\r\n    <h2>Key Components Explained</h2>\r\n    <ul>\r\n        <li><b>using System:</b> Means we can use classes from the <code>System</code> namespace.</li>\r\n        <li><b>namespace:</b> Used to organize your code and is a container for classes.</li>\r\n        <li><b>class:</b> A container for data and methods. Every line of code that runs in C# must be inside a class.</li>\r\n        <li><b>Main Method:</b> The entry point of any C# program. Any code inside its curly brackets <code>{}</code> will be executed.</li>\r\n        <li><b>Console.WriteLine():</b> A method used to print text to the screen.</li>\r\n    </ul>\r\n\r\n    <p class=\"text-red-600 font-bold mt-4 italic\">Note: Every C# statement ends with a semicolon <b>;</b>.</p>\r\n</div>', NULL, NULL, 'Topic: C# Basic Syntax Key Concepts:\r\n\r\nProgram Structure: Classes are the fundamental containers for C# code.\r\n\r\nEntry Point: The Main method is where code execution begins.\r\n\r\nNamespaces: Used to group and organize code.\r\n\r\nOutput: Console.WriteLine() is used to display text.\r\n\r\nSemicolons: Required to terminate statements. Quiz Focus: Purpose of the Main method, naming the common output command, and identifying the mandatory statement terminator (;).', 2),
(34, 2, 'C# Variables', '<div class=\"topic-content-area\">\r\n    <h2>C# Variables</h2>\r\n    <p>Variables are containers for storing data values. In C#, you must specify the <b>type</b> of the variable.</p>\r\n    \r\n    <div class=\"w3-example\">\r\n        <h3>Common Types:</h3>\r\n        <ul>\r\n            <li><b>int:</b> Stores integers (whole numbers) like 123 or -123.</li>\r\n            <li><b>double:</b> Stores floating point numbers with decimals like 19.99 or -19.99.</li>\r\n            <li><b>char:</b> Stores single characters like \'a\' or \'B\'. Surrounded by single quotes.</li>\r\n            <li><b>string:</b> Stores text like \"Hello World\". Surrounded by double quotes.</li>\r\n            <li><b>bool:</b> Stores values with two states: True or False.</li>\r\n        </ul>\r\n    </div>\r\n\r\n    <div class=\"w3-example\">\r\n        <h3>Declaring (Creating) a Variable</h3>\r\n        <code>type variableName = value;</code>\r\n        <pre><code>string name = \"John\";\r\nint myNum = 15;\r\nConsole.WriteLine(name);</code></pre>\r\n    </div>\r\n\r\n    <hr class=\"my-8 border-gray-100\">\r\n\r\n    <h2>Constants</h2>\r\n    <p>If you don\'t want others (or yourself) to overwrite existing values, add the <code>const</code> keyword, which makes the variable unchangeable and read-only.</p>\r\n    <pre><code>const int myNum = 15;\r\nmyNum = 20; // error</code></pre>\r\n</div>', NULL, NULL, 'Topic: C# Variables and Constants Key Concepts:\r\n\r\nTyped Language: Variables require an explicit type definition (int, string, etc.).\r\n\r\nSyntax: type name = value;.\r\n\r\nConstants: const keyword prevents value modification.\r\n\r\nSpecific Types: Differences between int, double, char, string, and bool. Quiz Focus: Correct declaration syntax, identifying variable types based on data, and the effect of the const keyword.', 3),
(36, 2, 'C# Data Types', '<div class=\"topic-content-area\">\r\n    <h2>The Importance of Data Types</h2>\r\n    <p>A data type specifies the size and type of variable values. It is important to use the correct data type to avoid errors and save memory. In C#, data types are divided into two groups: <b>Value types</b> and <b>Reference types</b>.</p>\r\n    \r\n    <h3>Common Value Types</h3>\r\n    <div class=\"w3-example\">\r\n        <p>Here are the most common types you will use daily:</p>\r\n        <ul>\r\n            <li><b>int:</b> 4 bytes. Stores whole numbers from -2,147,483,648 to 2,147,483,647.</li>\r\n            <li><b>long:</b> 8 bytes. Stores whole numbers. You should end the value with an \"L\".</li>\r\n            <li><b>float:</b> 4 bytes. Stores fractional numbers. Sufficient for storing 6 to 7 decimal digits. You must end the value with an \"f\".</li>\r\n            <li><b>double:</b> 8 bytes. Stores fractional numbers. Sufficient for storing 15 decimal digits.</li>\r\n            <li><b>bool:</b> 1 bit. Stores <code>true</code> or <code>false</code> values.</li>\r\n            <li><b>char:</b> 2 bytes. Stores a single character/letter, surrounded by single quotes.</li>\r\n        </ul>\r\n    </div>\r\n\r\n    \r\n\r\n    <hr class=\"my-8 border-gray-100\">\r\n\r\n    <h2>Scientific Numbers</h2>\r\n    <p>A floating point number can also be a scientific number with an \"e\" to indicate the power of 10:</p>\r\n    <div class=\"w3-example\">\r\n        <pre><code>float f1 = 35e3f;\r\ndouble d1 = 12E4D;\r\nConsole.WriteLine(f1);\r\nConsole.WriteLine(d1);</code></pre>\r\n    </div>\r\n</div>', NULL, NULL, 'Topic: C# Data Types Key Concepts: Memory allocation for different types (int vs long). Difference between floating point types (float vs double). Suffix requirements: \'L\' for long and \'f\' for float. Precision levels for decimals. Quiz Focus: Choosing the correct type based on a value\'s size, identifying the correct suffix for float/long, and understanding which types allow decimals.', 4),
(37, 2, 'C# Type Casting', '<div class=\"topic-content-area\">\r\n    <h2>What is Type Casting?</h2>\r\n    <p>Type casting is when you assign a value of one data type to another type. In C#, there are two types of casting:</p>\r\n    \r\n    <h3>1. Implicit Casting (Automatically)</h3>\r\n    <p>Converting a smaller type to a larger type size. This is handled by C# automatically because there is no risk of losing data.</p>\r\n    <div class=\"w3-example\">\r\n        <p><b>Sequence:</b> <code>char -> int -> long -> float -> double</code></p>\r\n        <pre><code>int myInt = 9;\r\ndouble myDouble = myInt; // Automatic casting</code></pre>\r\n    </div>\r\n\r\n    <hr class=\"my-8 border-gray-100\">\r\n\r\n    <h3>2. Explicit Casting (Manually)</h3>\r\n    <p>Converting a larger type to a smaller size type. You must do this manually by placing the type in parentheses in front of the value.</p>\r\n    <div class=\"w3-example\">\r\n        <p><b>Sequence:</b> <code>double -> float -> long -> int -> char</code></p>\r\n        <pre><code>double myDouble = 9.78;\r\nint myInt = (int)myDouble; // Manual casting: double to int</code></pre>\r\n    </div>\r\n\r\n    <p class=\"bg-yellow-50 p-4 border-l-4 border-yellow-500 rounded-r-lg mt-6 text-sm\">\r\n        <b>Note:</b> When casting <code>9.78</code> to an <code>int</code>, the result will be <code>9</code>. The decimal values are lost.\r\n    </p>\r\n</div>', NULL, NULL, 'Topic: C# Type Casting Key Concepts: Implicit vs Explicit casting. Memory size hierarchy. The syntax for manual (explicit) casting (targetType)value. Loss of precision when casting from double to int. Quiz Focus: Distinguishing between automatic and manual casting, predicting the result of a double-to-int cast, and identifying correct casting syntax.', 5),
(38, 2, 'C# User Input', '<div class=\"topic-content-area\">\r\n    <h2>Getting User Input</h2>\r\n    <p>You have already used <code>Console.WriteLine()</code> to output (print) values. Now we will use <code>Console.ReadLine()</code> to get user input.</p>\r\n    \r\n    <div class=\"w3-example\">\r\n        <pre><code>// Ask the user for their name\r\nConsole.WriteLine(\"Enter username:\");\r\n\r\n// Create a string variable and get user input from the keyboard\r\nstring userName = Console.ReadLine();\r\n\r\n// Print the value of the variable\r\nConsole.WriteLine(\"Username is: \" + userName);</code></pre>\r\n    </div>\r\n\r\n    <hr class=\"my-8 border-gray-100\">\r\n\r\n    <h2>User Input and Numbers</h2>\r\n    <p>The <code>Console.ReadLine()</code> method returns a <b>string</b>. Therefore, you cannot get information from another data type, such as <b>int</b>, directly. You must convert it manually.</p>\r\n\r\n    <div class=\"w3-example border-l-red-500 bg-red-50\">\r\n        <h3>Conversion Example</h3>\r\n        <pre><code>Console.WriteLine(\"Enter your age:\");\r\nint age = Convert.ToInt32(Console.ReadLine());\r\nConsole.WriteLine(\"Your age is: \" + age);</code></pre>\r\n    </div>\r\n</div>', NULL, NULL, 'Topic: C# User Input Key Concepts: Console.ReadLine() for keyboard input. The default return type is always a string. Using the Convert class (e.g., Convert.ToInt32) to process numeric inputs. Quiz Focus: Identifying why numeric input requires conversion, the method used to read lines, and the result of trying to store ReadLine directly into an int.', 6),
(39, 2, 'C# Operators', '<div class=\"topic-content-area\">\r\n    <h2>Operators in C#</h2>\r\n    <p>Operators are used to perform operations on variables and values. We can group them into Arithmetic, Assignment, Comparison, and Logical operators.</p>\r\n    \r\n    <h3>Arithmetic Operators</h3>\r\n    <div class=\"w3-example\">\r\n        <ul>\r\n            <li><code>+</code> Addition</li>\r\n            <li><code>-</code> Subtraction</li>\r\n            <li><code>*</code> Multiplication</li>\r\n            <li><code>/</code> Division</li>\r\n            <li><code>%</code> <b>Modulus:</b> Returns the division remainder.</li>\r\n            <li><code>++</code> <b>Increment:</b> Increases value by 1.</li>\r\n            <li><code>--</code> <b>Decrement:</b> Decreases value by 1.</li>\r\n        </ul>\r\n    </div>\r\n\r\n    <hr class=\"my-8 border-gray-100\">\r\n\r\n    <h2>Comparison and Logical</h2>\r\n    <p>Comparison operators return a <b>boolean</b> value (True or False).</p>\r\n    <div class=\"w3-example\">\r\n        <p><b>Examples:</b> <code>==</code> (Equal to), <code>!=</code> (Not equal), <code>></code> (Greater than).</p>\r\n        <p><b>Logical Operators:</b> <code>&&</code> (Logical and), <code>||</code> (Logical or), <code>!</code> (Logical not).</p>\r\n    </div>\r\n</div>', NULL, NULL, 'Topic: C# Operators Key Concepts: Basic arithmetic. The Modulus (%) operator for remainders. Increment (++) and Decrement (--) operators. Comparison vs Logical operators. Quiz Focus: Solving basic modulus expressions, identifying logical AND/OR results, and distinguishing between assignment (=) and equality (==).', 7);
INSERT INTO `topics` (`topic_id`, `language_id`, `title`, `content_description`, `pdf_file_path`, `external_link`, `ai_context_summary`, `order_sequence`) VALUES
(40, 2, 'C# Math', '<div class=\"topic-content-area\">\r\n    <h2>The C# Math Class</h2>\r\n    <p>The C# Math class has many methods that allow you to perform mathematical tasks on numbers. These are pre-built functions provided by the language.</p>\r\n    \r\n    <div class=\"w3-example\">\r\n        <h3>Common Math Methods:</h3>\r\n        <ul>\r\n            <li><code>Math.Max(x, y)</code>: Can be used to find the highest value of x and y.</li>\r\n            <li><code>Math.Min(x, y)</code>: Can be used to find the lowest value of x and y.</li>\r\n            <li><code>Math.Sqrt(x)</code>: Returns the square root of x.</li>\r\n            <li><code>Math.Abs(x)</code>: Returns the absolute (positive) value of x.</li>\r\n            <li><code>Math.Round()</code>: Rounds a number to the nearest whole number.</li>\r\n        </ul>\r\n    </div>\r\n\r\n    <div class=\"w3-example bg-gray-50\">\r\n        <h3>Example Snippet</h3>\r\n        <pre><code>Console.WriteLine(Math.Max(5, 10));  // Outputs 10\r\nConsole.WriteLine(Math.Sqrt(64));   // Outputs 8\r\nConsole.WriteLine(Math.Abs(-4.7));  // Outputs 4.7</code></pre>\r\n    </div>\r\n</div>', NULL, NULL, 'Topic: C# Math Class Key Concepts: Static methods in the Math class. Finding maximum/minimum values. Square root and absolute value functions. Rounding logic. Quiz Focus: Predicting the output of Math.Max, identifying the method for square roots, and understanding absolute value results.', 8),
(41, 2, 'C# Strings', '<div class=\"topic-content-area\">\r\n    <h2>Working with Text</h2>\r\n    <p>Strings are used for storing text. In C#, a string is an <b>object</b> that contains many built-in properties and methods to manipulate text.</p>\r\n    \r\n    <h3>1. String Length</h3>\r\n    <p>A common property is <code>Length</code>, which returns the number of characters in the string:</p>\r\n    <div class=\"w3-example\">\r\n        <pre><code>string txt = \"ABCDEFGHIJKLMNOPQRSTUVWXYZ\";\r\nConsole.WriteLine(\"The length is: \" + txt.Length);</code></pre>\r\n    </div>\r\n\r\n    <hr class=\"my-8 border-gray-100\">\r\n\r\n    <h2>String Interpolation</h2>\r\n    <p>Interpolation is the preferred way to join strings. It uses the <code>$</code> sign and curly brackets <code>{}</code> to insert variables directly into a string.</p>\r\n\r\n    <div class=\"w3-example bg-green-50\">\r\n        <pre><code>string firstName = \"John\";\r\nstring lastName = \"Doe\";\r\nstring name = $\"My full name is: {firstName} {lastName}\";\r\nConsole.WriteLine(name);</code></pre>\r\n    </div>\r\n\r\n    <h3>Accessing Strings</h3>\r\n    <p>You can access the characters in a string by referring to its <b>index</b> number inside square brackets <code>[]</code>. Indexing starts at <b>0</b>.</p>\r\n    <div class=\"w3-example\">\r\n        <pre><code>string myString = \"Hello\";\r\nConsole.WriteLine(myString[0]);  // Outputs \"H\"</code></pre>\r\n    </div>\r\n</div>', NULL, NULL, 'Topic: C# Strings, Interpolation, and Indexing. Key Concepts: String as an object. The .Length property. Methods like .ToUpper() and .ToLower(). String interpolation syntax $\"{var}\". Zero-based indexing for character access. Quiz Focus: Finding string length, identifying characters at specific indices, and correct syntax for string interpolation.', 9),
(42, 2, 'C# Booleans', '<div class=\"topic-content-area\">\r\n    <h2>Boolean Values</h2>\r\n    <p>Very often, in programming, you will need a data type that can only have one of two values: <b>true</b> or <b>false</b>.</p>\r\n    \r\n    <div class=\"w3-example\">\r\n        <pre><code>bool isCSharpFun = true;\r\nbool isFishTasty = false;\r\nConsole.WriteLine(isCSharpFun);   // Outputs True</code></pre>\r\n    </div>\r\n\r\n    <hr class=\"my-8 border-gray-100\">\r\n\r\n    <h2>Boolean Expressions</h2>\r\n    <p>A Boolean expression returns a boolean value: <code>True</code> or <code>False</code> by comparing values/variables. This is the foundation of logic in C#.</p>\r\n\r\n    <div class=\"w3-example\">\r\n        <pre><code>int x = 10;\r\nint y = 9;\r\nConsole.WriteLine(x > y); // Returns True, because 10 is higher than 9</code></pre>\r\n    </div>\r\n\r\n    <p>You can also use the equal to (<code>==</code>) operator to evaluate an expression:</p>\r\n    <pre><code>int x = 10;\r\nConsole.WriteLine(x == 10); // Returns True</code></pre>\r\n</div>', NULL, NULL, 'Topic: C# Booleans and Logic. Key Concepts: The bool data type. True and False values. Using comparison operators (>, <, ==) to generate boolean results. Quiz Focus: Predicting the boolean outcome of comparison expressions and identifying the correct keyword for boolean declarations.', 10),
(43, 2, 'C# If...Else', '<div class=\"topic-content-area\">\r\n    <h2>Conditional Statements</h2>\r\n    <p>C# supports the usual logical conditions from mathematics. Use <code>if</code> to specify a block of code to be executed if a condition is <b>true</b>.</p>\r\n    \r\n    <div class=\"w3-example\">\r\n        <pre><code>int time = 20;\r\nif (time < 18) \r\n{\r\n  Console.WriteLine(\"Good day.\");\r\n} \r\nelse \r\n{\r\n  Console.WriteLine(\"Good evening.\");\r\n}\r\n// Outputs \"Good evening.\"</code></pre>\r\n    </div>\r\n\r\n    <hr class=\"my-8 border-gray-100\">\r\n\r\n    <h2>The Else If Statement</h2>\r\n    <p>Use <code>else if</code> to specify a new condition if the first condition is false.</p>\r\n\r\n    <div class=\"w3-example\">\r\n        <pre><code>int time = 22;\r\nif (time < 10) {\r\n  Console.WriteLine(\"Good morning.\");\r\n} else if (time < 20) {\r\n  Console.WriteLine(\"Good day.\");\r\n} else {\r\n  Console.WriteLine(\"Good evening.\");\r\n}</code></pre>\r\n    </div>\r\n</div>', NULL, NULL, 'Topic: C# Conditional Logic (If, Else If, Else). Key Concepts: Logical branching. Syntax for if, else, and else if. The importance of curly brackets {} for code blocks. Quiz Focus: Predicting which block of code will execute based on variable values and identifying syntax errors in conditional structures.', 11),
(44, 2, 'C# Switch', '<div class=\"topic-content-area\">\r\n    <h2>The Switch Statement</h2>\r\n    <p>Use the <code>switch</code> statement to select one of many code blocks to be executed. It is a more efficient alternative to long <code>if...else if</code> chains.</p>\r\n    \r\n    <div class=\"w3-example\">\r\n        <pre><code>int day = 4;\r\nswitch (day) \r\n{\r\n  case 1:\r\n    Console.WriteLine(\"Monday\");\r\n    break;\r\n  case 2:\r\n    Console.WriteLine(\"Tuesday\");\r\n    break;\r\n  default:\r\n    Console.WriteLine(\"Looking forward to the Weekend\");\r\n    break;\r\n}</code></pre>\r\n    </div>\r\n\r\n    <hr class=\"my-8 border-gray-100\">\r\n\r\n    <h2>The Break and Default Keywords</h2>\r\n    <ul>\r\n        <li><b>break:</b> Jumps out of the switch block. This stops the execution of more code inside the block.</li>\r\n        <li><b>default:</b> Specifies some code to run if there is no case match.</li>\r\n    </ul>\r\n</div>', NULL, NULL, 'Topic: C# Switch Statement. Key Concepts: Multi-way branching. The case keyword. The necessity of the break statement to prevent \"fall-through.\" The default case for unmatched values. Quiz Focus: Identifying the purpose of break, when the default block executes, and how a switch evaluates variables.', 12),
(45, 2, 'C# While Loop', '<div class=\"topic-content-area\">\r\n    <h2>The while Loop</h2>\r\n    <p>Loops can execute a block of code as long as a specified condition is reached. Loops save time, reduce errors, and make code more readable.</p>\r\n    \r\n    <div class=\"w3-example\">\r\n        <h3>Example: While Loop</h3>\r\n        <pre><code>int i = 0;\r\nwhile (i < 5) \r\n{\r\n  Console.WriteLine(i);\r\n  i++;\r\n}</code></pre>\r\n    </div>\r\n\r\n    <p class=\"text-red-600\"><b>Note:</b> Do not forget to increase the variable used in the condition (<code>i++</code>), otherwise the loop will never end (infinite loop)!</p>\r\n\r\n    <hr class=\"my-8 border-gray-100\">\r\n\r\n    <h2>The do/while Loop</h2>\r\n    <p>The <code>do/while</code> loop is a variant of the while loop. This loop will execute the code block <b>once</b>, before checking if the condition is true, and then it will repeat the loop as long as the condition is true.</p>\r\n</div>', NULL, NULL, 'Topic: C# While and Do-While Loops. Key Concepts: Iteration based on a boolean condition. Incrementing/decrementing loop counters. Difference between while (check then run) and do/while (run then check). Quiz Focus: Determining the number of times a loop will run, identifying infinite loops, and the guaranteed single execution of a do/while loop.', 13),
(46, 2, 'C# For Loop', '<div class=\"topic-content-area\">\r\n    <h2>The for Loop</h2>\r\n    <p>When you know exactly how many times you want to loop through a block of code, use the <code>for</code> loop instead of a <code>while</code> loop.</p>\r\n    \r\n    <div class=\"w3-example\">\r\n        <h3>Syntax</h3>\r\n        <pre><code>for (statement 1; statement 2; statement 3) \r\n{\r\n  // code block to be executed\r\n}</code></pre>\r\n    </div>\r\n\r\n    <p><b>Statement 1</b> is executed (one time) before the execution of the code block.</p>\r\n    <p><b>Statement 2</b> defines the condition for executing the code block.</p>\r\n    <p><b>Statement 3</b> is executed (every time) after the code block has been executed.</p>\r\n\r\n    <div class=\"w3-example\">\r\n        <h3>Example</h3>\r\n        <pre><code>for (int i = 0; i < 5; i++) \r\n{\r\n  Console.WriteLine(i);\r\n}</code></pre>\r\n    </div>\r\n\r\n    \r\n\r\n    <hr class=\"my-8 border-gray-100\">\r\n\r\n    <h2>The foreach Loop</h2>\r\n    <p>There is also a <b>foreach</b> loop, which is used exclusively to loop through elements in an <b>array</b> (or other collections):</p>\r\n    <div class=\"w3-example\">\r\n        <pre><code>string[] cars = {\"Volvo\", \"BMW\", \"Ford\", \"Mazda\"};\r\nforeach (string i in cars) \r\n{\r\n  Console.WriteLine(i);\r\n}</code></pre>\r\n    </div>\r\n</div>', NULL, NULL, 'Topic: C# For and Foreach Loops. Key Concepts: The three statements of a for loop (initialization, condition, increment). Loop control. The foreach loop for simplified collection traversal. Quiz Focus: Identifying the parts of a for loop, predicting number of iterations, and choosing between for and foreach.', 14),
(47, 2, 'C# Break and Continue', '<div class=\"topic-content-area\">\r\n    <h2>Loop Control: Break and Continue</h2>\r\n    <p>Sometimes you need to exit a loop early or skip a specific part of a loop. This is where <code>break</code> and <code>continue</code> come in.</p>\r\n    \r\n    <h3>The break Statement</h3>\r\n    <p>You have already seen the <code>break</code> statement used in an earlier chapter of this tutorial. It was used to \"jump out\" of a <code>switch</code> statement. It can also be used to jump out of a <b>loop</b>.</p>\r\n    <div class=\"w3-example\">\r\n        <pre><code>for (int i = 0; i < 10; i++) \r\n{\r\n  if (i == 4) { break; }\r\n  Console.WriteLine(i);\r\n} // Loop stops entirely when i reaches 4</code></pre>\r\n    </div>\r\n\r\n    <hr class=\"my-8 border-gray-100\">\r\n\r\n    <h3>The continue Statement</h3>\r\n    <p>The <code>continue</code> statement breaks one iteration (in the loop), if a specified condition occurs, and continues with the next iteration in the loop.</p>\r\n    <div class=\"w3-example\">\r\n        <pre><code>for (int i = 0; i < 10; i++) \r\n{\r\n  if (i == 4) { continue; }\r\n  Console.WriteLine(i);\r\n} // Skips the number 4, but continues with 5</code></pre>\r\n    </div>\r\n</div>', NULL, NULL, 'Topic: C# Loop Control (Break and Continue). Key Concepts: break exits the loop entirely. continue skips only the current iteration. Quiz Focus: Predicting loop output when break or continue is present and identifying the difference in behavior between the two.', 15),
(48, 2, 'C# Arrays', '<div class=\"topic-content-area\">\r\n    <h2>Creating Arrays</h2>\r\n    <p>Arrays are used to store multiple values in a single variable, instead of declaring separate variables for each value. To declare an array, define the variable type with <b>square brackets</b>.</p>\r\n    \r\n    <div class=\"w3-example\">\r\n        <pre><code>string[] cars = {\"Volvo\", \"BMW\", \"Ford\", \"Mazda\"};\r\nint[] myNum = {10, 20, 30, 40};</code></pre>\r\n    </div>\r\n\r\n    \r\n\r\n    <hr class=\"my-8 border-gray-100\">\r\n\r\n    <h2>Accessing and Changing Elements</h2>\r\n    <p>You access an array element by referring to the index number. Like strings, array indices start at <b>0</b>.</p>\r\n    <div class=\"w3-example\">\r\n        <pre><code>string[] cars = {\"Volvo\", \"BMW\", \"Ford\"};\r\nConsole.WriteLine(cars[0]); // Outputs Volvo\r\n\r\ncars[0] = \"Opel\"; // Changes Volvo to Opel</code></pre>\r\n    </div>\r\n\r\n    <h3>Array Length</h3>\r\n    <p>To find out how many elements an array has, use the <code>Length</code> property:</p>\r\n    <pre><code>Console.WriteLine(cars.Length);</code></pre>\r\n</div>', NULL, NULL, 'Topic: C# Arrays. Key Concepts: Storing multiple items of the same type. Zero-based indexing. Modifying array elements. The .Length property. Quiz Focus: Identifying the index of an element, determining array size, and correctly declaring array syntax.', 16),
(49, 2, 'C# Methods', '<div class=\"topic-content-area\">\r\n    <h2>What is a Method?</h2>\r\n    <p>A <b>method</b> is a block of code which only runs when it is called. You can pass data, known as parameters, into a method. Methods are used to perform certain actions, and they are also known as <b>functions</b>.</p>\r\n    \r\n    <div class=\"w3-example\">\r\n        <h3>Creating a Method</h3>\r\n        <p>A method is defined with the name of the method, followed by parentheses <b>()</b>.</p>\r\n        <pre><code>class Program \r\n{\r\n  static void MyMethod() \r\n  {\r\n    Console.WriteLine(\"I just got executed!\");\r\n  }\r\n\r\n  static void Main(string[] args) \r\n  {\r\n    MyMethod(); // Call the method\r\n  }\r\n}</code></pre>\r\n    </div>\r\n\r\n    <p><b>static</b> means that the method belongs to the Program class and not an object of the Program class.</p>\r\n    <p><b>void</b> means that this method does not have a return value.</p>\r\n</div>', NULL, NULL, 'Topic: C# Method Basics. Key Concepts: Defining methods with static void. Calling methods from Main. Code reusability. Quiz Focus: Recognizing method definition vs. method calling, the meaning of void, and the purpose of methods in code organization.', 17),
(50, 2, 'C# Method Parameters', '<div class=\"topic-content-area\">\r\n    <h2>Parameters and Arguments</h2>\r\n    <p>Information can be passed to methods as parameter. Parameters act as variables inside the method.</p>\r\n    \r\n    <div class=\"w3-example\">\r\n        <pre><code>static void MyMethod(string fname) \r\n{\r\n  Console.WriteLine(fname + \" Refsnes\");\r\n}\r\n\r\nstatic void Main(string[] args) \r\n{\r\n  MyMethod(\"Liam\");\r\n  MyMethod(\"Jenny\");\r\n}</code></pre>\r\n    </div>\r\n\r\n    <hr class=\"my-8 border-gray-100\">\r\n\r\n    <h2>Return Values</h2>\r\n    <p>The <code>void</code> keyword, used in the examples above, indicates that the method should not return a value. If you want the method to return a value, you can use a <b>primitive type</b> (such as <code>int</code> or <code>double</code>) instead of void, and use the <b>return</b> keyword inside the method.</p>\r\n\r\n    <div class=\"w3-example\">\r\n        <pre><code>static int MyMethod(int x) \r\n{\r\n  return 5 + x;\r\n}\r\n\r\nstatic void Main(string[] args) \r\n{\r\n  Console.WriteLine(MyMethod(3)); // Outputs 8\r\n}</code></pre>\r\n    </div>\r\n</div>', NULL, NULL, 'Topic: C# Parameters and Return Values. Key Concepts: Passing data into methods. Returning data from methods. Difference between void and typed methods. Quiz Focus: Identifying parameters in a method signature, predicting the result of a return statement, and distinguishing between arguments and parameters.', 18),
(51, 2, 'C# Classes and Objects', '<div class=\"topic-content-area\">\r\n    <h2>OOP: Classes and Objects</h2>\r\n    <p>C# is an <b>Object-Oriented Programming</b> language. Everything in C# is associated with classes and objects, along with its attributes and methods.</p>\r\n    \r\n    <p>A <b>Class</b> is like an object constructor, or a \"blueprint\" for creating objects.</p>\r\n\r\n    \r\n\r\n    <div class=\"w3-example\">\r\n        <h3>Create a Class and Object</h3>\r\n        <pre><code>class Car \r\n{\r\n  string color = \"red\";\r\n\r\n  static void Main(string[] args) \r\n  {\r\n    Car myObj = new Car();\r\n    Console.WriteLine(myObj.color);\r\n  }\r\n}</code></pre>\r\n    </div>\r\n\r\n    <p>To create an object of <code>Car</code>, use the <b>new</b> keyword.</p>\r\n</div>', NULL, NULL, 'Topic: C# Classes and Objects. Key Concepts: Classes as blueprints. Objects as instances of classes. The new keyword for object instantiation. Attributes/Fields. Quiz Focus: Defining the relationship between a class and an object and identifying the syntax for creating an object.', 19),
(52, 2, 'C# Class Members', '<div class=\"topic-content-area\">\r\n    <h2>Fields and Methods</h2>\r\n    <p>Inside a class, you can have <b>Fields</b> (variables) and <b>Methods</b> (functions). These are collectively called \"Class Members\".</p>\r\n    \r\n    <div class=\"w3-example\">\r\n        <pre><code>class Car \r\n{\r\n  // Field\r\n  string model;\r\n  string color;\r\n  int year;\r\n\r\n  // Method\r\n  public void fullThrottle() \r\n  {\r\n    Console.WriteLine(\"The car is going as fast as it can!\"); \r\n  }\r\n\r\n  static void Main(string[] args)\r\n  {\r\n    Car Ford = new Car();\r\n    Ford.model = \"Mustang\";\r\n    Ford.color = \"red\";\r\n    Ford.year = 1964;\r\n\r\n    Ford.fullThrottle();\r\n  }\r\n}</code></pre>\r\n    </div>\r\n\r\n    <p>By using the <b>dot syntax (.)</b>, you can access fields and methods belonging to the object.</p>\r\n</div>', NULL, NULL, 'Topic: C# Class Members (Fields and Methods). Key Concepts: Accessing object data. Using dot notation. Difference between static and non-static members. Quiz Focus: Identifying fields vs methods, predicting object attribute values, and correct dot syntax usage.', 20),
(53, 3, 'Introduction to C++', '<div class=\"topic-content-area\">\r\n    <h2>What is C++?</h2>\r\n    <p>C++ is a cross-platform language that can be used to create high-performance applications. It was developed by <b>Bjarne Stroustrup</b> at Bell Labs in 1979 as an extension to the C language.</p>\r\n    \r\n    <h3>Why Use C++?</h3>\r\n    <p>C++ is one of the world\'s most popular programming languages. You can find it in operating systems (Windows, macOS), graphical user interfaces, and embedded systems. Because it is close to the hardware, it allows for incredible optimization.</p>\r\n\r\n    <div class=\"w3-example bg-blue-50\">\r\n        <h3>Key Differences:</h3>\r\n        <ul>\r\n            <li><b>Compiled Language:</b> C++ code is translated directly into machine code that the processor executes, making it extremely fast.</li>\r\n            <li><b>Object-Oriented:</b> Like C#, C++ supports classes and objects, providing a clear structure to programs.</li>\r\n            <li><b>Manual Memory Management:</b> C++ gives you the power to manage computer memory directly, which is powerful but requires care.</li>\r\n        </ul>\r\n    </div>\r\n\r\n    <hr class=\"my-8 border-gray-100\">\r\n\r\n    <h2>Your First C++ Program</h2>\r\n    <div class=\"w3-example\">\r\n        <pre><code>#include &lt;iostream&gt;\r\nusing namespace std;\r\n\r\nint main() {\r\n  cout << \"Hello World!\";\r\n  return 0;\r\n}</code></pre>\r\n    </div>\r\n\r\n    <h3>Breakdown:</h3>\r\n    <ul>\r\n        <li><code>#include &lt;iostream&gt;</code>: A header file library that lets us work with input and output objects (like <code>cout</code>).</li>\r\n        <li><code>using namespace std;</code>: Means we can use names for objects and variables from the standard library.</li>\r\n        <li><code>int main()</code>: The function where the program execution begins.</li>\r\n        <li><code>cout</code>: (pronounced \"see-out\") is an object used together with the insertion operator (<code><<</code>) to output text.</li>\r\n        <li><code>return 0;</code>: Ends the main function.</li>\r\n    </ul>\r\n    <p class=\"text-red-600 font-bold italic\">Rule: Every C++ statement must end with a semicolon (;).</p>\r\n</div>', NULL, NULL, 'Topic: Introduction to C++. Key Concepts: Developed by Bjarne Stroustrup. Compiled nature of the language. Structure of a basic program: #include, main(), and cout. Importance of the semicolon and the iostream library. Quiz Focus: Creator of C++, identifying the entry point (main), the purpose of cout, and the result of missing a semicolon.', 1),
(54, 3, 'C++ Syntax & Output', '<div class=\"topic-content-area\">\r\n    <h2>Basic Syntax & The \"cout\" Object</h2>\r\n    <p>In C++, text is displayed using the <code>cout</code> object. To move to a new line, you have two main options: <code>\\n</code> or <code>endl</code>.</p>\r\n    \r\n    <div class=\"w3-example\">\r\n        <h3>Outputting Multiple Lines</h3>\r\n        <pre><code>#include &lt;iostream&gt;\r\nusing namespace std;\r\n\r\nint main() {\r\n  cout << \"Hello World!\" << endl;\r\n  cout << \"I am learning C++ \\n\";\r\n  cout << \"This is fun!\";\r\n  return 0;\r\n}</code></pre>\r\n    </div>\r\n\r\n    <hr class=\"my-8 border-gray-100\">\r\n\r\n    <h2>Important Formatting Notes</h2>\r\n    <ul>\r\n        <li><b>New Lines:</b> <code>endl</code> flushes the output buffer, while <code>\\n</code> is generally faster because it does not.</li>\r\n        <li><b>The &lt;&lt; Operator:</b> This is called the <b>Insertion Operator</b>. You can chain it to output multiple pieces of data in one line.</li>\r\n    </ul>\r\n\r\n    <div class=\"w3-example bg-gray-100\">\r\n        <h3>Case Sensitivity</h3>\r\n        <p>Like C#, C++ is <b>case-sensitive</b>. <code>Cout</code> or <code>COUT</code> will result in a compilation error. The language is strict about lowercase and uppercase letters.</p>\r\n    </div>\r\n</div>', NULL, NULL, 'Topic: C++ Syntax and Output Formatting. Key Concepts: Using cout with <<. Line breaks using \\n and endl. Semicolon rules. Case sensitivity in keywords. Quiz Focus: Correct syntax for multi-line output, the name of the << operator, and the difference between \\n and endl.', 2),
(55, 3, 'C++ Variables & Types', '<div class=\"topic-content-area\">\r\n    <h2>Declaring Variables</h2>\r\n    <p>In C++, you must declare the variable type before assigning a value. C++ is a <b>statically typed</b> language.</p>\r\n    \r\n    <div class=\"w3-example\">\r\n        <h3>Syntax:</h3>\r\n        <code>type variableName = value;</code>\r\n    </div>\r\n\r\n    \r\n\r\n    <hr class=\"my-8 border-gray-100\">\r\n\r\n    <h2>Fundamental Data Types</h2>\r\n    <ul>\r\n        <li><b>int:</b> Whole numbers (e.g., <code>5</code>, <code>-100</code>).</li>\r\n        <li><b>double:</b> Floating point numbers with decimals (e.g., <code>19.99</code>).</li>\r\n        <li><b>char:</b> Single characters surrounded by <b>single quotes</b> (e.g., <code>\'A\'</code>).</li>\r\n        <li><b>string:</b> Text surrounded by <b>double quotes</b> (e.g., <code>\"Hello\"</code>). <i>Note: requires #include &lt;string&gt;</i>.</li>\r\n        <li><b>bool:</b> Stores <code>true</code> or <code>false</code>.</li>\r\n    </ul>\r\n\r\n    <div class=\"w3-example\">\r\n        <h3>Example:</h3>\r\n        <pre><code>int myNum = 15;\r\ndouble myFloatNum = 5.99;\r\nchar myLetter = \'D\';\r\nstring myText = \"Hello\";\r\nbool myBoolean = true;</code></pre>\r\n    </div>\r\n</div>', NULL, NULL, 'Topic: C++ Variables and Data Types. Key Concepts: Static typing. int, double, char (single quotes), string (double quotes), and bool. Variable declaration syntax. Quiz Focus: Distinguishing between double and int, correct quote usage for characters vs strings, and naming the variable type used for truth values.', 3),
(56, 3, 'C++ User Input', '<div class=\"topic-content-area\">\r\n    <h2>The \"cin\" Object</h2>\r\n    <p>To get user input from the keyboard, we use <code>cin</code> (pronounced \"see-in\"). While <code>cout</code> uses the insertion operator (<code><<</code>), <code>cin</code> uses the <b>extraction operator</b> (<code>>></code>).</p>\r\n    \r\n    <div class=\"w3-example\">\r\n        <h3>Example: Simple Input</h3>\r\n        <pre><code>int x;\r\ncout << \"Type a number: \";\r\ncin >> x; // Get user input from the keyboard\r\ncout << \"Your number is: \" << x;</code></pre>\r\n    </div>\r\n\r\n    <hr class=\"my-8 border-gray-100\">\r\n\r\n    <h2>Handling Multiple Inputs</h2>\r\n    <p>You can use <code>cin</code> to capture multiple values in a single line by chaining the extraction operator.</p>\r\n\r\n    <div class=\"w3-example\">\r\n        <pre><code>int x, y;\r\nint sum;\r\ncout << \"Type two numbers: \";\r\ncin >> x >> y;\r\nsum = x + y;\r\ncout << \"Sum is: \" << sum;</code></pre>\r\n    </div>\r\n\r\n    <p class=\"text-red-600 font-bold italic\">Rule: Always ensure the variable is declared BEFORE you try to store user input in it.</p>\r\n</div>', NULL, NULL, 'Topic: C++ User Input. Key Concepts: cin object. Extraction operator (>>). Reading single and multiple values. Requirement of prior variable declaration. Quiz Focus: Differentiating between << and >>, predicting behavior of multi-value cin, and identifying errors in input code.', 4),
(57, 3, 'C++ Operators', '<div class=\"topic-content-area\">\r\n    <h2>C++ Operators</h2>\r\n    <p>Operators are symbols used to perform operations on variables and values. C++ groups them into Arithmetic, Assignment, Comparison, and Logical operators.</p>\r\n\r\n    <h3>Arithmetic Operators</h3>\r\n    <div class=\"w3-example\">\r\n        <ul>\r\n            <li><code>+</code>, <code>-</code>, <code>*</code>, <code>/</code> : Basic Math.</li>\r\n            <li><code>%</code> <b>Modulus</b>: Returns the division remainder (e.g., <code>9 % 2</code> is 1).</li>\r\n            <li><code>++</code> <b>Increment</b>: Increases value by 1.</li>\r\n            <li><code>--</code> <b>Decrement</b>: Decreases value by 1.</li>\r\n        </ul>\r\n    </div>\r\n\r\n    <hr class=\"my-8 border-gray-100\">\r\n\r\n    <h3>Logical Operators</h3>\r\n    <p>These are used to determine the logic between variables or values:</p>\r\n    <ul>\r\n        <li><code>&&</code> <b>Logical AND</b>: True if both statements are true.</li>\r\n        <li><code>||</code> <b>Logical OR</b>: True if one of the statements is true.</li>\r\n        <li><code>!</code> <b>Logical NOT</b>: Reverses the result (True becomes False).</li>\r\n    </ul>\r\n</div>', NULL, NULL, 'Topic: C++ Operators. Key Concepts: Modulus operator (%). Increment/Decrement. Comparison operators (==, !=, <, >). Logical AND (&&), OR (||), and NOT (!). Quiz Focus: Calculating modulus, understanding logical operator behavior, and predicting variable changes after increment operations.', 5),
(58, 3, 'C++ Math', '<div class=\"topic-content-area\">\r\n    <h2>C++ Math Functions</h2>\r\n    <p>C++ has many functions that allow you to perform mathematical tasks on numbers. To use the advanced mathematical functions, you must include the <code>&lt;cmath&gt;</code> header file.</p>\r\n    \r\n    <div class=\"w3-example\">\r\n        <h3>Basic Math: Max and Min</h3>\r\n        <p>The <code>max(x, y)</code> and <code>min(x, y)</code> functions can be used to find the highest or lowest value:</p>\r\n        <pre><code>cout << max(5, 10); // Outputs 10\r\ncout << min(5, 10); // Outputs 5</code></pre>\r\n    </div>\r\n\r\n    <hr class=\"my-8 border-gray-100\">\r\n\r\n    <h2>The &lt;cmath&gt; Library</h2>\r\n    <p>By including <code>#include &lt;cmath&gt;</code>, you gain access to functions like <code>sqrt</code> (square root), <code>round</code> (rounding), and <code>log</code> (natural logarithm).</p>\r\n\r\n    <div class=\"w3-example\">\r\n        <h3>Examples:</h3>\r\n        <pre><code>#include &lt;iostream&gt;\r\n#include &lt;cmath&gt;\r\nusing namespace std;\r\n\r\nint main() {\r\n  cout << sqrt(64) << \"\\n\";   // Outputs 8\r\n  cout << round(2.6) << \"\\n\"; // Outputs 3\r\n  cout << pow(2, 3) << \"\\n\";  // 2 to the power of 3 (8)\r\n  return 0;\r\n}</code></pre>\r\n    </div>\r\n</div>', NULL, NULL, 'Topic: C++ Math functions and cmath library. Key Concepts: Built-in max() and min(). Including the <cmath> header for advanced math. Standard functions: sqrt(), pow(), abs(), round(). Quiz Focus: Identifying the correct header for math functions, predicting results of sqrt() or pow(), and using max/min.', 6),
(59, 3, 'C++ Strings', '<div class=\"topic-content-area\">\r\n    <h2>Working with Strings</h2>\r\n    <p>A <code>string</code> variable contains a collection of characters surrounded by double quotes. To use strings, you must include the <code>&lt;string&gt;</code> header file in your source code.</p>\r\n    \r\n    <div class=\"w3-example\">\r\n        <pre><code>#include &lt;string&gt;\r\nstring greeting = \"Hello\";</code></pre>\r\n    </div>\r\n\r\n    <hr class=\"my-8 border-gray-100\">\r\n\r\n    <h2>String Concatenation & Length</h2>\r\n    <p>You can join strings using the <code>+</code> operator. To find the length of a string, use the <code>length()</code> or <code>size()</code> function.</p>\r\n\r\n    <div class=\"w3-example\">\r\n        <pre><code>string firstName = \"John \";\r\nstring lastName = \"Doe\";\r\nstring fullName = firstName + lastName;\r\ncout << fullName.length(); // Outputs 8</code></pre>\r\n    </div>\r\n\r\n    <hr class=\"my-8 border-gray-100\">\r\n\r\n    <h2>Accessing Characters</h2>\r\n    <p>You can access characters in a string by referring to its index number inside square brackets <code>[]</code>. Indexing starts at <b>0</b>.</p>\r\n    <div class=\"w3-example\">\r\n        <pre><code>string myString = \"Hello\";\r\nmyString[0] = \'J\';\r\ncout << myString; // Outputs \"Jello\"</code></pre>\r\n    </div>\r\n</div>', NULL, NULL, 'Topic: C++ Strings library. Key Concepts: #include <string>. Concatenation using + or .append(). Finding length with .length(). Accessing and modifying characters via zero-based indexing []. Quiz Focus: Correct header for strings, predicting concatenation results, and identifying characters at specific indices.', 7),
(60, 3, 'C++ Booleans', '<div class=\"topic-content-area\">\r\n    <h2>Boolean Values</h2>\r\n    <p>In C++, the <code>bool</code> data type can take the values <code>true</code> (1) or <code>false</code> (0).</p>\r\n    \r\n    <div class=\"w3-example\">\r\n        <pre><code>bool isCodingFun = true;\r\nbool isFishTasty = false;\r\ncout << isCodingFun;  // Outputs 1\r\ncout << isFishTasty;  // Outputs 0</code></pre>\r\n    </div>\r\n\r\n    <hr class=\"my-8 border-gray-100\">\r\n\r\n    <h2>Boolean Expressions</h2>\r\n    <p>A Boolean expression returns a boolean value by comparing values/variables. This is useful for building logic.</p>\r\n\r\n    <div class=\"w3-example\">\r\n        <pre><code>int x = 10;\r\nint y = 9;\r\ncout << (x > y); // Returns 1 (true) because 10 is higher than 9</code></pre>\r\n    </div>\r\n\r\n    <div class=\"bg-blue-50 p-4 rounded-lg text-sm mt-6\">\r\n        <p><b>Real-life Use:</b> You can use boolean logic to check if a user is old enough to vote, if a light is on, or if a game level is completed.</p>\r\n    </div>\r\n</div>', NULL, NULL, 'Topic: C++ Booleans. Key Concepts: bool data type. Truth values as 1 (true) and 0 (false). Comparison expressions yielding boolean results. Quiz Focus: Predicting boolean outcomes of integer comparisons and understanding the numeric representation of true/false in C++.', 8),
(61, 3, 'C++ Conditional Statements', '<div class=\"topic-content-area\">\r\n    <h2>C++ Conditions</h2>\r\n    <p>C++ uses conditional statements to perform different actions based on different conditions.</p>\r\n    \r\n    <div class=\"w3-example\">\r\n        <ul>\r\n            <li><b>if:</b> executes code if a condition is true.</li>\r\n            <li><b>else:</b> executes code if the same condition is false.</li>\r\n            <li><b>else if:</b> specifies a new condition to test if the first condition is false.</li>\r\n        </ul>\r\n    </div>\r\n\r\n    \r\n\r\n    <div class=\"w3-example\">\r\n        <h3>Example:</h3>\r\n        <pre><code>int time = 22;\r\nif (time < 10) {\r\n  cout << \"Good morning.\";\r\n} else if (time < 20) {\r\n  cout << \"Good day.\";\r\n} else {\r\n  cout << \"Good evening.\";\r\n}</code></pre>\r\n    </div>\r\n\r\n    <hr class=\"my-8 border-gray-100\">\r\n\r\n    <h2>Short Hand If...Else (Ternary Operator)</h2>\r\n    <p>There is also a short-hand if else, which is known as the ternary operator because it consists of three operands.</p>\r\n    <code>variable = (condition) ? expressionTrue : expressionFalse;</code>\r\n</div>', NULL, NULL, 'Topic: C++ Conditional Statements. Key Concepts: if, else, else if syntax. Block scope using {}. Ternary operator syntax ? :. Quiz Focus: Correct syntax for else if, predicting execution paths, and identifying correct ternary operator usage.', 9),
(62, 3, 'C++ Switch Statement', '<div class=\"topic-content-area\">\r\n    <h2>The switch Statement</h2>\r\n    <p>Use the <code>switch</code> statement to select one of many code blocks to be executed. It is often used as a cleaner alternative to multiple <code>else if</code> statements.</p>\r\n    \r\n    <div class=\"w3-example\">\r\n        <pre><code>int day = 4;\r\nswitch (day) {\r\n  case 1:\r\n    cout << \"Monday\";\r\n    break;\r\n  case 2:\r\n    cout << \"Tuesday\";\r\n    break;\r\n  case 4:\r\n    cout << \"Thursday\";\r\n    break;\r\n  default:\r\n    cout << \"Looking forward to the Weekend\";\r\n}</code></pre>\r\n    </div>\r\n\r\n    <hr class=\"my-8 border-gray-100\">\r\n\r\n    <h2>The break and default Keywords</h2>\r\n    <ul>\r\n        <li><b>break:</b> When C++ reaches a <code>break</code> keyword, it breaks out of the switch block. This stops the execution of more code and case testing inside the block.</li>\r\n        <li><b>default:</b> The <code>default</code> keyword specifies some code to run if there is no case match.</li>\r\n    </ul>\r\n</div>', NULL, NULL, 'Topic: C++ Switch statement. Key Concepts: switch variable evaluation. case labels. The necessity of break to prevent fall-through. default case for catch-all logic. Quiz Focus: Identifying what happens if a break is missing, the purpose of the default keyword, and valid data types for switch variables (integers and chars).', 10),
(63, 3, 'C++ While Loops', '<div class=\"topic-content-area\">\r\n    <h2>The while Loop</h2>\r\n    <p>Loops can execute a block of code as long as a specified condition is <b>true</b>. Loops save time, reduce errors, and make code more readable.</p>\r\n    \r\n    <div class=\"w3-example\">\r\n        <h3>Syntax</h3>\r\n        <pre><code>while (condition) {\r\n  // code block to be executed\r\n}</code></pre>\r\n    </div>\r\n\r\n    <div class=\"w3-example bg-gray-50\">\r\n        <h3>Example</h3>\r\n        <pre><code>int i = 0;\r\nwhile (i < 5) {\r\n  cout << i << \"\\n\";\r\n  i++;\r\n}</code></pre>\r\n    </div>\r\n\r\n    <p class=\"text-red-600 font-bold\">Important Note:</p>\r\n    <p>Do not forget to increase the variable used in the condition (<code>i++</code>), otherwise the loop will never end. This is called an <b>infinite loop</b>.</p>\r\n\r\n    <hr class=\"my-8 border-gray-100\">\r\n\r\n    <h2>The do/while Loop</h2>\r\n    <p>The <code>do/while</code> loop is a variant of the while loop. This loop will execute the code block <b>once</b>, before checking if the condition is true, then it will repeat the loop as long as the condition is true.</p>\r\n    \r\n    <div class=\"w3-example\">\r\n        <pre><code>int i = 0;\r\ndo {\r\n  cout << i << \"\\n\";\r\n  i++;\r\n}\r\nwhile (i < 5);</code></pre>\r\n    </div>\r\n</div>', NULL, NULL, 'Topic: C++ While and Do/While Loops. Key Concepts: Iteration based on a boolean condition. Requirement of an updater (increment/decrement) to avoid infinite loops. The fundamental difference: while checks the condition before the first run; do/while runs at least once before checking. Quiz Focus: Predicting the number of iterations, identifying infinite loops, and distinguishing between while and do/while logic.', 11),
(64, 3, 'C++ For Loops', '<div class=\"topic-content-area\">\r\n    <h2>The for Loop</h2>\r\n    <p>When you know exactly how many times you want to loop through a block of code, use the <code>for</code> loop instead of a <code>while</code> loop.</p>\r\n    \r\n    <div class=\"w3-example\">\r\n        <h3>Syntax</h3>\r\n        <pre><code>for (statement 1; statement 2; statement 3) {\r\n  // code block to be executed\r\n}</code></pre>\r\n    </div>\r\n\r\n    <ul>\r\n        <li><b>Statement 1:</b> Executed (one time) before the execution of the code block. Usually used for a counter.</li>\r\n        <li><b>Statement 2:</b> Defines the condition for executing the code block.</li>\r\n        <li><b>Statement 3:</b> Executed (every time) after the code block has been executed. Usually updates the counter.</li>\r\n    </ul>\r\n\r\n    \r\n\r\n    <div class=\"w3-example bg-gray-50\">\r\n        <h3>Example</h3>\r\n        <pre><code>for (int i = 0; i < 5; i++) {\r\n  cout << i << \"\\n\";\r\n}</code></pre>\r\n    </div>\r\n</div>', NULL, NULL, 'Topic: C++ For Loops. Key Concepts: Structure of the for loop (initialization; condition; increment). Scope of the loop variable. Counting up and counting down. Quiz Focus: Determining the number of times a loop runs based on the range, identifying the three components of the loop header, and predicting the final value of the counter.', 12),
(65, 3, 'C++ Break and Continue', '<div class=\"topic-content-area\">\r\n    <h2>Break and Continue</h2>\r\n    <p>C++ provides two special keywords that allow you to jump out of a loop or skip a specific iteration.</p>\r\n    \r\n    <hr class=\"my-8 border-gray-100\">\r\n\r\n    <h3>The break Statement</h3>\r\n    <p>The <code>break</code> statement is used to jump out of a <b>loop</b> (or a switch) entirely.</p>\r\n    <div class=\"w3-example\">\r\n        <pre><code>for (int i = 0; i < 10; i++) {\r\n  if (i == 4) {\r\n    break;\r\n  }\r\n  cout << i << \"\\n\";\r\n} // Loop stops completely when i is 4</code></pre>\r\n    </div>\r\n\r\n    <hr class=\"my-8 border-gray-100\">\r\n\r\n    <h3>The continue Statement</h3>\r\n    <p>The <code>continue</code> statement breaks one iteration (in the loop), if a specified condition occurs, and continues with the next iteration in the loop.</p>\r\n    <div class=\"w3-example\">\r\n        <pre><code>for (int i = 0; i < 10; i++) {\r\n  if (i == 4) {\r\n    continue;\r\n  }\r\n  cout << i << \"\\n\";\r\n} // Skips the number 4, then continues to 5</code></pre>\r\n    </div>\r\n</div>', NULL, NULL, 'Topic: C++ Loop Control (Break/Continue). Key Concepts: Terminating a loop early with break. Skipping current iteration logic with continue. Efficiency in searching or filtering. Quiz Focus: Predicting loop output when break/continue is encountered and differentiating between loop termination vs. skipping.', 13),
(66, 3, 'C++ Arrays', '<div class=\"topic-content-area\">\r\n    <h2>Introduction to Arrays</h2>\r\n    <p>Arrays are used to store multiple values in a single variable, instead of declaring separate variables for each value.</p>\r\n    \r\n    <div class=\"w3-example\">\r\n        <h3>Declaration</h3>\r\n        <p>To declare an array, define the variable type, specify the name of the array followed by <b>square brackets</b> and specify the number of elements it should store:</p>\r\n        <pre><code>string cars[4] = {\"Volvo\", \"BMW\", \"Ford\", \"Mazda\"};\r\nint myNum[3] = {10, 20, 30};</code></pre>\r\n    </div>\r\n\r\n    \r\n\r\n    <hr class=\"my-8 border-gray-100\">\r\n\r\n    <h2>Accessing Elements</h2>\r\n    <p>You access an array element by referring to the index number. Array indices start at <b>0</b>.</p>\r\n    <div class=\"w3-example\">\r\n        <pre><code>string cars[4] = {\"Volvo\", \"BMW\", \"Ford\", \"Mazda\"};\r\ncout << cars[0]; // Outputs \"Volvo\"\r\n\r\ncars[0] = \"Opel\"; // Changes Volvo to Opel</code></pre>\r\n    </div>\r\n\r\n    <p class=\"text-xs text-gray-500 italic mt-4\">Note: If you access an index that doesn\'t exist (like index 10 in a 4-item array), C++ will likely crash or show garbage data.</p>\r\n</div>', NULL, NULL, 'Topic: C++ Arrays Basics. Key Concepts: Fixed-size collections. Square bracket [] syntax. Initialization with curly braces {}. Zero-based indexing. Modifying elements. Quiz Focus: Identifying the index of a specific element, correct array declaration syntax, and accessing values using indices.', 14),
(67, 3, 'C++ Arrays and Loops', '<div class=\"topic-content-area\">\r\n    <h2>Looping Through Arrays</h2>\r\n    <p>You can loop through the array elements with the <code>for</code> loop. The <code>sizeof()</code> operator is often used to help determine array size.</p>\r\n    \r\n    <div class=\"w3-example\">\r\n        <pre><code>string cars[5] = {\"Volvo\", \"BMW\", \"Ford\", \"Mazda\", \"Tesla\"};\r\nfor (int i = 0; i < 5; i++) {\r\n  cout << cars[i] << \"\\n\";\r\n}</code></pre>\r\n    </div>\r\n\r\n    <hr class=\"my-8 border-gray-100\">\r\n\r\n    <h2>The for-each Loop</h2>\r\n    <p>Introduced in C++11, there is also a \"range-based for loop\", which is used exclusively to loop through elements in an array:</p>\r\n    <div class=\"w3-example\">\r\n        <pre><code>int myNumbers[5] = {10, 20, 30, 40, 50};\r\nfor (int i : myNumbers) {\r\n  cout << i << \"\\n\";\r\n}</code></pre>\r\n    </div>\r\n\r\n    <p class=\"bg-blue-50 p-4 border-l-4 border-blue-500 rounded-r-lg mt-6\">\r\n        <b>Tip:</b> Range-based loops are safer and easier to read because you don\'t have to manage the counter <code>i</code> or check the array size.\r\n    </p>\r\n</div>', NULL, NULL, 'Topic: Iterating through Arrays in C++. Key Concepts: Using standard for loops with indices. The range-based for loop (C++11). Efficiency and readability differences between the two. Quiz Focus: Predicting loop output for array elements and identifying correct range-based for loop syntax.', 15),
(68, 3, 'C++ References', '<div class=\"topic-content-area\">\r\n    <h2>References in C++</h2>\r\n    <p>A reference variable is a \"nickname\" or an alias for an existing variable. It is created with the <code>&</code> operator.</p>\r\n    \r\n    <div class=\"w3-example\">\r\n        <pre><code>string food = \"Pizza\";\r\nstring &meal = food; // meal is now a reference to food\r\n\r\ncout << food; // Outputs Pizza\r\ncout << meal; // Outputs Pizza</code></pre>\r\n    </div>\r\n\r\n    <hr class=\"my-8 border-gray-100\">\r\n\r\n    <h2>The Memory Address</h2>\r\n    <p>The <code>&</code> operator can also be used to get the <b>memory address</b> of a variable; which is the location of where the variable is stored on the computer.</p>\r\n    <div class=\"w3-example bg-gray-50\">\r\n        <pre><code>string food = \"Pizza\";\r\ncout << &food; // Outputs something like 0x6dfed4</code></pre>\r\n    </div>\r\n    <p><b>Note:</b> The memory address is in hexadecimal form (0x...). Knowing the address is essential for understanding how Pointers work.</p>\r\n</div>', NULL, NULL, 'Topic: C++ References and Memory Addresses. Key Concepts: Creating aliases using the ampersand (&). The distinction between a reference and the actual variable. Accessing the physical memory address of a variable. Quiz Focus: Identifying reference syntax, understanding that changing a reference changes the original variable, and recognizing hexadecimal memory addresses.', 16),
(69, 3, 'C++ Pointers', '<div class=\"topic-content-area\">\r\n    <h2>What is a Pointer?</h2>\r\n    <p>A <b>pointer</b> is a variable that <b>stores the memory address</b> of another variable as its value. Pointers are one of C++\'s most powerful features.</p>\r\n    \r\n    <div class=\"w3-example\">\r\n        <p>A pointer variable is created with the <code>*</code> operator. The type of the pointer must match the type of the variable you are working with.</p>\r\n        <pre><code>string food = \"Pizza\";  // Variable declaration\r\nstring* ptr = &food;    // Pointer declaration (stores address of food)\r\n\r\ncout << food;  // Outputs the value (Pizza)\r\ncout << &food; // Outputs the address (0x6dfed4)\r\ncout << ptr;   // Outputs the address (0x6dfed4)</code></pre>\r\n    </div>\r\n\r\n    \r\n\r\n    <hr class=\"my-8 border-gray-100\">\r\n\r\n    <h2>Dereferencing</h2>\r\n    <p>You can also use the <code>*</code> operator to get the <b>value</b> of the variable the pointer is pointing to. This is called <b>dereferencing</b>.</p>\r\n    <div class=\"w3-example\">\r\n        <pre><code>string food = \"Pizza\";\r\nstring* ptr = &food;\r\n\r\n// Dereference: Output the value of food with the pointer\r\ncout << *ptr; // Outputs \"Pizza\"</code></pre>\r\n    </div>\r\n</div>', NULL, NULL, 'Topic: C++ Pointers and Dereferencing. Key Concepts: Pointer declaration (type* name). Storing addresses using &. Accessing values via the pointer using the dereference operator (*). Quiz Focus: Differentiating between a pointer\'s value (an address) and a dereferenced pointer\'s value (the data), and identifying correct pointer syntax.', 17),
(70, 3, 'C++ Functions', '<div class=\"topic-content-area\">\r\n    <h2>Creating and Calling Functions</h2>\r\n    <p>A function is a block of code which only runs when it is called. You can pass data, known as parameters, into a function. Functions are used to perform certain actions, and they are important for reusing code.</p>\r\n    \r\n    <div class=\"w3-example\">\r\n        <pre><code>// Declaration\r\nvoid myFunction() {\r\n  cout << \"I just got executed!\";\r\n}\r\n\r\nint main() {\r\n  myFunction(); // Call the function\r\n  return 0;\r\n}</code></pre>\r\n    </div>\r\n\r\n    <hr class=\"my-8 border-gray-100\">\r\n\r\n    <h2>Parameters and Arguments</h2>\r\n    <p>Information can be passed to functions as a parameter. Parameters act as variables inside the function.</p>\r\n    <div class=\"w3-example\">\r\n        <pre><code>void greeting(string name) {\r\n  cout << \"Hello \" << name << \"\\n\";\r\n}\r\n\r\nint main() {\r\n  greeting(\"Liam\");\r\n  greeting(\"Jenny\");\r\n  return 0;\r\n}</code></pre>\r\n    </div>\r\n</div>', NULL, NULL, 'Topic: C++ Function basics. Key Concepts: Defining functions using void or return types. Calling functions within main(). Using parameters to pass data. Quiz Focus: Distinguishing between function definition and invocation, identifying the role of parameters, and understanding code reusability.', 18),
(71, 3, 'C++ Function Overloading', '<div class=\"topic-content-area\">\r\n    <h2>Function Overloading</h2>\r\n    <p>With <b>function overloading</b>, multiple functions can have the same name with different parameters.</p>\r\n    \r\n    <div class=\"w3-example\">\r\n        <h3>Example: Multiple types for one name</h3>\r\n        <pre><code>int plusFunc(int x, int y) {\r\n  return x + y;\r\n}\r\n\r\ndouble plusFunc(double x, double y) {\r\n  return x + y;\r\n}\r\n\r\nint main() {\r\n  int myNum1 = plusFunc(8, 5);\r\n  double myNum2 = plusFunc(4.3, 6.26);\r\n  cout << \"Int: \" << myNum1 << \"\\n\";\r\n  cout << \"Double: \" << myNum2;\r\n  return 0;\r\n}</code></pre>\r\n    </div>\r\n\r\n    <p class=\"bg-blue-50 p-4 border-l-4 border-blue-500 rounded-r-lg mt-6\">\r\n        <b>Why use it?</b> Instead of defining two functions that should do the same thing (like <code>plusFuncInt</code> and <code>plusFuncDouble</code>), it is better to overload one.\r\n    </p>\r\n</div>', NULL, NULL, 'Topic: C++ Function Overloading. Key Concepts: Multiple functions with the same name. Differentiating functions by parameter type or number. Improving code readability. Quiz Focus: Identifying valid overloads, understanding how the compiler chooses the correct function, and the benefits of overloading.', 19);
INSERT INTO `topics` (`topic_id`, `language_id`, `title`, `content_description`, `pdf_file_path`, `external_link`, `ai_context_summary`, `order_sequence`) VALUES
(72, 3, 'C++ OOP', '<div class=\"topic-content-area\">\r\n    <h2>Classes and Objects</h2>\r\n    <p>C++ is an Object-Oriented Programming language. Everything in C++ is associated with classes and objects, along with its attributes and methods.</p>\r\n    \r\n    <div class=\"w3-example\">\r\n        <p>A <b>class</b> is a user-defined data type that we can use in our program, and it works as an object constructor, or a \"blueprint\" for creating objects.</p>\r\n        <pre><code>class MyClass {       // The class\r\n  public:             // Access specifier\r\n    int myNum;        // Attribute (int variable)\r\n    string myString;  // Attribute (string variable)\r\n};\r\n\r\nint main() {\r\n  MyClass myObj;     // Create an object of MyClass\r\n\r\n  // Access attributes and set values\r\n  myObj.myNum = 15; \r\n  myObj.myString = \"Some text\";\r\n\r\n  cout << myObj.myNum;\r\n  return 0;\r\n}</code></pre>\r\n    </div>\r\n\r\n    \r\n\r\n    <hr class=\"my-8 border-gray-100\">\r\n\r\n    <h2>The \"public\" Keyword</h2>\r\n    <p>The <code>public</code> keyword is an <b>access specifier</b>, which specifies that members (attributes and methods) of the class are accessible from outside the class.</p>\r\n</div>', NULL, NULL, 'Topic: C++ Classes and Objects. Key Concepts: Procedural vs Object-Oriented programming. Class as a blueprint. Objects as instances. The public access specifier. Accessing attributes using the dot (.) operator. Quiz Focus: Defining a class, creating an object, the purpose of access specifiers, and how to access class members.', 20),
(73, 4, 'Introduction to JavaScript', '<div class=\"topic-content-area\">\r\n    <h2>What is JavaScript?</h2>\r\n    <p>JavaScript is the world\'s most popular programming language. It is the language of the <b>Web</b>. It is easy to learn and powerful enough to build complex applications like Gmail or Facebook.</p>\r\n    \r\n    <h3>Why Study JavaScript?</h3>\r\n    <p>JavaScript is one of the <b>3 languages</b> all web developers must learn:</p>\r\n    <ul>\r\n        <li><b>HTML</b> to define the content of web pages.</li>\r\n        <li><b>CSS</b> to specify the layout of web pages.</li>\r\n        <li><b>JavaScript</b> to program the behavior of web pages.</li>\r\n    </ul>\r\n\r\n    \r\n\r\n    <hr class=\"my-8 border-gray-100\">\r\n\r\n    <h2>Where to Put JavaScript</h2>\r\n    <p>In HTML, JavaScript code is inserted between <code>&lt;script&gt;</code> and <code>&lt;script&gt;</code> tags.</p>\r\n\r\n    <div class=\"w3-example\">\r\n        <h3>Example: Internal Script</h3>\r\n        <pre><code>&lt;script&gt;\r\n  alert(\"Hello, World!\");\r\n&lt;/script&gt;</code></pre>\r\n    </div>\r\n\r\n    <p>You can also place scripts in <b>External files</b> (with a <code>.js</code> extension), which is the best practice for keeping your code organized.</p>\r\n    <div class=\"w3-example bg-blue-50\">\r\n        <code>&lt;script src=\"myscript.js\"&gt;&lt;/script&gt;</code>\r\n    </div>\r\n</div>', NULL, NULL, 'Topic: Introduction to JavaScript. Key Concepts: JavaScript\'s role in the web (Behavior). The script tag <script>. Internal vs. External JavaScript. Relationship with HTML and CSS. Quiz Focus: Identifying the tag used for JS in HTML, understanding the difference between HTML/CSS/JS roles, and the benefit of external .js files.', 1),
(74, 4, 'JavaScript Output', '<div class=\"topic-content-area\">\r\n    <h2>Displaying Data</h2>\r\n    <p>JavaScript can \"display\" data in different ways. Depending on whether you want to alert the user or log data for debugging, you will use different methods.</p>\r\n    \r\n    <h3>Common Output Methods:</h3>\r\n    <ul>\r\n        <li><b>innerHTML:</b> Writing into an HTML element.</li>\r\n        <li><b>document.write():</b> Writing directly into the HTML output (mostly used for testing).</li>\r\n        <li><b>window.alert():</b> Writing into an alert box.</li>\r\n        <li><b>console.log():</b> Writing into the browser console (the primary tool for developers).</li>\r\n    </ul>\r\n\r\n    <div class=\"w3-example\">\r\n        <h3>Example: Using console.log</h3>\r\n        <p>This is the most common way to debug your code. You can see the output in the \"Console\" tab of your browser\'s Developer Tools.</p>\r\n        <pre><code>console.log(5 + 6); // Outputs 11 in the console</code></pre>\r\n    </div>\r\n\r\n    <div class=\"w3-example\">\r\n        <h3>Example: Using innerHTML</h3>\r\n        <pre><code>document.getElementById(\"demo\").innerHTML = \"Hello JS!\";</code></pre>\r\n    </div>\r\n</div>', NULL, NULL, 'Topic: JavaScript Output Methods. Key Concepts: Four ways to display data: innerHTML, document.write(), window.alert(), and console.log(). Accessing elements via document.getElementById(). Quiz Focus: Choosing the best output method for debugging (console.log), identifying the correct syntax for changing HTML content, and understanding the behavior of alert boxes.', 2),
(75, 4, 'JavaScript Statements & Syntax', '<div class=\"topic-content-area\">\r\n    <h2>JavaScript Statements</h2>\r\n    <p>A computer program is a list of \"instructions\" to be \"executed\" by a computer. In a programming language, these programming instructions are called <b>statements</b>.</p>\r\n    \r\n    <div class=\"w3-example\">\r\n        <pre><code>let x, y, z;    // Statement 1\r\nx = 5;          // Statement 2\r\ny = 6;          // Statement 3\r\nz = x + y;      // Statement 4</code></pre>\r\n    </div>\r\n\r\n    <hr class=\"my-8 border-gray-100\">\r\n\r\n    <h2>Syntax Basics</h2>\r\n    <ul>\r\n        <li><b>Semicolons (;):</b> Semicolons separate JavaScript statements. While sometimes optional in JS, it is a professional standard to use them.</li>\r\n        <li><b>White Space:</b> JavaScript ignores multiple spaces. You can add white space to make your script more readable.</li>\r\n        <li><b>Case Sensitivity:</b> JavaScript is <b>case-sensitive</b>. A variable named <code>lastName</code> is different from <code>lastname</code>.</li>\r\n    </ul>\r\n\r\n    <div class=\"w3-example bg-gray-100\">\r\n        <h3>Comments</h3>\r\n        <p>Single line comments start with <code>//</code>.</p>\r\n        <p>Multi-line comments start with <code>/*</code> and end with <code>*/</code>.</p>\r\n    </div>\r\n</div>', NULL, NULL, 'Topic: JS Statements and Syntax. Key Concepts: Statements as instructions. Semicolon usage. Case sensitivity in naming variables. Commenting styles. Quiz Focus: Identifying correct comment syntax, recognizing case-sensitive variable names, and understanding statement separation.', 3),
(76, 4, 'JavaScript Variables', '<div class=\"topic-content-area\">\r\n    <h2>Declaring Variables</h2>\r\n    <p>Variables are containers for storing data. In modern JavaScript (ES6), there are three ways to declare a variable:</p>\r\n    \r\n    <div class=\"w3-example\">\r\n        <ul>\r\n            <li><b>let:</b> The modern way to declare variables. It has block scope (preferred).</li>\r\n            <li><b>const:</b> Used for variables that <b>should not change</b>. It is short for \"constant.\"</li>\r\n            <li><b>var:</b> The old way (pre-2015). It is generally avoided in modern coding.</li>\r\n        </ul>\r\n    </div>\r\n\r\n    <hr class=\"my-8 border-gray-100\">\r\n\r\n    <h2>When to use const?</h2>\r\n    <p>Always declare a variable with <code>const</code> if you know that the value will not change. Use <code>const</code> for:</p>\r\n    <ul>\r\n        <li>Arrays</li>\r\n        <li>Objects</li>\r\n        <li>Functions</li>\r\n        <li>Fixed values (like PI)</li>\r\n    </ul>\r\n\r\n    <div class=\"w3-example\">\r\n        <h3>Example:</h3>\r\n        <pre><code>const price1 = 5;\r\nconst price2 = 6;\r\nlet total = price1 + price2;</code></pre>\r\n    </div>\r\n</div>', NULL, NULL, 'Topic: JavaScript Variables (let vs const vs var). Key Concepts: Introduction of ES6 standards. Block scope. Use cases for const (immutable references). Variable naming rules (cannot start with a number). Quiz Focus: Choosing between let and const, identifying variable naming errors, and understanding the read-only nature of const.', 4),
(77, 4, 'JavaScript Data Types', '<div class=\"topic-content-area\">\r\n    <h2>Dynamic Types</h2>\r\n    <p>JavaScript has <b>dynamic types</b>. This means that the same variable can be used to hold different data types at different times.</p>\r\n    \r\n    <div class=\"w3-example\">\r\n        <h3>Core Data Types:</h3>\r\n        <ul>\r\n            <li><b>String:</b> Text, e.g., <code>\"John Doe\"</code>.</li>\r\n            <li><b>Number:</b> All numbers (integers or decimals), e.g., <code>3.14</code>.</li>\r\n            <li><b>BigInt:</b> For very large integers.</li>\r\n            <li><b>Boolean:</b> <code>true</code> or <code>false</code>.</li>\r\n            <li><b>Undefined:</b> A variable without a value.</li>\r\n            <li><b>Null:</b> Represents an intentional empty value.</li>\r\n            <li><b>Object:</b> Complex data structures like <code>{name:\"John\"}</code>.</li>\r\n        </ul>\r\n    </div>\r\n\r\n    \r\n\r\n    <hr class=\"my-8 border-gray-100\">\r\n\r\n    <h2>The typeof Operator</h2>\r\n    <p>You can use the JavaScript <code>typeof</code> operator to find the type of a variable.</p>\r\n    <div class=\"w3-example\">\r\n        <pre><code>typeof \"\"             // Returns \"string\"\r\ntypeof 3.14           // Returns \"number\"\r\ntypeof false          // Returns \"boolean\"\r\ntypeof x              // Returns \"undefined\" (if x has no value)</code></pre>\r\n    </div>\r\n</div>', NULL, NULL, 'Topic: JavaScript Data Types. Key Concepts: Primitive types (String, Number, Boolean, Undefined, Null, Symbol, BigInt). Dynamic typing. Objects. The typeof operator. Quiz Focus: Determining the data type of a value using typeof, identifying the behavior of undefined variables, and recognizing dynamic typing in action.', 5),
(78, 4, 'JavaScript Functions', '<div class=\"topic-content-area\">\r\n    <h2>JavaScript Functions</h2>\r\n    <p>A JavaScript function is a block of code designed to perform a particular task. A function is executed when \"something\" invokes it (calls it).</p>\r\n    \r\n    <h3>Function Syntax</h3>\r\n    <p>A function is defined with the <code>function</code> keyword, followed by a <b>name</b>, followed by parentheses <b>()</b>.</p>\r\n    <div class=\"w3-example\">\r\n        <pre><code>function name(parameter1, parameter2) {\r\n  // code to be executed\r\n}</code></pre>\r\n    </div>\r\n\r\n    <hr class=\"my-8 border-gray-100\">\r\n\r\n    <h2>The Return Statement</h2>\r\n    <p>When JavaScript reaches a <code>return</code> statement, the function will stop executing. If the function was invoked from a statement, JavaScript will \"return\" to execute the code after the invoking statement.</p>\r\n\r\n    <div class=\"w3-example\">\r\n        <h3>Example: Calculate Product</h3>\r\n        <pre><code>let x = myFunction(4, 3);   // Function is called, return value will end up in x\r\n\r\nfunction myFunction(a, b) {\r\n  return a * b;             // Function returns the product of a and b\r\n}</code></pre>\r\n    </div>\r\n\r\n    <div class=\"bg-blue-50 p-4 border-l-4 border-blue-500 rounded-r-lg mt-6\">\r\n        <p class=\"text-sm\"><b>Why Functions?</b> You can reuse code: Define the code once, and use it many times with different arguments to produce different results.</p>\r\n    </div>\r\n</div>', NULL, NULL, 'Topic: JavaScript Functions. Key Concepts: Function declaration using the function keyword. Parameters (placeholders) vs. Arguments (actual values). The return statement and its role in stopping execution and outputting values. Quiz Focus: Identifying function parts, predicting return values, and understanding the difference between defining and invoking a function.', 6),
(79, 4, 'JavaScript Objects', '<div class=\"topic-content-area\">\r\n    <h2>JavaScript Objects</h2>\r\n    <p>In real life, a car is an <b>object</b>. A car has <b>properties</b> like weight and color, and <b>methods</b> like start and stop.</p>\r\n    \r\n    <p>JavaScript variables are containers for data values. Objects are variables too, but objects can contain many values. Object values are written as <b>name:value</b> pairs.</p>\r\n\r\n    <div class=\"w3-example\">\r\n        <h3>Example: Object Definition</h3>\r\n        <pre><code>const car = {type:\"Fiat\", model:\"500\", color:\"white\"};</code></pre>\r\n    </div>\r\n\r\n    \r\n\r\n    <hr class=\"my-8 border-gray-100\">\r\n\r\n    <h2>Accessing Object Properties</h2>\r\n    <p>You can access object properties in two ways:</p>\r\n    <ul>\r\n        <li><b>Dot Notation:</b> <code>objectName.propertyName</code></li>\r\n        <li><b>Bracket Notation:</b> <code>objectName[\"propertyName\"]</code></li>\r\n    </ul>\r\n\r\n    <div class=\"w3-example\">\r\n        <pre><code>console.log(car.type);     // Outputs \"Fiat\"\r\nconsole.log(car[\"model\"]); // Outputs \"500\"</code></pre>\r\n    </div>\r\n</div>', NULL, NULL, 'Topic: JavaScript Objects. Key Concepts: Objects as collections of named values (properties). Syntax using curly braces {}. Accessing data via dot notation and bracket notation. Quiz Focus: Correct syntax for object creation, identifying keys vs. values, and retrieving data from nested properties.', 7),
(80, 4, 'JavaScript Events', '<div class=\"topic-content-area\">\r\n    <h2>What is an Event?</h2>\r\n    <p>HTML events are \"things\" that happen to HTML elements. When JavaScript is used in HTML pages, JavaScript can \"react\" to these events.</p>\r\n    \r\n    <h3>Common HTML Events</h3>\r\n    <table class=\"w-full text-left text-sm\">\r\n        <tr class=\"border-b\"><th>Event</th><th>Description</th></tr>\r\n        <tr><td><code>onchange</code></td><td>An HTML element has been changed</td></tr>\r\n        <tr><td><code>onclick</code></td><td>The user clicks an HTML element</td></tr>\r\n        <tr><td><code>onmouseover</code></td><td>The user moves the mouse over an HTML element</td></tr>\r\n        <tr><td><code>onkeydown</code></td><td>The user pushes a keyboard key</td></tr>\r\n        <tr><td><code>onload</code></td><td>The browser has finished loading the page</td></tr>\r\n    </table>\r\n\r\n    <hr class=\"my-8 border-gray-100\">\r\n\r\n    <h2>JavaScript Event Handlers</h2>\r\n    <p>Event handlers can be used to handle and verify user input, user actions, and browser actions. You can add JavaScript code directly to an HTML attribute:</p>\r\n\r\n    <div class=\"w3-example\">\r\n        <pre><code>&lt;button onclick=\"document.getElementById(\'demo\').innerHTML = Date()\"&gt;\r\n  What is the time?\r\n&lt;/button&gt;</code></pre>\r\n    </div>\r\n</div>', NULL, NULL, 'Topic: JavaScript Events. Key Concepts: Interaction between user/browser and HTML elements. Event attributes (onclick, onchange, etc.). Executing JS code in response to an event. Quiz Focus: Matching events to user actions (e.g., mouseover vs. click) and identifying the correct HTML attribute for event handling.', 8),
(81, 4, 'JavaScript Strings & Methods', '<div class=\"topic-content-area\">\r\n    <h2>JavaScript Strings</h2>\r\n    <p>A JavaScript string is zero or more characters written inside quotes. You can use single or double quotes.</p>\r\n    \r\n    <div class=\"w3-example\">\r\n        <pre><code>let text = \"John Doe\";\r\nlet carName = \'Volvo\';</code></pre>\r\n    </div>\r\n\r\n    <hr class=\"my-8 border-gray-100\">\r\n\r\n    <h2>String Methods</h2>\r\n    <p>String methods help you work with text. Important methods include:</p>\r\n    <ul>\r\n        <li><b>length:</b> Property that returns the number of characters.</li>\r\n        <li><b>slice(start, end):</b> Extracts a part of a string.</li>\r\n        <li><b>replace(old, new):</b> Replaces a specified value with another value.</li>\r\n        <li><b>toUpperCase() / toLowerCase():</b> Changes the casing.</li>\r\n    </ul>\r\n\r\n    <div class=\"w3-example bg-gray-50\">\r\n        <h3>Example Snippet</h3>\r\n        <pre><code>let text = \"Hello World\";\r\nconsole.log(text.length);      // 11\r\nconsole.log(text.toUpperCase()); // \"HELLO WORLD\"</code></pre>\r\n    </div>\r\n</div>', NULL, NULL, 'Topic: JavaScript String Methods. Key Concepts: Strings as primitive data. Using the .length property. Basic manipulation methods: slice, replace, toUpperCase. Strings are immutable (methods return a new string). Quiz Focus: Finding string length, predicting output of slice(), and correctly using casing methods.', 9),
(82, 4, 'JavaScript Template Literals', '<div class=\"topic-content-area\">\r\n    <h2>Template Literals</h2>\r\n    <p>Template Literals (previously called Template Strings) use <b>back-ticks (``)</b> rather than the quotes (\"\"). This allows for much easier string building.</p>\r\n    \r\n    <h3>Variable Interpolation</h3>\r\n    <p>Template literals allow variables and expressions inside strings using the <code>${...}</code> syntax.</p>\r\n\r\n    <div class=\"w3-example bg-green-50\">\r\n        <pre><code>let firstName = \"John\";\r\nlet lastName = \"Doe\";\r\n\r\nlet text = `Welcome ${firstName}, ${lastName}!`;</code></pre>\r\n    </div>\r\n\r\n    <hr class=\"my-8 border-gray-100\">\r\n\r\n    <h2>Multi-line Strings</h2>\r\n    <p>With template literals, you can have multiline strings without using <code>\\n</code>:</p>\r\n    <div class=\"w3-example\">\r\n        <pre><code>let text = `\r\n  The quick\r\n  brown fox\r\n  jumps over\r\n  the lazy dog`;</code></pre>\r\n    </div>\r\n</div>', NULL, NULL, 'Topic: JavaScript Template Literals (ES6). Key Concepts: Back-tick usage (``). Variable substitution with ${variable}. Multi-line string support. Quiz Focus: Identifying the back-tick symbol, predicting the output of an interpolated string, and recognizing the benefits over standard concatenation.', 10),
(83, 4, 'JavaScript Numbers', '<div class=\"topic-content-area\">\r\n    <h2>JavaScript Numbers</h2>\r\n    <p>JavaScript has only one type of number. Numbers can be written with, or without, decimals.</p>\r\n    \r\n    <div class=\"w3-example\">\r\n        <pre><code>let x = 3.14;    // A number with decimals\r\nlet y = 3;       // A number without decimals</code></pre>\r\n    </div>\r\n\r\n    <hr class=\"my-8 border-gray-100\">\r\n\r\n    <h2>NaN - Not a Number</h2>\r\n    <p><code>NaN</code> is a JavaScript reserved word indicating that a number is not a legal number. Trying to do arithmetic with a non-numeric string will result in <code>NaN</code>.</p>\r\n\r\n    <div class=\"w3-example\">\r\n        <pre><code>let x = 100 / \"Apple\";  // x will be NaN\r\nlet y = 100 / \"10\";     // y will be 10 (JS converts strings automatically in math)</code></pre>\r\n    </div>\r\n\r\n    <div class=\"bg-blue-50 p-4 border-l-4 border-blue-500 rounded-r-lg mt-6\">\r\n        <p class=\"text-sm\"><b>Note:</b> If you use <code>NaN</code> in a mathematical operation, the result will also be <code>NaN</code>.</p>\r\n    </div>\r\n</div>', NULL, NULL, 'Topic: JavaScript Numbers and NaN. Key Concepts: Single number type (64-bit float). Automatic string-to-number conversion in division/multiplication. Definition and behavior of NaN (Not a Number). Using isNaN() to check values. Quiz Focus: Identifying what causes NaN, the result of mixing numbers and non-numeric strings, and the difference between \"10\" (numeric string) and \"Apple\" in calculations.', 11),
(84, 4, 'JavaScript Arrays', '<div class=\"topic-content-area\">\r\n    <h2>JavaScript Arrays</h2>\r\n    <p>Arrays are used to store multiple values in a single variable. It is a special type of object that can hold more than one value at a time.</p>\r\n    \r\n    <div class=\"w3-example\">\r\n        <h3>Creating an Array</h3>\r\n        <pre><code>const cars = [\"Saab\", \"Volvo\", \"BMW\"];</code></pre>\r\n    </div>\r\n\r\n    \r\n\r\n    <hr class=\"my-8 border-gray-100\">\r\n\r\n    <h2>Accessing Elements</h2>\r\n    <p>You access an array element by referring to the <b>index number</b>. Array indices start with <b>0</b>.</p>\r\n    <div class=\"w3-example\">\r\n        <pre><code>const fruits = [\"Banana\", \"Orange\", \"Apple\"];\r\nlet fruit = fruits[0]; // Accesses \"Banana\"\r\n\r\nfruits[0] = \"Pear\";    // Changes \"Banana\" to \"Pear\"</code></pre>\r\n    </div>\r\n\r\n    <h3>The length Property</h3>\r\n    <p>The <code>length</code> property of an array returns the number of elements it contains.</p>\r\n    <pre><code>console.log(fruits.length); // Outputs 3</code></pre>\r\n</div>', NULL, NULL, 'Topic: JavaScript Arrays. Key Concepts: Storing multiple values in one variable. Literal notation []. Zero-based indexing. Modifying elements by index. The .length property. Quiz Focus: Identifying the value at a specific index, calculating array length, and correctly modifying an element within an array.', 12),
(85, 4, 'JavaScript Array Methods', '<div class=\"topic-content-area\">\r\n    <h2>Common Array Methods</h2>\r\n    <p>JavaScript provides built-in methods to add, remove, and combine elements in an array.</p>\r\n    \r\n    <div class=\"w3-example\">\r\n        <ul>\r\n            <li><b>push():</b> Adds a new element to the <b>end</b> of an array.</li>\r\n            <li><b>pop():</b> Removes the <b>last</b> element from an array.</li>\r\n            <li><b>shift():</b> Removes the <b>first</b> element.</li>\r\n            <li><b>unshift():</b> Adds an element to the <b>beginning</b>.</li>\r\n        </ul>\r\n    </div>\r\n\r\n    <hr class=\"my-8 border-gray-100\">\r\n\r\n    <h2>Converting Arrays to Strings</h2>\r\n    <p>The <code>toString()</code> method converts an array to a string of (comma separated) array values.</p>\r\n    <div class=\"w3-example\">\r\n        <pre><code>const fruits = [\"Banana\", \"Orange\", \"Apple\"];\r\nconsole.log(fruits.join(\" * \")); // \"Banana * Orange * Apple\"</code></pre>\r\n    </div>\r\n\r\n    <p>The <code>join()</code> method also joins all array elements into a string, but you can specify the separator!</p>\r\n</div>', NULL, NULL, 'Topic: JavaScript Array Methods (Basic). Key Concepts: Mutating arrays using push, pop, shift, and unshift. Converting arrays to strings using toString and join. Quiz Focus: Differentiating between push and unshift, predicting the state of an array after a pop() call, and identifying the result of the join method.', 13),
(86, 4, 'JavaScript Math Object', '<div class=\"topic-content-area\">\r\n    <h2>The Math Object</h2>\r\n    <p>The JavaScript <code>Math</code> object allows you to perform mathematical tasks on numbers. Unlike other objects, you do not need to create it; you call the methods directly.</p>\r\n    \r\n    <div class=\"w3-example\">\r\n        <h3>Common Methods:</h3>\r\n        <ul>\r\n            <li><b>Math.round(x):</b> Rounds to the nearest integer.</li>\r\n            <li><b>Math.ceil(x):</b> Rounds <b>up</b> to the nearest integer.</li>\r\n            <li><b>Math.floor(x):</b> Rounds <b>down</b> to the nearest integer.</li>\r\n            <li><b>Math.sqrt(x):</b> Returns the square root of x.</li>\r\n            <li><b>Math.random():</b> Returns a random number between 0 and 1.</li>\r\n        </ul>\r\n    </div>\r\n\r\n    <div class=\"w3-example bg-gray-50\">\r\n        <h3>Example:</h3>\r\n        <pre><code>Math.round(4.6); // 5\r\nMath.ceil(4.1);  // 5\r\nMath.floor(4.9); // 4</code></pre>\r\n    </div>\r\n</div>', NULL, NULL, 'Topic: JavaScript Math Object. Key Concepts: Static Math methods. Difference between round, ceil, and floor. Generating random decimals with Math.random(). Using Math.PI. Quiz Focus: Predicting results of rounding methods, identifying the method for square roots, and knowing the range of Math.random().', 14),
(87, 4, 'JavaScript Booleans & Comparisons', '<div class=\"topic-content-area\">\r\n    <h2>Booleans and Logic</h2>\r\n    <p>A JavaScript Boolean represents one of two values: <b>true</b> or <b>false</b>.</p>\r\n    \r\n    <div class=\"w3-example\">\r\n        <h3>Comparison Operators</h3>\r\n        <ul>\r\n            <li><code>==</code> Equal to</li>\r\n            <li><code>===</code> <b>Strict equal:</b> Checks both value AND type.</li>\r\n            <li><code>!=</code> Not equal</li>\r\n            <li><code>></code> Greater than</li>\r\n        </ul>\r\n    </div>\r\n\r\n    <hr class=\"my-8 border-gray-100\">\r\n\r\n    <h2>The Strict Equality Operator (===)</h2>\r\n    <p>In JavaScript, <code>5 == \"5\"</code> is <b>true</b> because JS converts the types. However, <code>5 === \"5\"</code> is <b>false</b> because the types (Number vs String) are different.</p>\r\n\r\n    <div class=\"w3-example bg-red-50\">\r\n        <pre><code>let x = 5;\r\nconsole.log(x === 5);   // true\r\nconsole.log(x === \"5\"); // false</code></pre>\r\n    </div>\r\n</div>', NULL, NULL, 'Topic: JavaScript Booleans and Comparisons. Key Concepts: Boolean values. Comparison operators. The critical difference between == (abstract equality) and === (strict equality/identity). Quiz Focus: Evaluating boolean expressions, identifying truthy/falsy logic, and predicting the output of strict equality tests.', 15),
(88, 4, 'JavaScript Conditional Statements', '<div class=\"topic-content-area\">\r\n    <h2>Conditional Statements</h2>\r\n    <p>Conditional statements are used to perform different actions based on different conditions. In JavaScript we have the following conditional statements:</p>\r\n    \r\n    <ul>\r\n        <li><b>if:</b> Use to specify a block of code to be executed, if a specified condition is true.</li>\r\n        <li><b>else:</b> Use to specify a block of code to be executed, if the same condition is false.</li>\r\n        <li><b>else if:</b> Use to specify a new condition to test, if the first condition is false.</li>\r\n    </ul>\r\n\r\n    <div class=\"w3-example\">\r\n        <h3>Example: The Time Greeting</h3>\r\n        <pre><code>const time = new Date().getHours();\r\nlet greeting;\r\n\r\nif (time < 10) {\r\n  greeting = \"Good morning\";\r\n} else if (time < 20) {\r\n  greeting = \"Good day\";\r\n} else {\r\n  greeting = \"Good evening\";\r\n}</code></pre>\r\n    </div>\r\n\r\n    \r\n\r\n    <hr class=\"my-8 border-gray-100\">\r\n\r\n    <h2>Logical Operators</h2>\r\n    <p>You can test multiple conditions using logical operators: <code>&&</code> (AND), <code>||</code> (OR), and <code>!</code> (NOT).</p>\r\n</div>', NULL, NULL, 'Topic: JavaScript Conditional Statements (If, Else, Else If). Key Concepts: Logical branching. Syntax for if, else, and else if. Using curly braces {} to define code blocks. Logical operators (&&, ||, !) for complex conditions. Quiz Focus: Predicting the execution path based on variable values, identifying correct syntax for else if, and understanding how multiple conditions are evaluated.', 16),
(89, 4, 'JavaScript Switch Statement', '<div class=\"topic-content-area\">\r\n    <h2>The switch Statement</h2>\r\n    <p>The <code>switch</code> statement is used to perform different actions based on different conditions. It is a cleaner way to handle multiple \"else if\" conditions when comparing one variable against multiple values.</p>\r\n    \r\n    <div class=\"w3-example\">\r\n        <pre><code>switch(new Date().getDay()) {\r\n  case 0:\r\n    day = \"Sunday\";\r\n    break;\r\n  case 1:\r\n    day = \"Monday\";\r\n    break;\r\n  case 6:\r\n    day = \"Saturday\";\r\n    break;\r\n  default:\r\n    day = \"Looking forward to the Weekend\";\r\n}</code></pre>\r\n    </div>\r\n\r\n    <hr class=\"my-8 border-gray-100\">\r\n\r\n    <h2>Keywords: break and default</h2>\r\n    <ul>\r\n        <li><b>break:</b> When JavaScript reaches a <code>break</code> keyword, it breaks out of the switch block. This stops the execution of more code inside the block.</li>\r\n        <li><b>default:</b> The <code>default</code> keyword specifies the code to run if there is no case match. It is similar to the <code>else</code> in an if-else statement.</li>\r\n    </ul>\r\n</div>', NULL, NULL, 'Topic: JavaScript Switch Statement. Key Concepts: Multi-way branching based on a single value. Using case labels. Importance of the break keyword to prevent \"fall-through.\" Using default for catch-all logic. Quiz Focus: Identifying the purpose of break, determining which case executes, and understanding the role of the default keyword.', 17),
(90, 4, 'JavaScript For Loop', '<div class=\"topic-content-area\">\r\n    <h2>The For Loop</h2>\r\n    <p>Loops can execute a block of code a number of times. The <code>for</code> loop is used when you know exactly how many times you want to repeat an action.</p>\r\n    \r\n    <div class=\"w3-example\">\r\n        <h3>Syntax</h3>\r\n        <pre><code>for (expression 1; expression 2; expression 3) {\r\n  // code block to be executed\r\n}</code></pre>\r\n    </div>\r\n\r\n    <p><b>Expression 1</b> is executed (one time) before the execution of the code block (initializes the counter).</p>\r\n    <p><b>Expression 2</b> defines the condition for executing the code block (must be true for the loop to continue).</p>\r\n    <p><b>Expression 3</b> is executed (every time) after the code block has been executed (usually increments the counter).</p>\r\n\r\n    \r\n\r\n    <div class=\"w3-example bg-gray-50\">\r\n        <h3>Example: Looping through an Array</h3>\r\n        <pre><code>const fruits = [\"Apple\", \"Banana\", \"Orange\"];\r\nfor (let i = 0; i < fruits.length; i++) {\r\n  console.log(fruits[i]);\r\n}</code></pre>\r\n    </div>\r\n</div>', NULL, NULL, 'Topic: JavaScript For Loop. Key Concepts: The three-part loop header: initialization, condition, and increment/decrement. Iterating through arrays using the index. Loop life cycle. Quiz Focus: Determining the number of iterations, identifying the components of the for statement, and predicting the output of an array iteration.', 18),
(91, 4, 'JavaScript Do/While Loops', '<div class=\"topic-content-area\">\r\n    <h2>The while Loop</h2>\r\n    <p>The <code>while</code> loop loops through a block of code as long as a specified condition is <b>true</b>.</p>\r\n    \r\n    <div class=\"w3-example\">\r\n        <pre><code>let i = 0;\r\nwhile (i < 5) {\r\n  console.log(\"The number is \" + i);\r\n  i++;\r\n}</code></pre>\r\n    </div>\r\n\r\n    <p class=\"text-red-600\"><b>Warning:</b> If you forget to increase the variable used in the condition, the loop will never end. This will crash your browser!</p>\r\n\r\n    <hr class=\"my-8 border-gray-100\">\r\n\r\n    <h2>The do while Loop</h2>\r\n    <p>The <code>do while</code> loop is a variant of the while loop. This loop will execute the code block <b>once</b>, before checking if the condition is true, then it will repeat the loop as long as the condition is true.</p>\r\n    <pre><code>do {\r\n  // code block\r\n} while (condition);</code></pre>\r\n</div>', NULL, NULL, 'Topic: JavaScript While and Do While Loops. Key Concepts: Conditional-based iteration. Difference between entry-controlled (while) and exit-controlled (do while) loops. The risk of infinite loops. Quiz Focus: Identifying the difference between while and do while, predicting loop behavior, and recognizing conditions that lead to infinite loops.', 19),
(92, 4, 'JavaScript Break and Continue', '<div class=\"topic-content-area\">\r\n    <h2>Loop Control: Break and Continue</h2>\r\n    <p>JavaScript provides two keywords that allow you to interrupt the normal flow of a loop.</p>\r\n    \r\n    <h3>The break Statement</h3>\r\n    <p>The <code>break</code> statement \"jumps out\" of a loop entirely. It stops the execution of the loop and moves to the code following the loop.</p>\r\n    <div class=\"w3-example\">\r\n        <pre><code>for (let i = 0; i < 10; i++) {\r\n  if (i === 3) { break; }\r\n  console.log(i); \r\n} // Outputs 0, 1, 2</code></pre>\r\n    </div>\r\n\r\n    <hr class=\"my-8 border-gray-100\">\r\n\r\n    <h3>The continue Statement</h3>\r\n    <p>The <code>continue</code> statement \"jumps over\" one iteration in the loop. It breaks the current iteration and continues with the next value in the loop.</p>\r\n    <div class=\"w3-example\">\r\n        <pre><code>for (let i = 0; i < 5; i++) {\r\n  if (i === 3) { continue; }\r\n  console.log(i);\r\n} // Outputs 0, 1, 2, 4</code></pre>\r\n    </div>\r\n</div>', NULL, NULL, 'Topic: JavaScript Break and Continue. Key Concepts: Controlling loop execution flow. Terminating a loop with break. Skipping an iteration with continue. Using these keywords within if statements inside loops. Quiz Focus: Predicting loop output when break or continue is used, and distinguishing between stopping a loop and skipping an item.', 20),
(93, 5, 'Introduction to Java', '<div class=\"topic-content-area\">\r\n    <h2>What is Java?</h2>\r\n    <p>Java is a popular programming language, created in 1995 by <b>James Gosling</b> at Sun Microsystems (now owned by Oracle). It is used for mobile applications (especially Android), desktop applications, web applications, games, and much more.</p>\r\n    \r\n    <h3>Why Use Java?</h3>\r\n    <ul>\r\n        <li><b>Platform Independent:</b> Java code is compiled into \"Bytecode,\" which runs on any machine with a Java Virtual Machine (JVM).</li>\r\n        <li><b>Object-Oriented:</b> It follows the OOP paradigm, which helps keep code modular and reusable.</li>\r\n        <li><b>Secure & Powerful:</b> It is designed to be secure and is the backbone of many enterprise-level banking and retail systems.</li>\r\n    </ul>\r\n\r\n    \r\n\r\n    <hr class=\"my-8 border-gray-100\">\r\n\r\n    <h2>Your First Java Program</h2>\r\n    <p>In Java, every application must begin with a <b>class</b> name, and that class must match the filename.</p>\r\n\r\n    <div class=\"w3-example\">\r\n        <pre><code>public class Main {\r\n  public static void main(String[] args) {\r\n    System.out.println(\"Hello World\");\r\n  }\r\n}</code></pre>\r\n    </div>\r\n\r\n    <h3>Key Components:</h3>\r\n    <ul>\r\n        <li><b>public class Main:</b> This creates a class named Main. The file must be saved as <code>Main.java</code>.</li>\r\n        <li><b>main() method:</b> This is the entry point of every Java program.</li>\r\n        <li><b>System.out.println():</b> This is used to print text to the screen.</li>\r\n    </ul>\r\n    <p class=\"text-red-600 font-bold italic\">Rule: Java is case-sensitive and every statement must end with a semicolon (;).</p>\r\n</div>', NULL, NULL, 'Topic: Introduction to Java. Key Concepts: \"Write Once, Run Anywhere\" (WORA) philosophy. The roles of the JVM (Virtual Machine) and JDK (Development Kit). Basic structure: Class name must match filename. The main method as the entry point. System.out.println for output. Quiz Focus: Identifying the entry point method, naming the platform-independent nature of Java, and recognizing that class names must match filenames.', 1),
(94, 5, 'Java Syntax & Variables', '<div class=\"topic-content-area\">\r\n    <h2>Java Variables</h2>\r\n    <p>Variables are containers for storing data values. In Java, you must declare the <b>Type</b> of the variable before you can use it.</p>\r\n    \r\n    <div class=\"w3-example\">\r\n        <h3>Primary Data Types:</h3>\r\n        <ul>\r\n            <li><b>String:</b> Text, e.g., <code>\"Hello\"</code> (Double quotes).</li>\r\n            <li><b>int:</b> Integers (whole numbers), e.g., <code>123</code>.</li>\r\n            <li><b>float:</b> Floating point numbers, e.g., <code>19.99f</code> (Requires \'f\' suffix).</li>\r\n            <li><b>char:</b> Single characters, e.g., <code>\'A\'</code> (Single quotes).</li>\r\n            <li><b>boolean:</b> <code>true</code> or <code>false</code>.</li>\r\n        </ul>\r\n    </div>\r\n\r\n    <hr class=\"my-8 border-gray-100\">\r\n\r\n    <h2>Declaring Variables</h2>\r\n    <div class=\"w3-example\">\r\n        <pre><code>String name = \"John\";\r\nint myNum = 15;\r\nfloat myFloatNum = 5.99f;\r\nchar myLetter = \'D\';\r\nboolean myBool = true;</code></pre>\r\n    </div>\r\n\r\n    <h3>The Final Keyword</h3>\r\n    <p>If you don\'t want others (or yourself) to overwrite existing values, use the <code>final</code> keyword. This makes the variable \"constant,\" meaning unchangeable and read-only.</p>\r\n    <div class=\"w3-example bg-gray-50\">\r\n        <pre><code>final int myNum = 15;\r\n// myNum = 20; // will generate an error</code></pre>\r\n    </div>\r\n</div>', NULL, NULL, 'Topic: Java Variables and Constants. Key Concepts: Strongly typed system. Declaration syntax: type name = value;. Difference between float (requires \'f\') and double. Use of final for constants. Difference between single quotes (char) and double quotes (String). Quiz Focus: Correct variable declaration syntax, identifying the effect of the final keyword, and choosing the right data type for a value.', 2),
(95, 5, 'Java Data Types', '<div class=\"topic-content-area\">\r\n    <h2>Data Type Categories</h2>\r\n    <p>Data types in Java are divided into two groups:</p>\r\n    \r\n    <h3>1. Primitive Data Types</h3>\r\n    <p>These are predefined by the language and include <code>byte</code>, <code>short</code>, <code>int</code>, <code>long</code>, <code>float</code>, <code>double</code>, <code>boolean</code>, and <code>char</code>.</p>\r\n    \r\n    \r\n\r\n    <div class=\"w3-example\">\r\n        <p><b>Integer Types:</b> Use <code>int</code> for standard numbers and <code>long</code> (with an \'L\' suffix) for very large numbers.</p>\r\n        <p><b>Floating Point:</b> Use <code>double</code> for most decimals, as it is more precise than <code>float</code>.</p>\r\n    </div>\r\n\r\n    <hr class=\"my-8 border-gray-100\">\r\n\r\n    <h3>2. Non-Primitive Data Types</h3>\r\n    <p>These are created by the programmer and are often called \"Reference types\" because they refer to objects. Examples include:</p>\r\n    <ul>\r\n        <li><b>Strings</b></li>\r\n        <li><b>Arrays</b></li>\r\n        <li><b>Classes</b></li>\r\n    </ul>\r\n    <p><b>Key Difference:</b> Primitives always have a value, while non-primitives can be <code>null</code>.</p>\r\n</div>', NULL, NULL, 'Topic: Java Data Types (Primitive vs Reference). Key Concepts: The 8 primitive types. Memory sizes (int=4 bytes, double=8 bytes). Suffixes for long (L) and float (f). Non-primitive types like Strings and Arrays. The concept of null for reference types. Quiz Focus: Distinguishing between primitive and non-primitive types, identifying which types can be null, and knowing memory sizes for common types.', 3),
(96, 5, 'Java Type Casting', '<div class=\"topic-content-area\">\r\n    <h2>Widening vs. Narrowing</h2>\r\n    <p>Type casting is when you assign a value of one primitive data type to another type.</p>\r\n    \r\n    <h3>1. Widening Casting (Automatically)</h3>\r\n    <p>Converting a smaller type to a larger type size. It is done automatically by Java because there is no risk of losing data.</p>\r\n    <div class=\"w3-example\">\r\n        <p><code>byte -> short -> char -> int -> long -> float -> double</code></p>\r\n        <pre><code>int myInt = 9;\r\ndouble myDouble = myInt; // Automatic casting: int to double</code></pre>\r\n    </div>\r\n\r\n    <hr class=\"my-8 border-gray-100\">\r\n\r\n    <h3>2. Narrowing Casting (Manually)</h3>\r\n    <p>Converting a larger type to a smaller size type. You must do this manually by placing the type in parentheses in front of the value.</p>\r\n    <div class=\"w3-example\">\r\n        <p><code>double -> float -> long -> int -> char -> short -> byte</code></p>\r\n        <pre><code>double myDouble = 9.78d;\r\nint myInt = (int) myDouble; // Manual casting: double to int\r\n// myInt will be 9 (decimal is lost)</code></pre>\r\n    </div>\r\n</div>', NULL, NULL, 'Topic: Java Type Casting. Key Concepts: Widening (Automatic) vs Narrowing (Manual). Syntax for manual casting: (targetType)value. Data loss during narrowing (e.g., losing decimals from double to int). Quiz Focus: Identifying which conversions are automatic, performing manual cast calculations, and recognizing syntax for explicit casting.', 4),
(97, 5, 'Java Operators', '<div class=\"topic-content-area\">\r\n    <h2>Operators in Java</h2>\r\n    <p>Operators are used to perform operations on variables and values. They are grouped into Arithmetic, Assignment, Comparison, and Logical operators.</p>\r\n\r\n    <h3>Arithmetic & Assignment</h3>\r\n    <div class=\"w3-example\">\r\n        <ul>\r\n            <li><code>%</code> (Modulus): Returns the division remainder.</li>\r\n            <li><code>++</code> / <code>--</code>: Increments or decrements a value by 1.</li>\r\n            <li><code>+=</code>: Add and assign (e.g., <code>x += 3</code> is <code>x = x + 3</code>).</li>\r\n        </ul>\r\n    </div>\r\n\r\n    <hr class=\"my-8 border-gray-100\">\r\n\r\n    <h3>Logical Operators</h3>\r\n    <p>Used to determine the logic between variables or values:</p>\r\n    <div class=\"w3-example\">\r\n        <ul>\r\n            <li><code>&&</code> (Logical AND): Returns true if both statements are true.</li>\r\n            <li><code>||</code> (Logical OR): Returns true if one of the statements is true.</li>\r\n            <li><code>!</code> (Logical NOT): Reverse the result (returns false if the result is true).</li>\r\n        </ul>\r\n    </div>\r\n</div>', NULL, NULL, 'Topic: Java Operators. Key Concepts: Arithmetic (+, -, *, /, %). Assignment shortcuts (+=, *=). Logical operators (&&, ||, !). Comparison operators (==, !=, >, <). Quiz Focus: Calculating results of modulus operations, evaluating boolean logic with AND/OR, and understanding increment/decrement effects.', 5),
(98, 5, 'Java Strings', '<div class=\"topic-content-area\">\r\n    <h2>Working with Strings</h2>\r\n    <p>A <code>String</code> in Java is actually an <b>object</b>, which contains methods that can perform certain operations on strings.</p>\r\n    \r\n    <h3>String Length</h3>\r\n    <p>The length of a string can be found with the <code>length()</code> method:</p>\r\n    <div class=\"w3-example\">\r\n        <pre><code>String txt = \"ABCDEFGHIJKLMNOPQRSTUVWXYZ\";\r\nSystem.out.println(\"The length is: \" + txt.length());</code></pre>\r\n    </div>\r\n\r\n    <hr class=\"my-8 border-gray-100\">\r\n\r\n    <h2>More String Methods</h2>\r\n    <ul>\r\n        <li><code>toUpperCase()</code>: Converts all characters to uppercase.</li>\r\n        <li><code>toLowerCase()</code>: Converts all characters to lowercase.</li>\r\n        <li><code>indexOf()</code>: Returns the <b>index</b> (position) of the first occurrence of a specified text in a string (including whitespace).</li>\r\n    </ul>\r\n\r\n    <div class=\"w3-example\">\r\n        <pre><code>String txt = \"Hello World\";\r\nSystem.out.println(txt.indexOf(\"World\")); // Outputs 6</code></pre>\r\n    </div>\r\n\r\n    <hr class=\"my-8 border-gray-100\">\r\n\r\n    <h2>String Concatenation</h2>\r\n    <p>The <code>+</code> operator can be used between strings to combine them. This is called <b>concatenation</b>. You can also use the <code>concat()</code> method.</p>\r\n    <div class=\"w3-example\">\r\n        <pre><code>String firstName = \"John\";\r\nString lastName = \"Doe\";\r\nSystem.out.println(firstName + \" \" + lastName);</code></pre>\r\n    </div>\r\n</div>', NULL, NULL, 'Topic: Java Strings and Methods. Key Concepts: String as a non-primitive object. Using .length() for character count. Case conversion methods. Finding substrings with .indexOf(). Concatenation using + vs .concat(). Quiz Focus: Predicting the output of string methods, understanding that indexing starts at 0, and identifying correct concatenation syntax.', 6),
(99, 5, 'Java Math', '<div class=\"topic-content-area\">\r\n    <h2>The Java Math Class</h2>\r\n    <p>The Java <code>Math</code> class has many methods that allow you to perform mathematical tasks on numbers. You don\'t need to import it; it is part of the <code>java.lang</code> package.</p>\r\n    \r\n    <div class=\"w3-example\">\r\n        <h3>Common Methods:</h3>\r\n        <ul>\r\n            <li><code>Math.max(x, y)</code>: Finds the highest value.</li>\r\n            <li><code>Math.min(x, y)</code>: Finds the lowest value.</li>\r\n            <li><code>Math.sqrt(x)</code>: Returns the square root of x.</li>\r\n            <li><code>Math.abs(x)</code>: Returns the absolute (positive) value of x.</li>\r\n            <li><code>Math.random()</code>: Returns a random number between 0.0 (inclusive), and 1.0 (exclusive).</li>\r\n        </ul>\r\n    </div>\r\n\r\n    <div class=\"w3-example bg-gray-50\">\r\n        <h3>Random Number Tip:</h3>\r\n        <p>To get a random whole number between 0 and 100, use this formula:</p>\r\n        <pre><code>int randomNum = (int)(Math.random() * 101);</code></pre>\r\n    </div>\r\n</div>', NULL, NULL, 'Topic: Java Math Class. Key Concepts: Static methods for math operations. Finding extremes (max/min). Square root and absolute values. Using Math.random() to generate decimals and casting to int for whole numbers. Quiz Focus: Identifying the output of Math.sqrt, the range of Math.random(), and choosing the correct method for absolute values.', 7),
(100, 5, 'Java Booleans & Comparisons', '<div class=\"topic-content-area\">\r\n    <h2>Boolean Values</h2>\r\n    <p>A boolean type is declared with the <code>boolean</code> keyword and can only take the values <code>true</code> or <code>false</code>.</p>\r\n    \r\n    <div class=\"w3-example\">\r\n        <pre><code>boolean isJavaFun = true;\r\nboolean isFishTasty = false;\r\nSystem.out.println(isJavaFun); // Outputs true</code></pre>\r\n    </div>\r\n\r\n    <hr class=\"my-8 border-gray-100\">\r\n\r\n    <h2>Boolean Expressions</h2>\r\n    <p>Boolean expressions use comparison operators (like <code>></code>, <code>==</code>, <code><</code>) to return a true or false result. This is the heart of logic.</p>\r\n\r\n    <div class=\"w3-example\">\r\n        <pre><code>int x = 10;\r\nint y = 9;\r\nSystem.out.println(x > y); // Returns true because 10 is higher than 9</code></pre>\r\n    </div>\r\n</div>', NULL, NULL, 'Topic: Java Booleans and Comparison Logic. Key Concepts: The boolean keyword. Evaluating truth through expressions. Using == for comparison vs = for assignment. Quiz Focus: Differentiating between assignment and comparison, predicting the boolean output of numeric expressions, and identifying the correct boolean keywords.', 8),
(101, 5, 'Java Conditional Statements', '<div class=\"topic-content-area\">\r\n    <h2>Conditional Statements</h2>\r\n    <p>Java uses conditional statements to perform different actions based on different conditions.</p>\r\n    \r\n    <div class=\"w3-example\">\r\n        <ul>\r\n            <li><b>if:</b> Code block runs if the condition is true.</li>\r\n            <li><b>else:</b> Code block runs if the condition is false.</li>\r\n            <li><b>else if:</b> Used to specify a new condition if the first one is false.</li>\r\n        </ul>\r\n    </div>\r\n\r\n    \r\n\r\n[Image of if-else-if condition flowchart]\r\n\r\n\r\n    <div class=\"w3-example\">\r\n        <h3>Example:</h3>\r\n        <pre><code>int time = 22;\r\nif (time < 10) {\r\n  System.out.println(\"Good morning.\");\r\n} else if (time < 18) {\r\n  System.out.println(\"Good day.\");\r\n} else {\r\n  System.out.println(\"Good evening.\");\r\n}\r\n// Outputs \"Good evening.\"</code></pre>\r\n    </div>\r\n\r\n    <hr class=\"my-8 border-gray-100\">\r\n\r\n    <h2>Short Hand If...Else (Ternary Operator)</h2>\r\n    <p>For simple if-else statements, you can use a one-liner:</p>\r\n    <code>variable = (condition) ? expressionTrue : expressionFalse;</code>\r\n</div>', NULL, NULL, 'Topic: Java If, Else, and Else If. Key Concepts: Branching logic. Syntax and mandatory curly braces {} for multi-line blocks. Ternary operator structure. Quiz Focus: Determining the code execution path based on variable values, identifying syntax for else if, and correctly using the ternary operator.', 9),
(102, 5, 'Java Switch Statement', '<div class=\"topic-content-area\">\r\n    <h2>The Switch Statement</h2>\r\n    <p>Use the <code>switch</code> statement to select one of many code blocks to be executed. It is often more readable than a long chain of <code>else if</code> statements.</p>\r\n    \r\n    <div class=\"w3-example\">\r\n        <pre><code>int day = 4;\r\nswitch (day) {\r\n  case 1:\r\n    System.out.println(\"Monday\");\r\n    break;\r\n  case 2:\r\n    System.out.println(\"Tuesday\");\r\n    break;\r\n  case 4:\r\n    System.out.println(\"Thursday\");\r\n    break;\r\n  default:\r\n    System.out.println(\"Looking forward to the Weekend\");\r\n}</code></pre>\r\n    </div>\r\n\r\n    <hr class=\"my-8 border-gray-100\">\r\n\r\n    <h2>The break and default Keywords</h2>\r\n    <ul>\r\n        <li><b>break:</b> Tells Java to stop executing the switch once a match is found and the code is run. Without it, the next case will execute automatically (fall-through).</li>\r\n        <li><b>default:</b> Specifies code to run if there is no case match.</li>\r\n    </ul>\r\n</div>', NULL, NULL, 'Topic: Java Switch statement. Key Concepts: Multi-way branching. case labels. The critical role of break. The default keyword as a catch-all. Quiz Focus: Identifying what happens when break is omitted, the purpose of default, and which data types work with switch (int, char, String).', 10);
INSERT INTO `topics` (`topic_id`, `language_id`, `title`, `content_description`, `pdf_file_path`, `external_link`, `ai_context_summary`, `order_sequence`) VALUES
(103, 5, 'Java While Loop', '<div class=\"topic-content-area\">\r\n    <h2>The while Loop</h2>\r\n    <p>Loops can execute a block of code as long as a specified condition is <b>true</b>. Loops are handy because they save time, reduce errors, and they make code more readable.</p>\r\n    \r\n    <div class=\"w3-example\">\r\n        <h3>Syntax</h3>\r\n        <pre><code>while (condition) {\r\n  // code block to be executed\r\n}</code></pre>\r\n    </div>\r\n\r\n    <div class=\"w3-example bg-gray-50\">\r\n        <pre><code>int i = 0;\r\nwhile (i < 5) {\r\n  System.out.println(i);\r\n  i++;\r\n}</code></pre>\r\n    </div>\r\n\r\n    <p class=\"text-red-600 font-bold\">Important Note:</p>\r\n    <p>Do not forget to increase the variable used in the condition (<code>i++</code>), otherwise the loop will never end. This is called an <b>infinite loop</b>.</p>\r\n\r\n    <hr class=\"my-8 border-gray-100\">\r\n\r\n    <h2>The do/while Loop</h2>\r\n    <p>The <code>do/while</code> loop is a variant of the while loop. This loop will execute the code block <b>once</b>, before checking if the condition is true, then it will repeat the loop as long as the condition is true.</p>\r\n    \r\n    <div class=\"w3-example\">\r\n        <pre><code>int i = 0;\r\ndo {\r\n  System.out.println(i);\r\n  i++;\r\n}\r\nwhile (i < 5);</code></pre>\r\n    </div>\r\n</div>', NULL, NULL, 'Topic: Java While and Do-While Loops. Key Concepts: Conditional iteration. The while loop checks the condition before executing. The do-while loop executes the body at least once because the condition is checked at the end. The necessity of a loop counter update (increment/decrement). Quiz Focus: Differentiating between while and do-while behavior, identifying the result of specific loop conditions, and recognizing infinite loops.', 11),
(104, 5, 'Java For Loop', '<div class=\"topic-content-area\">\r\n    <h2>The for Loop</h2>\r\n    <p>When you know exactly how many times you want to loop through a block of code, use the <code>for</code> loop instead of a <code>while</code> loop.</p>\r\n    \r\n    <div class=\"w3-example\">\r\n        <h3>Syntax</h3>\r\n        <pre><code>for (statement 1; statement 2; statement 3) {\r\n  // code block to be executed\r\n}</code></pre>\r\n    </div>\r\n\r\n    <ul>\r\n        <li><b>Statement 1:</b> Executed (one time) before the execution of the code block.</li>\r\n        <li><b>Statement 2:</b> Defines the condition for executing the code block.</li>\r\n        <li><b>Statement 3:</b> Executed (every time) after the code block has been executed.</li>\r\n    </ul>\r\n\r\n    <div class=\"w3-example\">\r\n        <pre><code>for (int i = 0; i < 5; i++) {\r\n  System.out.println(i);\r\n}</code></pre>\r\n    </div>\r\n\r\n    <hr class=\"my-8 border-gray-100\">\r\n\r\n    <h2>Nested Loops</h2>\r\n    <p>It is also possible to place a loop inside another loop. This is called a <b>nested loop</b>. The \"inner loop\" will be executed one time for each iteration of the \"outer loop\".</p>\r\n</div>', NULL, NULL, 'Topic: Java For Loops and Nesting. Key Concepts: Structure of a for loop (initialization; condition; update). Control flow of nested loops. Using for loops for counting. Quiz Focus: Identifying the three parts of the for statement, predicting the output of nested loops, and determining the total number of iterations.', 12),
(105, 5, 'Java For-Each Loop', '<div class=\"topic-content-area\">\r\n    <h2>The For-Each Loop</h2>\r\n    <p>There is also a \"for-each\" loop, which is used exclusively to loop through elements in an <b>array</b> (or other collections).</p>\r\n    \r\n    <div class=\"w3-example\">\r\n        <h3>Syntax</h3>\r\n        <pre><code>for (type variableName : arrayName) {\r\n  // code block to be executed\r\n}</code></pre>\r\n    </div>\r\n\r\n    <div class=\"w3-example\">\r\n        <h3>Example</h3>\r\n        <pre><code>String[] cars = {\"Volvo\", \"BMW\", \"Ford\", \"Mazda\"};\r\nfor (String i : cars) {\r\n  System.out.println(i);\r\n}</code></pre>\r\n    </div>\r\n\r\n    <p class=\"bg-blue-50 p-4 border-l-4 border-blue-500 rounded-r-lg mt-6\">\r\n        <b>Tip:</b> The for-each loop is easier to write and more readable when you don\'t need the index (position) of the elements.\r\n    </p>\r\n</div>', NULL, NULL, 'Topic: Java For-Each (Enhanced For) Loop. Key Concepts: Iterating through arrays and collections without using a counter/index. Syntax: for (DataType item : collection). Readability advantages. Quiz Focus: Correct syntax for the for-each loop, predicting output from array iteration, and identifying when to use for-each vs. a traditional for loop.', 13),
(106, 5, 'Java Break and Continue', '<div class=\"topic-content-area\">\r\n    <h2>Break and Continue</h2>\r\n    <p>Java provides two keywords that allow you to change the flow of a loop: <code>break</code> and <code>continue</code>.</p>\r\n    \r\n    <h3>The break Statement</h3>\r\n    <p>The <code>break</code> statement can be used to jump out of a loop entirely.</p>\r\n    <div class=\"w3-example\">\r\n        <pre><code>for (int i = 0; i < 10; i++) {\r\n  if (i == 4) {\r\n    break;\r\n  }\r\n  System.out.println(i);\r\n} // Loop stops at 3</code></pre>\r\n    </div>\r\n\r\n    <hr class=\"my-8 border-gray-100\">\r\n\r\n    <h3>The continue Statement</h3>\r\n    <p>The <code>continue</code> statement breaks one iteration (in the loop), if a specified condition occurs, and continues with the next iteration in the loop.</p>\r\n    <div class=\"w3-example\">\r\n        <pre><code>for (int i = 0; i < 10; i++) {\r\n  if (i == 4) {\r\n    continue;\r\n  }\r\n  System.out.println(i);\r\n} // Skips 4 and continues to 5</code></pre>\r\n    </div>\r\n</div>', NULL, NULL, 'Topic: Java Loop Control (Break and Continue). Key Concepts: Terminating a loop immediately with break. Skipping only the current iteration with continue. Using these keywords inside conditional if statements within loops. Quiz Focus: Predicting the output of loops containing break or continue, and distinguishing between stopping a loop vs. skipping an element.', 14),
(107, 5, 'Java Arrays', '<div class=\"topic-content-area\">\r\n    <h2>Java Arrays</h2>\r\n    <p>Arrays are used to store multiple values in a single variable, instead of declaring separate variables for each value. To declare an array, define the variable type with <b>square brackets</b>.</p>\r\n    \r\n    <div class=\"w3-example\">\r\n        <pre><code>String[] cars = {\"Volvo\", \"BMW\", \"Ford\", \"Mazda\"};\r\nint[] myNum = {10, 20, 30, 40};</code></pre>\r\n    </div>\r\n\r\n    <hr class=\"my-8 border-gray-100\">\r\n\r\n    <h2>Accessing Elements</h2>\r\n    <p>You access an array element by referring to the index number. Array indices start at <b>0</b>.</p>\r\n    <div class=\"w3-example\">\r\n        <pre><code>String[] cars = {\"Volvo\", \"BMW\", \"Ford\"};\r\nSystem.out.println(cars[0]); // Outputs Volvo\r\n\r\ncars[0] = \"Opel\"; // Changes Volvo to Opel</code></pre>\r\n    </div>\r\n\r\n    <h3>Array Length</h3>\r\n    <p>To find out how many elements an array has, use the <code>length</code> property:</p>\r\n    <pre><code>System.out.println(cars.length);</code></pre>\r\n</div>', NULL, NULL, 'Topic: Java Arrays Basics. Key Concepts: Fixed-size collection of elements of the same type. Zero-based indexing. Accessing and modifying elements via array[index]. Using the .length property (not a method for arrays). Quiz Focus: Identifying array indices, modifying array values, and using the length property.', 15),
(108, 5, 'Java Methods', '<div class=\"topic-content-area\">\r\n    <h2>Creating and Calling Methods</h2>\r\n    <p>A <b>method</b> is a block of code which only runs when it is called. Methods are used to perform certain actions, and they are also known as <b>functions</b>. They help in reusing code: define the code once, and use it many times.</p>\r\n    \r\n    <div class=\"w3-example\">\r\n        <h3>Example: Defining a Method</h3>\r\n        <p>A method must be declared within a class. It is defined with the name of the method, followed by parentheses <b>()</b>.</p>\r\n        <pre><code>public class Main {\r\n  static void myMethod() {\r\n    System.out.println(\"I just got executed!\");\r\n  }\r\n\r\n  public static void main(String[] args) {\r\n    myMethod(); // Calling the method\r\n  }\r\n}</code></pre>\r\n    </div>\r\n\r\n    <hr class=\"my-8 border-gray-100\">\r\n\r\n    <h2>Method Structure</h2>\r\n    <ul>\r\n        <li><b>static:</b> Means that the method belongs to the Main class and not an object of the Main class.</li>\r\n        <li><b>void:</b> Means that this method does not have a return value.</li>\r\n        <li><b>Main Method:</b> You have been using the <code>main</code> method since the start; it is the primary entry point for all Java programs.</li>\r\n    </ul>\r\n</div>', NULL, NULL, 'Topic: Java Methods Basics. Key Concepts: Method definition vs. invocation. The static keyword (class-level methods). The void keyword (no return value). Code modularity and reusability. Quiz Focus: Identifying how to call a method, understanding the purpose of static, and recognizing the difference between a method\'s definition and its execution.', 16),
(109, 5, 'Java Method Parameters', '<div class=\"topic-content-area\">\r\n    <h2>Parameters and Arguments</h2>\r\n    <p>Information can be passed to methods as <b>parameters</b>. Parameters act as variables inside the method. They are specified after the method name, inside the parentheses.</p>\r\n    \r\n    <div class=\"w3-example\">\r\n        <pre><code>static void myMethod(String fname) {\r\n  System.out.println(fname + \" Doe\");\r\n}\r\n\r\npublic static void main(String[] args) {\r\n  myMethod(\"Liam\"); // \"Liam\" is the argument\r\n  myMethod(\"Jenny\");\r\n}</code></pre>\r\n    </div>\r\n\r\n    <hr class=\"my-8 border-gray-100\">\r\n\r\n    <h2>Return Values</h2>\r\n    <p>If you want the method to return a value, you can use a primitive type (like <code>int</code>, <code>char</code>, etc.) instead of <code>void</code>, and use the <b>return</b> keyword inside the method.</p>\r\n\r\n    <div class=\"w3-example\">\r\n        <pre><code>static int sum(int x, int y) {\r\n  return x + y;\r\n}\r\n\r\npublic static void main(String[] args) {\r\n  int result = sum(5, 3);\r\n  System.out.println(result); // Outputs 8\r\n}</code></pre>\r\n    </div>\r\n</div>', NULL, NULL, 'Topic: Java Parameters and Return Statements. Key Concepts: Passing data via arguments. Defining parameters in method signatures. Using the return keyword to send data back to the caller. Replacing void with a data type for returning values. Quiz Focus: Distinguishing between parameters and arguments, predicting method output based on return values, and identifying correct method signatures.', 17),
(110, 5, 'Java Method Overloading', '<div class=\"topic-content-area\">\r\n    <h2>Method Overloading</h2>\r\n    <p>With <b>method overloading</b>, multiple methods can have the same name with different parameters.</p>\r\n    \r\n    <div class=\"w3-example\">\r\n        <h3>Example: Different Parameter Types</h3>\r\n        <pre><code>static int plusMethod(int x, int y) {\r\n  return x + y;\r\n}\r\n\r\nstatic double plusMethod(double x, double y) {\r\n  return x + y;\r\n}\r\n\r\npublic static void main(String[] args) {\r\n  int myNum1 = plusMethod(8, 5);\r\n  double myNum2 = plusMethod(4.3, 6.26);\r\n  System.out.println(\"int: \" + myNum1);\r\n  System.out.println(\"double: \" + myNum2);\r\n}</code></pre>\r\n    </div>\r\n\r\n    <p class=\"bg-blue-50 p-4 border-l-4 border-blue-500 rounded-r-lg mt-6 text-sm\">\r\n        <b>Why overload?</b> Instead of defining two methods that should do the same thing (like <code>plusMethodInt</code> and <code>plusMethodDouble</code>), we overload one name to handle different data types.\r\n    </p>\r\n</div>', NULL, NULL, 'Topic: Java Method Overloading. Key Concepts: Multiple methods with the same name. Uniqueness determined by parameter lists (type, number, or both). Improved code readability and consistency. Quiz Focus: Identifying valid method overloads, understanding how the compiler distinguishes between them, and recognizing that return types alone cannot be used to overload a method.', 18),
(111, 5, 'Java Scope', '<div class=\"topic-content-area\">\r\n    <h2>Understanding Scope</h2>\r\n    <p>In Java, variables are only accessible inside the region they are created. This is called <b>scope</b>.</p>\r\n    \r\n    <h3>Method Scope</h3>\r\n    <p>Variables declared directly inside a method are available only within that method, between the curly braces <code>{}</code> where they were defined.</p>\r\n\r\n    <div class=\"w3-example\">\r\n        <pre><code>public static void main(String[] args) {\r\n  int x = 100; // x is in Method Scope\r\n  System.out.println(x);\r\n}\r\n// x cannot be used here outside of main</code></pre>\r\n    </div>\r\n\r\n    <hr class=\"my-8 border-gray-100\">\r\n\r\n    <h3>Block Scope</h3>\r\n    <p>A block of code refers to all of the code between curly braces <code>{}</code>. Variables declared inside a block (like an <code>if</code> or <code>for</code> loop) are only accessible within that specific block.</p>\r\n    \r\n    <div class=\"w3-example\">\r\n        <pre><code>if (true) {\r\n  int y = 50; // Block scope\r\n  System.out.println(y);\r\n}\r\n// y is not accessible here!</code></pre>\r\n    </div>\r\n</div>', NULL, NULL, 'Topic: Java Variable Scope. Key Concepts: Method scope vs. Block scope. Visibility of variables. Lifetime of a variable within curly braces. Quiz Focus: Determining if a variable is accessible at a certain point in a program, identifying errors related to \"cannot find symbol,\" and understanding nested scopes.', 19),
(112, 5, 'Java OOP', '<div class=\"topic-content-area\">\r\n    <h2>Object-Oriented Programming (OOP)</h2>\r\n    <p>Java is an OOP language. OOP stands for Object-Oriented Programming. While procedural programming is about writing procedures or methods that perform operations on data, OOP is about creating <b>objects</b> that contain both data and methods.</p>\r\n    \r\n    <p>A <b>Class</b> is a template for objects, and an <b>Object</b> is an instance of a class.</p>\r\n\r\n    \r\n\r\n    <div class=\"w3-example\">\r\n        <h3>Creating a Class and Object</h3>\r\n        <pre><code>public class Main {\r\n  int x = 5; // Class attribute\r\n\r\n  public static void main(String[] args) {\r\n    Main myObj = new Main(); // Creating an object\r\n    System.out.println(myObj.x); // Accessing attribute\r\n  }\r\n}</code></pre>\r\n    </div>\r\n\r\n    <p>To create an object of a class, we use the <b>new</b> keyword followed by the class name.</p>\r\n</div>', NULL, NULL, 'Topic: Java Classes and Objects. Key Concepts: The difference between procedural and OOP. Class as a blueprint/template. Object as a specific instance. Using the new keyword for instantiation. Accessing class attributes using the dot (.) operator. Quiz Focus: Defining the relationship between a class and an object, identifying the syntax for object creation, and understanding the basic structure of a Java object.', 20),
(113, 6, 'Introduction to Dart', '<div class=\"topic-content-area\">\r\n    <h2>What is Dart?</h2>\r\n    <p>Dart is an open-source, general-purpose, object-oriented programming language developed by <b>Google</b>. It is designed for building fast, high-performance apps for mobile, desktop, backend, and web.</p>\r\n    \r\n    <h3>Why Use Dart?</h3>\r\n    <ul>\r\n        <li><b>Optimized for UI:</b> It’s built to handle the needs of modern user interfaces.</li>\r\n        <li><b>Hot Reload:</b> Used in Flutter to see code changes instantly without losing app state.</li>\r\n        <li><b>Compiled Language:</b> Dart can be compiled to machine code (ARM/x64) or JavaScript.</li>\r\n        <li><b>Null Safety:</b> Modern versions of Dart help prevent \"null pointer\" crashes before you even run your code.</li>\r\n    </ul>\r\n\r\n    <hr class=\"my-8 border-gray-100\">\r\n\r\n    <h2>Your First Dart Program</h2>\r\n    <p>In Dart, execution starts with the <code>main()</code> function. Every app must have one.</p>\r\n\r\n    <div class=\"w3-example\">\r\n        <pre><code>void main() {\r\n  print(\"Hello, Dart!\");\r\n}</code></pre>\r\n    </div>\r\n\r\n    <h3>Breakdown:</h3>\r\n    <ul>\r\n        <li><b>void:</b> This means the function doesn\'t return any value.</li>\r\n        <li><b>main():</b> The entry point where the program begins.</li>\r\n        <li><b>print():</b> Displays the text on the console.</li>\r\n    </ul>\r\n    <p class=\"text-red-600 font-bold italic\">Rule: Every Dart statement must end with a semicolon (;).</p>\r\n</div>', NULL, NULL, 'Topic: Introduction to Dart. Key Concepts: Google-developed, UI-optimized language. Entry point is the main() function. Use of print() for output. Requirement of semicolons. Compilation to machine code or JS. Quiz Focus: Identifying the entry point function, the company that developed Dart, and the role of the main() function in program execution.', 1),
(114, 6, 'Dart Variables', '<div class=\"topic-content-area\">\r\n    <h2>Variables and Declaration</h2>\r\n    <p>A variable is a container for storing data. Dart supports <b>Type Inference</b>, meaning it can often guess the type of data you are using based on the value provided.</p>\r\n    \r\n    <div class=\"w3-example\">\r\n        <h3>Declaring Variables</h3>\r\n        <p>You can use the <code>var</code> keyword to let Dart infer the type, or you can specify the type explicitly.</p>\r\n        <pre><code>var name = \"John\"; // Inferred as String\r\nString city = \"Tokyo\"; // Explicitly defined\r\nint age = 25; // Explicitly defined</code></pre>\r\n    </div>\r\n\r\n    <hr class=\"my-8 border-gray-100\">\r\n\r\n    <h2>Constants: final vs. const</h2>\r\n    <p>If you don\'t want a variable to be changed, use <code>final</code> or <code>const</code>.</p>\r\n    <ul>\r\n        <li><b>final:</b> The value is set only once and is initialized when it is accessed (run-time constant).</li>\r\n        <li><b>const:</b> The value must be known at compile-time (compile-time constant).</li>\r\n    </ul>\r\n\r\n    <div class=\"w3-example bg-gray-50\">\r\n        <pre><code>final currentTime = DateTime.now(); // Works with final\r\n// const currentTime = DateTime.now(); // Error: const must be fixed at compile time</code></pre>\r\n    </div>\r\n</div>', NULL, NULL, 'Topic: Dart Variables and Constants. Key Concepts: Type inference using var. Explicit type declaration (String, int). Difference between final (runtime) and const (compile-time). Immutability of constants. Quiz Focus: Distinguishing between var and explicit types, identifying the difference between final and const, and understanding variable re-assignment rules.', 2),
(115, 6, 'Dart Data Types', '<div class=\"topic-content-area\">\r\n    <h2>Core Data Types</h2>\r\n    <p>Dart is a <b>statically typed</b> language, meaning every variable has a type. Here are the most common types:</p>\r\n    \r\n    <div class=\"w3-example\">\r\n        <ul>\r\n            <li><b>int:</b> Whole numbers (e.g., <code>10</code>, <code>-5</code>).</li>\r\n            <li><b>double:</b> Fractional numbers (e.g., <code>10.5</code>, <code>3.14</code>).</li>\r\n            <li><b>String:</b> Textual data (e.g., <code>\"Hello\"</code>).</li>\r\n            <li><b>bool:</b> Boolean values (<code>true</code> or <code>false</code>).</li>\r\n            <li><b>num:</b> A type that can be either <b>int</b> or <b>double</b>.</li>\r\n        </ul>\r\n    </div>\r\n\r\n    <hr class=\"my-8 border-gray-100\">\r\n\r\n    <h2>Lists, Sets, and Maps</h2>\r\n    <p>Dart also provides collection types to store groups of data:</p>\r\n    <ul>\r\n        <li><b>List:</b> An ordered group of items (Arrays). <code>[1, 2, 3]</code></li>\r\n        <li><b>Set:</b> An unordered collection of unique items. <code>{\'a\', \'b\'}</code></li>\r\n        <li><b>Map:</b> Key-value pairs. <code>{\'name\': \'Aero\', \'age\': 20}</code></li>\r\n    </ul>\r\n\r\n    <div class=\"bg-blue-50 p-4 border-l-4 border-blue-500 rounded-r-lg mt-6\">\r\n        <p class=\"text-sm\"><b>Tip:</b> You can use the <code>runtimeType</code> property to check the type of a variable at any time.</p>\r\n    </div>\r\n</div>', NULL, NULL, 'Topic: Dart Core Data Types. Key Concepts: Numeric types (int, double, num). Strings and Booleans. Basic introduction to collections (List, Set, Map). Statically typed nature. Quiz Focus: Identifying the correct type for decimals vs. whole numbers, recognizing the num type, and basic structure of Lists and Maps.', 3),
(116, 6, 'Dart String Manipulation', '<div class=\"topic-content-area\">\r\n    <h2>Strings and Interpolation</h2>\r\n    <p>Strings in Dart can be created using either single or double quotes. Dart makes it very easy to include variables inside strings.</p>\r\n    \r\n    <h3>String Interpolation</h3>\r\n    <p>Use the <code>$</code> symbol to include a variable value, or <code>${expression}</code> for more complex logic inside a string.</p>\r\n    <div class=\"w3-example\">\r\n        <pre><code>String name = \"Dart\";\r\nprint(\"Learning $name is fun!\");\r\nprint(\"The name has ${name.length} letters.\");</code></pre>\r\n    </div>\r\n\r\n    <hr class=\"my-8 border-gray-100\">\r\n\r\n    <h2>Multiline and Raw Strings</h2>\r\n    <ul>\r\n        <li><b>Multiline:</b> Use triple quotes (<code>\'\'\'</code> or <code>\"\"\"</code>) to create text that spans multiple lines.</li>\r\n        <li><b>Raw Strings:</b> Use the <code>r</code> prefix to ignore special characters like <code>\\n</code>.</li>\r\n    </ul>\r\n\r\n    <div class=\"w3-example bg-gray-100\">\r\n        <pre><code>String raw = r\"This is a \\n raw string\";\r\nprint(raw); // Prints the \\n instead of a new line</code></pre>\r\n    </div>\r\n</div>', NULL, NULL, 'Topic: Dart Strings and Formatting. Key Concepts: Single/Double quotes. String interpolation using $ and ${}. Multiline strings via triple quotes. Raw strings using r prefix. Quiz Focus: Correct interpolation syntax, identifying raw string behavior, and finding string properties like length.', 4),
(117, 6, 'Dart Basic Operators', '<div class=\"topic-content-area\">\r\n    <h2>Operators in Dart</h2>\r\n    <p>Operators are used to perform operations on variables and values. Dart includes all standard math and logic operators.</p>\r\n    \r\n    <h3>Arithmetic Operators</h3>\r\n    <div class=\"w3-example\">\r\n        <ul>\r\n            <li><code>+</code>, <code>-</code>, <code>*</code>, <code>/</code>: Basic math.</li>\r\n            <li><code>~/</code>: <b>Integer Division</b> (Returns only the whole number).</li>\r\n            <li><code>%</code>: Modulus (Returns the remainder).</li>\r\n        </ul>\r\n    </div>\r\n\r\n    <hr class=\"my-8 border-gray-100\">\r\n\r\n    <h2>Type Test Operators</h2>\r\n    <p>These allow you to check types at runtime:</p>\r\n    <ul>\r\n        <li><b>as:</b> Typecast (treating an object as a specific type).</li>\r\n        <li><b>is:</b> True if the object has the specified type.</li>\r\n        <li><b>is!:</b> True if the object does NOT have the specified type.</li>\r\n    </ul>\r\n\r\n    <div class=\"w3-example bg-blue-50\">\r\n        <pre><code>var value = 10;\r\nprint(value is int); // true</code></pre>\r\n    </div>\r\n</div>', NULL, NULL, 'Topic: Dart Operators. Key Concepts: Standard arithmetic. Integer division (~/). Modulus (%). Type test operators (is, is!). Assignment and Logical operators. Quiz Focus: Calculating integer division vs. standard division, using is to check types, and basic modulus arithmetic.', 5),
(118, 6, 'Dart User Input', '<div class=\"topic-content-area\">\r\n    <h2>Getting User Input</h2>\r\n    <p>To capture input from the user via the console, Dart uses the <code>stdin.readLineSync()</code> method. This requires importing the <code>dart:io</code> library.</p>\r\n    \r\n    <div class=\"w3-example\">\r\n        <h3>Example: Reading a String</h3>\r\n        <pre><code>import \'dart:io\';\r\n\r\nvoid main() {\r\n  print(\"Enter your name:\");\r\n  String? name = stdin.readLineSync(); // Captures input\r\n  print(\"Hello, $name!\");\r\n}</code></pre>\r\n    </div>\r\n\r\n    <hr class=\"my-8 border-gray-100\">\r\n\r\n    <h2>Converting Input to Numbers</h2>\r\n    <p>Because <code>stdin.readLineSync()</code> always returns a <b>String</b>, you must parse the value if you need to perform calculations.</p>\r\n\r\n    <div class=\"w3-example bg-yellow-50\">\r\n        <pre><code>import \'dart:io\';\r\n\r\nvoid main() {\r\n  print(\"Enter a number:\");\r\n  int num = int.parse(stdin.readLineSync()!); // Converting String to Int\r\n  print(\"The square of the number is ${num * num}\");\r\n}</code></pre>\r\n    </div>\r\n\r\n    <p class=\"text-sm\"><b>Note:</b> The <code>!</code> and <code>?</code> symbols are part of Dart\'s <b>Null Safety</b> feature, ensuring the program doesn\'t crash if the user provides no input.</p>\r\n</div>', NULL, NULL, 'Topic: User Input in Dart. Key Concepts: Importing dart:io. Using stdin.readLineSync() for console input. The necessity of int.parse() or double.parse() for numeric data. Basic introduction to null safety symbols (!). Quiz Focus: Identifying the library needed for input, the return type of readLineSync, and the method used to convert text to integers.', 6),
(119, 6, 'Dart Conditional Statements', '<div class=\"topic-content-area\">\r\n    <h2>Decision Making</h2>\r\n    <p>Conditions allow your program to execute different blocks of code based on whether a statement is true or false. Dart uses standard <code>if</code>, <code>else if</code>, and <code>else</code> syntax.</p>\r\n    \r\n    <div class=\"w3-example\">\r\n        <pre><code>int age = 18;\r\n\r\nif (age >= 18) {\r\n  print(\"You are a voter.\");\r\n} else {\r\n  print(\"You are too young to vote.\");\r\n}</code></pre>\r\n    </div>\r\n\r\n    \r\n\r\n    <hr class=\"my-8 border-gray-100\">\r\n\r\n    <h2>Ternary Operator</h2>\r\n    <p>Dart also supports a short-hand version of If-Else called the <b>Ternary Operator</b>. It is useful for assigning values based on a condition in a single line.</p>\r\n    \r\n    <div class=\"w3-example bg-blue-50\">\r\n        <code>condition ? expression_if_true : expression_if_false;</code>\r\n        <pre><code>int age = 20;\r\nString status = (age >= 18) ? \"Adult\" : \"Minor\";\r\nprint(status);</code></pre>\r\n    </div>\r\n</div>', NULL, NULL, 'Topic: Dart Conditional Logic. Key Concepts: if, else if, and else blocks. Comparison operators (==, !=, >=, <=). Short-hand Ternary operator syntax. Quiz Focus: Predicting which code block will execute, correctly formatting ternary expressions, and identifying comparison vs assignment operators.', 7),
(120, 6, 'Dart Switch Case', '<div class=\"topic-content-area\">\r\n    <h2>The Switch Statement</h2>\r\n    <p>When you have many possible conditions for a single variable, <code>switch</code> is often cleaner and more efficient than long \"if-else if\" chains.</p>\r\n    \r\n    <div class=\"w3-example\">\r\n        <pre><code>String weather = \"sunny\";\r\n\r\nswitch (weather) {\r\n  case \"sunny\":\r\n    print(\"Wear sunglasses.\");\r\n    break;\r\n  case \"rainy\":\r\n    print(\"Bring an umbrella.\");\r\n    break;\r\n  case \"snowy\":\r\n    print(\"Wear a coat.\");\r\n    break;\r\n  default:\r\n    print(\"Check the forecast again.\");\r\n}</code></pre>\r\n    </div>\r\n\r\n    <hr class=\"my-8 border-gray-100\">\r\n\r\n    <h2>Key Rules:</h2>\r\n    <ul>\r\n        <li><b>case:</b> Represents a specific value to match.</li>\r\n        <li><b>break:</b> Essential to stop execution once a match is found.</li>\r\n        <li><b>default:</b> The fallback block that runs if no cases match.</li>\r\n    </ul>\r\n</div>', NULL, NULL, 'Topic: Dart Switch Statement. Key Concepts: Multi-way branching. The case keyword. Using break to exit the switch. The default case. Quiz Focus: Identifying the purpose of the break statement, predicting the output when no match is found, and recognizing valid switch syntax.', 8),
(121, 6, 'Dart Loops', '<div class=\"topic-content-area\">\r\n    <h2>The For Loop</h2>\r\n    <p>Loops are used to repeat a block of code. The <code>for</code> loop is ideal when you know how many times the code should run.</p>\r\n    \r\n    <div class=\"w3-example\">\r\n        <pre><code>for (int i = 1; i <= 5; i++) {\r\n  print(\"This is iteration $i\");\r\n}</code></pre>\r\n    </div>\r\n\r\n    \r\n\r\n    <hr class=\"my-8 border-gray-100\">\r\n\r\n    <h2>For-Each Loop</h2>\r\n    <p>In Dart, the <code>forEach</code> loop is a great way to iterate through collections like Lists without needing an index counter.</p>\r\n\r\n    <div class=\"w3-example bg-green-50\">\r\n        <pre><code>List&lt;String&gt; players = [\"Ronaldo\", \"Messi\", \"Neymar\"];\r\n\r\nplayers.forEach((name) => print(name));</code></pre>\r\n    </div>\r\n</div>', NULL, NULL, 'Topic: Dart For and For-Each Loops. Key Concepts: Standard for loop syntax (initialization, condition, increment). Using forEach with lambda/arrow functions for Lists. Quiz Focus: Determining the number of times a loop will run, correctly identifying for-each syntax, and using loop counters.', 9),
(122, 6, 'Dart Do-While Loops', '<div class=\"topic-content-area\">\r\n    <h2>Conditional Loops</h2>\r\n    <p>Sometimes you don\'t know how many times a loop should run in advance. <b>While</b> loops run as long as a condition remains true.</p>\r\n    \r\n    <div class=\"w3-example\">\r\n        <h3>While Loop</h3>\r\n        <p>Checks the condition <b>before</b> running the code.</p>\r\n        <pre><code>int i = 1;\r\nwhile (i <= 5) {\r\n  print(i);\r\n  i++;\r\n}</code></pre>\r\n    </div>\r\n\r\n    <hr class=\"my-8 border-gray-100\">\r\n\r\n    <h3>Do-While Loop</h3>\r\n    <p>Executes the code block <b>once</b> before checking the condition. This ensures the code runs at least one time.</p>\r\n    <div class=\"w3-example bg-gray-50\">\r\n        <pre><code>int i = 1;\r\ndo {\r\n  print(i);\r\n  i++;\r\n} while (i <= 0); // Runs once even though condition is false</code></pre>\r\n    </div>\r\n</div>', NULL, NULL, 'Topic: Dart While and Do-While. Key Concepts: Difference between entry-controlled (while) and exit-controlled (do-while) loops. Use cases for each. Avoiding infinite loops. Quiz Focus: Predicting how many times a do-while loop runs vs a while loop given the same false condition, and identifying the correct loop syntax.', 10),
(123, 6, 'Dart Functions', '<div class=\"topic-content-area\">\r\n    <h2>Introduction to Functions</h2>\r\n    <p>Functions are the building blocks of readable, maintainable, and reusable code. A function is a block of code that performs a specific task and can be called whenever needed.</p>\r\n    \r\n    <div class=\"w3-example\">\r\n        <h3>Function Syntax</h3>\r\n        <pre><code>returnType functionName(parameters) {\r\n  // body of function\r\n  return value;\r\n}</code></pre>\r\n    </div>\r\n\r\n    <div class=\"w3-example bg-gray-50\">\r\n        <h3>Example: A Simple Function</h3>\r\n        <pre><code>void printName() {\r\n  print(\"My name is Dart.\");\r\n}\r\n\r\nvoid main() {\r\n  printName(); // Calling the function\r\n}</code></pre>\r\n    </div>\r\n\r\n    <hr class=\"my-8 border-gray-100\">\r\n\r\n    <h2>Why Use Functions?</h2>\r\n    <ul>\r\n        <li><b>Code Reusability:</b> Write once, use many times.</li>\r\n        <li><b>Modularity:</b> Break complex problems into smaller, manageable pieces.</li>\r\n        <li><b>Easy Testing:</b> Functions can be tested individually.</li>\r\n    </ul>\r\n</div>', NULL, NULL, 'Topic: Dart Functions. Key Concepts: Function declaration and invocation. The void keyword for non-returning functions. Return types. Code reusability and modularity. Quiz Focus: Identifying the parts of a function declaration, understanding how to call a function, and the purpose of the void keyword.', 11),
(124, 6, 'Dart Function Parameters', '<div class=\"topic-content-area\">\r\n    <h2>Passing Data to Functions</h2>\r\n    <p>Parameters allow you to pass data into a function so it can produce different results based on the input.</p>\r\n    \r\n    <div class=\"w3-example\">\r\n        <h3>Positional Parameters</h3>\r\n        <pre><code>void add(int num1, int num2) {\r\n  int sum = num1 + num2;\r\n  print(\"The sum is $sum\");\r\n}</code></pre>\r\n    </div>\r\n\r\n    <hr class=\"my-8 border-gray-100\">\r\n\r\n    <h2>Return Statement</h2>\r\n    <p>The <code>return</code> keyword is used to send a value back to the place where the function was called. Once a return statement is executed, the function stops.</p>\r\n\r\n    <div class=\"w3-example bg-blue-50\">\r\n        <h3>Example: Function with Return</h3>\r\n        <pre><code>int calculateArea(int length, int width) {\r\n  return length * width;\r\n}\r\n\r\nvoid main() {\r\n  int area = calculateArea(10, 5);\r\n  print(\"Area: $area\");\r\n}</code></pre>\r\n    </div>\r\n</div>', NULL, NULL, 'Topic: Dart Function Parameters and Returns. Key Concepts: Using parameters to pass data. Positional vs. Named parameters (basic). The return keyword and its effect on function execution. Specifying return types (int, String, etc.). Quiz Focus: Distinguishing between parameters and arguments, predicting the output of a function with a return value, and identifying correct return type usage.', 12),
(125, 6, 'Dart Arrow Functions', '<div class=\"topic-content-area\">\r\n    <h2>Arrow Functions (Short-hand)</h2>\r\n    <p>For functions that contain only <b>one line</b> of code, Dart provides a concise syntax using the \"fat arrow\" symbol: <code>=></code>.</p>\r\n    \r\n    <div class=\"w3-example\">\r\n        <h3>Standard Syntax</h3>\r\n        <pre><code>double calculateInterest(double p, double r, double t) {\r\n  return (p * r * t) / 100;\r\n}</code></pre>\r\n    </div>\r\n\r\n    <div class=\"w3-example bg-green-50\">\r\n        <h3>Arrow Syntax</h3>\r\n        <pre><code>double calculateInterest(double p, double r, double t) => (p * r * t) / 100;</code></pre>\r\n    </div>\r\n\r\n    <p class=\"mt-4\"><b>Note:</b> In arrow functions, the <code>return</code> keyword is omitted; the expression after the arrow is automatically returned.</p>\r\n</div>', NULL, NULL, 'Topic: Dart Arrow Functions. Key Concepts: Short-hand notation =>. Restrictions (only works for single-expression functions). Implicit return behavior. Quiz Focus: Identifying the correct arrow function syntax and recognizing when a standard function can be converted to an arrow function.', 13),
(126, 6, 'Dart Scope', '<div class=\"topic-content-area\">\r\n    <h2>Understanding Variable Scope</h2>\r\n    <p>Scope defines the \"lifetime\" and accessibility of a variable. In Dart, variables are only accessible within the block of code (defined by curly braces <code>{}</code>) where they are declared.</p>\r\n    \r\n    <h3>1. Global Scope</h3>\r\n    <p>Variables defined outside of any function can be accessed from anywhere in the file.</p>\r\n\r\n    <h3>2. Local Scope</h3>\r\n    <p>Variables defined inside a function or a loop are local to that block.</p>\r\n\r\n    <div class=\"w3-example\">\r\n        <pre><code>String globalVar = \"I am global\";\r\n\r\nvoid main() {\r\n  String localVar = \"I am local\";\r\n  print(globalVar); // Works\r\n  print(localVar);  // Works\r\n}\r\n\r\nvoid anotherFunction() {\r\n  print(globalVar); // Works\r\n  // print(localVar); // Error! localVar is not defined here.\r\n}</code></pre>\r\n    </div>\r\n</div>', NULL, NULL, 'Topic: Dart Variable Scope. Key Concepts: Lexical scoping. Difference between global and local variables. Accessibility of variables inside vs. outside curly braces. Quiz Focus: Determining if a variable is accessible at a specific line of code and identifying errors related to variable scope.', 14),
(127, 6, 'Dart Exception Handling', '<div class=\"topic-content-area\">\r\n    <h2>Handling Errors</h2>\r\n    <p>An <b>exception</b> is an error that happens at runtime. If not handled, the program will crash. Dart uses <code>try-catch</code> blocks to handle these situations gracefully.</p>\r\n    \r\n    <div class=\"w3-example\">\r\n        <h3>Try-Catch Syntax</h3>\r\n        <pre><code>try {\r\n  // Code that might cause an exception\r\n  int result = 10 ~/ 0; // Integer division by zero\r\n} catch (e) {\r\n  // Code to run if an exception occurs\r\n  print(\"An error occurred: $e\");\r\n}</code></pre>\r\n    </div>\r\n\r\n    <hr class=\"my-8 border-gray-100\">\r\n\r\n    <h2>The Finally Block</h2>\r\n    <p>The <code>finally</code> block always executes, whether an exception was thrown or not. It is usually used for cleanup tasks like closing files.</p>\r\n    <div class=\"w3-example bg-gray-50\">\r\n        <pre><code>finally {\r\n  print(\"This always runs.\");\r\n}</code></pre>\r\n    </div>\r\n</div>', NULL, NULL, 'Topic: Dart Exception Handling. Key Concepts: Using try to wrap risky code. Using catch to intercept errors. The finally block for guaranteed execution. Handling common errors like division by zero. Quiz Focus: Identifying the purpose of try, catch, and finally, and predicting program flow when an exception occurs.', 15),
(128, 6, 'Dart Lists', '<div class=\"topic-content-area\">\r\n    <h2>Understanding Lists</h2>\r\n    <p>In Dart, an <b>Array</b> is known as a <b>List</b>. It is an ordered group of objects. Lists are one of the most common ways to store and manage multiple values.</p>\r\n    \r\n    <div class=\"w3-example\">\r\n        <h3>Creating a List</h3>\r\n        <pre><code>List&lt;String&gt; names = [\"John\", \"James\", \"Peter\"];\r\nList&lt;int&gt; numbers = [10, 20, 30];</code></pre>\r\n    </div>\r\n\r\n    \r\n\r\n    <hr class=\"my-8 border-gray-100\">\r\n\r\n    <h2>Basic List Operations</h2>\r\n    <ul>\r\n        <li><b>Access:</b> Use index starting at 0: <code>names[0]</code>.</li>\r\n        <li><b>Length:</b> Use <code>names.length</code>.</li>\r\n        <li><b>Add:</b> Use <code>names.add(\"New Name\")</code>.</li>\r\n        <li><b>Remove:</b> Use <code>names.remove(\"John\")</code>.</li>\r\n    </ul>\r\n\r\n    <div class=\"w3-example bg-gray-50\">\r\n        <pre><code>List&lt;int&gt; scores = [85, 90];\r\nscores.add(95); // Adds to the end\r\nprint(scores[1]); // Prints 90</code></pre>\r\n    </div>\r\n</div>', NULL, NULL, 'Topic: Dart Lists (Arrays). Key Concepts: Lists as ordered collections. Generic typing <type>. Zero-based indexing. Adding and removing elements. Using the .length property. Quiz Focus: Accessing specific indices, calculating list length, and identify the correct method to add new elements to a list.', 16),
(129, 6, 'Dart Sets', '<div class=\"topic-content-area\">\r\n    <h2>The Unique Collection: Sets</h2>\r\n    <p>A <b>Set</b> is an unordered collection of <b>unique</b> items. Unlike a List, a Set cannot contain duplicate values. This is perfect for storing data like ID numbers or unique usernames.</p>\r\n    \r\n    <div class=\"w3-example\">\r\n        <h3>Creating a Set</h3>\r\n        <pre><code>Set&lt;String&gt; fruits = {\"Apple\", \"Orange\", \"Banana\"};\r\n// Duplicates are ignored\r\nSet&lt;int&gt; ids = {101, 102, 101}; // Contains only {101, 102}</code></pre>\r\n    </div>\r\n\r\n    <hr class=\"my-8 border-gray-100\">\r\n\r\n    <h2>Common Set Methods</h2>\r\n    <ul>\r\n        <li><b>contains():</b> Checks if an item exists in the set.</li>\r\n        <li><b>add():</b> Adds a new unique item.</li>\r\n        <li><b>remove():</b> Deletes an item from the set.</li>\r\n    </ul>\r\n\r\n    <div class=\"bg-blue-50 p-4 border-l-4 border-blue-500 rounded-r-lg mt-6\">\r\n        <p class=\"text-sm\"><b>Note:</b> Searching for an item in a Set is generally faster than searching in a List.</p>\r\n    </div>\r\n</div>', NULL, NULL, 'Topic: Dart Sets. Key Concepts: Unordered collections. Unique items only (no duplicates allowed). Using curly braces {}. Method contains() for membership testing. Quiz Focus: Predicting the content of a Set after adding a duplicate, identifying the syntax for Sets, and understanding the difference between a Set and a List.', 17),
(130, 6, 'Dart Maps', '<div class=\"topic-content-area\">\r\n    <h2>Working with Maps</h2>\r\n    <p>A <b>Map</b> is an object that associates <b>keys</b> and <b>values</b>. Both keys and values can be any type of object. Each key must be unique, but you can use the same value multiple times.</p>\r\n    \r\n    <div class=\"w3-example\">\r\n        <h3>Creating a Map</h3>\r\n        <pre><code>Map&lt;String, String&gt; capitals = {\r\n  \"USA\": \"Washington D.C.\",\r\n  \"India\": \"New Delhi\",\r\n  \"Japan\": \"Tokyo\"\r\n};</code></pre>\r\n    </div>\r\n\r\n    \r\n\r\n    <hr class=\"my-8 border-gray-100\">\r\n\r\n    <h2>Accessing Map Data</h2>\r\n    <p>You access values by providing the key inside square brackets.</p>\r\n    <div class=\"w3-example bg-green-50\">\r\n        <pre><code>print(capitals[\"Japan\"]); // Outputs: Tokyo\r\n\r\n// Adding a new entry\r\ncapitals[\"Germany\"] = \"Berlin\";</code></pre>\r\n    </div>\r\n</div>', NULL, NULL, 'Topic: Dart Maps. Key Concepts: Key-Value association. Unique keys. Accessing values via keys. Map declaration using {key: value}. Quiz Focus: Retrieving a value using a key, identifying valid Map syntax, and understanding that keys must be unique.', 18),
(131, 6, 'Dart Null Safety', '<div class=\"topic-content-area\">\r\n    <h2>What is Null Safety?</h2>\r\n    <p>Null Safety is a feature that helps you avoid \"null pointer exceptions\"—errors that happen when you try to use a variable that doesn\'t have a value (is <code>null</code>).</p>\r\n    \r\n    <h3>Non-Nullable Types (Default)</h3>\r\n    <p>In modern Dart, variables cannot be null by default.</p>\r\n    <div class=\"w3-example\">\r\n        <pre><code>int age = 25;\r\n// age = null; // This will cause a compile error!</code></pre>\r\n    </div>\r\n\r\n    <hr class=\"my-8 border-gray-100\">\r\n\r\n    <h2>Nullable Types (?)</h2>\r\n    <p>If you want a variable to allow a null value, you must add a <b>question mark (?)</b> after the type name.</p>\r\n    <div class=\"w3-example bg-blue-50\">\r\n        <pre><code>String? name; // name is null by default\r\nname = \"Aero\";\r\nname = null; // This is now allowed!</code></pre>\r\n    </div>\r\n</div>', NULL, NULL, 'Topic: Dart Null Safety. Key Concepts: Default non-nullable behavior. Using the ? operator for nullable variables. Preventing runtime crashes through compile-time checks. Quiz Focus: Identifying how to declare a nullable variable, identifying why code fails under null safety, and recognizing the ? symbol\'s purpose.', 19),
(132, 6, 'Dart Null Safety Operators', '<div class=\"topic-content-area\">\r\n    <h2>Handling Null Values</h2>\r\n    <p>When working with nullable variables, Dart provides special operators to safely handle potential null values.</p>\r\n    \r\n    <h3>1. The Null Assertion Operator (!)</h3>\r\n    <p>Use the <code>!</code> operator if you are <b>100% sure</b> a variable is not null, even if it is a nullable type. Use with caution!</p>\r\n\r\n    <h3>2. The Null-Coalescing Operator (??)</h3>\r\n    <p>The <code>??</code> operator provides a <b>default value</b> if the variable on the left is null.</p>\r\n\r\n    <div class=\"w3-example\">\r\n        <pre><code>String? user;\r\nString displayName = user ?? \"Guest\"; // If user is null, use \"Guest\"\r\nprint(displayName); // Outputs: Guest</code></pre>\r\n    </div>\r\n</div>', NULL, NULL, 'Topic: Null Safety Operators. Key Concepts: The Bang operator (!) for force-unwrapping. The Null-coalescing operator (??) for default values. Improving code safety and readability. Quiz Focus: Predicting the result of a ?? operation, identifying the risk of the ! operator, and choosing the right operator for safety.', 20);

-- --------------------------------------------------------

--
-- Table structure for table `users`
--

CREATE TABLE `users` (
  `user_id` int(11) NOT NULL,
  `fullname` varchar(100) NOT NULL,
  `email` varchar(100) NOT NULL,
  `password_hash` varchar(255) NOT NULL,
  `role` enum('student','admin') DEFAULT 'student',
  `profile_image` varchar(255) DEFAULT NULL,
  `profile_banner` varchar(255) DEFAULT NULL,
  `date_registered` datetime DEFAULT current_timestamp(),
  `bio` text DEFAULT NULL,
  `phone` varchar(20) DEFAULT NULL,
  `location` varchar(100) DEFAULT NULL,
  `gender` varchar(20) DEFAULT NULL,
  `skills` text DEFAULT NULL,
  `is_verified` tinyint(1) DEFAULT 0,
  `verification_code` varchar(6) DEFAULT NULL
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_general_ci;

--
-- Dumping data for table `users`
--

INSERT INTO `users` (`user_id`, `fullname`, `email`, `password_hash`, `role`, `profile_image`, `profile_banner`, `date_registered`, `bio`, `phone`, `location`, `gender`, `skills`, `is_verified`, `verification_code`) VALUES
(6, 'Admin', 'admin@gmail.com', '$2y$10$wGdVXLOIcfyd.qIX5ybxru.sJJ0sgXUaAEhyZhISD4QaOM7n/8vNC', 'admin', NULL, NULL, '2026-01-03 16:16:33', NULL, NULL, NULL, NULL, NULL, 0, NULL);

-- --------------------------------------------------------

--
-- Table structure for table `user_progress`
--

CREATE TABLE `user_progress` (
  `progress_id` int(11) NOT NULL,
  `user_id` int(11) NOT NULL,
  `topic_id` int(11) NOT NULL,
  `is_completed` tinyint(1) DEFAULT 0,
  `date_marked` datetime DEFAULT current_timestamp()
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_general_ci;

--
-- Indexes for dumped tables
--

--
-- Indexes for table `feedback`
--
ALTER TABLE `feedback`
  ADD PRIMARY KEY (`feedback_id`),
  ADD KEY `feedback_ibfk_1` (`user_id`);

--
-- Indexes for table `forum_replies`
--
ALTER TABLE `forum_replies`
  ADD PRIMARY KEY (`reply_id`),
  ADD KEY `forum_replies_ibfk_1` (`thread_id`),
  ADD KEY `forum_replies_ibfk_2` (`user_id`);

--
-- Indexes for table `forum_threads`
--
ALTER TABLE `forum_threads`
  ADD PRIMARY KEY (`thread_id`),
  ADD KEY `forum_threads_ibfk_1` (`user_id`);

--
-- Indexes for table `generated_quizzes`
--
ALTER TABLE `generated_quizzes`
  ADD PRIMARY KEY (`quiz_id`),
  ADD KEY `generated_quizzes_ibfk_2` (`topic_id`),
  ADD KEY `generated_quizzes_ibfk_1` (`user_id`);

--
-- Indexes for table `languages`
--
ALTER TABLE `languages`
  ADD PRIMARY KEY (`language_id`);

--
-- Indexes for table `password_resets`
--
ALTER TABLE `password_resets`
  ADD PRIMARY KEY (`id`);

--
-- Indexes for table `quiz_questions`
--
ALTER TABLE `quiz_questions`
  ADD PRIMARY KEY (`question_id`),
  ADD KEY `quiz_id` (`quiz_id`);

--
-- Indexes for table `reports`
--
ALTER TABLE `reports`
  ADD PRIMARY KEY (`report_id`),
  ADD KEY `reports_ibfk_1` (`user_id`);

--
-- Indexes for table `saved_lessons`
--
ALTER TABLE `saved_lessons`
  ADD PRIMARY KEY (`save_id`),
  ADD KEY `saved_lessons_ibfk_2` (`topic_id`),
  ADD KEY `saved_lessons_ibfk_1` (`user_id`);

--
-- Indexes for table `topics`
--
ALTER TABLE `topics`
  ADD PRIMARY KEY (`topic_id`),
  ADD KEY `topics_ibfk_1` (`language_id`);

--
-- Indexes for table `users`
--
ALTER TABLE `users`
  ADD PRIMARY KEY (`user_id`),
  ADD UNIQUE KEY `email` (`email`);

--
-- Indexes for table `user_progress`
--
ALTER TABLE `user_progress`
  ADD PRIMARY KEY (`progress_id`),
  ADD KEY `user_progress_ibfk_2` (`topic_id`),
  ADD KEY `user_progress_ibfk_1` (`user_id`);

--
-- AUTO_INCREMENT for dumped tables
--

--
-- AUTO_INCREMENT for table `feedback`
--
ALTER TABLE `feedback`
  MODIFY `feedback_id` int(11) NOT NULL AUTO_INCREMENT, AUTO_INCREMENT=2;

--
-- AUTO_INCREMENT for table `forum_replies`
--
ALTER TABLE `forum_replies`
  MODIFY `reply_id` int(11) NOT NULL AUTO_INCREMENT, AUTO_INCREMENT=3;

--
-- AUTO_INCREMENT for table `forum_threads`
--
ALTER TABLE `forum_threads`
  MODIFY `thread_id` int(11) NOT NULL AUTO_INCREMENT, AUTO_INCREMENT=3;

--
-- AUTO_INCREMENT for table `generated_quizzes`
--
ALTER TABLE `generated_quizzes`
  MODIFY `quiz_id` int(11) NOT NULL AUTO_INCREMENT, AUTO_INCREMENT=52;

--
-- AUTO_INCREMENT for table `languages`
--
ALTER TABLE `languages`
  MODIFY `language_id` int(11) NOT NULL AUTO_INCREMENT, AUTO_INCREMENT=15;

--
-- AUTO_INCREMENT for table `password_resets`
--
ALTER TABLE `password_resets`
  MODIFY `id` int(11) NOT NULL AUTO_INCREMENT, AUTO_INCREMENT=2;

--
-- AUTO_INCREMENT for table `quiz_questions`
--
ALTER TABLE `quiz_questions`
  MODIFY `question_id` int(11) NOT NULL AUTO_INCREMENT, AUTO_INCREMENT=207;

--
-- AUTO_INCREMENT for table `reports`
--
ALTER TABLE `reports`
  MODIFY `report_id` int(11) NOT NULL AUTO_INCREMENT, AUTO_INCREMENT=3;

--
-- AUTO_INCREMENT for table `saved_lessons`
--
ALTER TABLE `saved_lessons`
  MODIFY `save_id` int(11) NOT NULL AUTO_INCREMENT, AUTO_INCREMENT=7;

--
-- AUTO_INCREMENT for table `topics`
--
ALTER TABLE `topics`
  MODIFY `topic_id` int(11) NOT NULL AUTO_INCREMENT, AUTO_INCREMENT=133;

--
-- AUTO_INCREMENT for table `users`
--
ALTER TABLE `users`
  MODIFY `user_id` int(11) NOT NULL AUTO_INCREMENT, AUTO_INCREMENT=20;

--
-- AUTO_INCREMENT for table `user_progress`
--
ALTER TABLE `user_progress`
  MODIFY `progress_id` int(11) NOT NULL AUTO_INCREMENT;

--
-- Constraints for dumped tables
--

--
-- Constraints for table `feedback`
--
ALTER TABLE `feedback`
  ADD CONSTRAINT `feedback_ibfk_1` FOREIGN KEY (`user_id`) REFERENCES `users` (`user_id`) ON DELETE CASCADE;

--
-- Constraints for table `forum_replies`
--
ALTER TABLE `forum_replies`
  ADD CONSTRAINT `forum_replies_ibfk_1` FOREIGN KEY (`thread_id`) REFERENCES `forum_threads` (`thread_id`) ON DELETE CASCADE,
  ADD CONSTRAINT `forum_replies_ibfk_2` FOREIGN KEY (`user_id`) REFERENCES `users` (`user_id`) ON DELETE CASCADE;

--
-- Constraints for table `forum_threads`
--
ALTER TABLE `forum_threads`
  ADD CONSTRAINT `forum_threads_ibfk_1` FOREIGN KEY (`user_id`) REFERENCES `users` (`user_id`) ON DELETE CASCADE;

--
-- Constraints for table `generated_quizzes`
--
ALTER TABLE `generated_quizzes`
  ADD CONSTRAINT `generated_quizzes_ibfk_1` FOREIGN KEY (`user_id`) REFERENCES `users` (`user_id`) ON DELETE CASCADE,
  ADD CONSTRAINT `generated_quizzes_ibfk_2` FOREIGN KEY (`topic_id`) REFERENCES `topics` (`topic_id`) ON DELETE CASCADE;

--
-- Constraints for table `quiz_questions`
--
ALTER TABLE `quiz_questions`
  ADD CONSTRAINT `quiz_questions_ibfk_1` FOREIGN KEY (`quiz_id`) REFERENCES `generated_quizzes` (`quiz_id`) ON DELETE CASCADE;

--
-- Constraints for table `reports`
--
ALTER TABLE `reports`
  ADD CONSTRAINT `reports_ibfk_1` FOREIGN KEY (`user_id`) REFERENCES `users` (`user_id`) ON DELETE CASCADE;

--
-- Constraints for table `saved_lessons`
--
ALTER TABLE `saved_lessons`
  ADD CONSTRAINT `saved_lessons_ibfk_1` FOREIGN KEY (`user_id`) REFERENCES `users` (`user_id`) ON DELETE CASCADE,
  ADD CONSTRAINT `saved_lessons_ibfk_2` FOREIGN KEY (`topic_id`) REFERENCES `topics` (`topic_id`) ON DELETE CASCADE;

--
-- Constraints for table `topics`
--
ALTER TABLE `topics`
  ADD CONSTRAINT `topics_ibfk_1` FOREIGN KEY (`language_id`) REFERENCES `languages` (`language_id`) ON DELETE CASCADE ON UPDATE CASCADE;

--
-- Constraints for table `user_progress`
--
ALTER TABLE `user_progress`
  ADD CONSTRAINT `user_progress_ibfk_1` FOREIGN KEY (`user_id`) REFERENCES `users` (`user_id`) ON DELETE CASCADE,
  ADD CONSTRAINT `user_progress_ibfk_2` FOREIGN KEY (`topic_id`) REFERENCES `topics` (`topic_id`) ON DELETE CASCADE;
COMMIT;

/*!40101 SET CHARACTER_SET_CLIENT=@OLD_CHARACTER_SET_CLIENT */;
/*!40101 SET CHARACTER_SET_RESULTS=@OLD_CHARACTER_SET_RESULTS */;
/*!40101 SET COLLATION_CONNECTION=@OLD_COLLATION_CONNECTION */;
